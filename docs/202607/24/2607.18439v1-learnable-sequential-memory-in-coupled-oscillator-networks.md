---
title: Learnable Sequential Memory in Coupled Oscillator Networks
title_zh: 耦合振荡器网络中的可学习序列记忆
authors: "Taosha Guo, Fabio Pasqualetti"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18439v1"
tags: ["query:sr"]
score: 8.0
evidence: 受生物多时间尺度组织启发的可学习序列记忆
tldr: 受大脑皮层多时间尺度组织启发，本文提出一种基于耦合振子网络的序列记忆架构，实现完全连续动力学、指数存储容量和可学习的序列过渡。架构由三层组成：快速Kuramoto层存储相位模式，中间滞后层控制停留时间，慢速注意层路由上下文驱动的过渡，过渡矩阵与存储模式解耦，可自由优化。理论分析证明了每层的稳定性与鲁棒性，数值模拟验证了有效检索。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18439v1/fig-005.webp\", \"caption\": \"Figure 1: (a) The storage graph G6 4 : a disjoint union of four hexagonal cycles (nc = 6), each storing one block of the target phase-difference pattern Φ as edge labels. Edges are colored by cycle: C1 (blue), C2 (red), C3 (green), C4 (purple). The cycles share no nodes or edges, so each carries an independent winding number ka. (b) The stored pattern Φ grouped by cycle, showing each cycle’s phase-difference vector. Each row corresponds to one cycle and is color-matched to the graph on the left. Five of each cycle’s six edge phases are free pattern variables; the sixth (closing) edge is fixed by the winding, so all six phases sum to 2π (winding ka = 1). The resulting configuration is an exponentially stable phase-locked configuration of the Kuramoto dynamics.\", \"page\": 5, \"index\": 5, \"width\": 929, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2607.18439v1/fig-001.webp\", \"caption\": \"Figure 2: Anatomy of one routing cycle of (6). (a) At a vertex α = ek: the fast layer settles to ϕ(k) exactly; recognition saturates, q̃ → ek for γ large; the integrator r → ek on timescale τr; and the routing K⊤r peaks on the column K·,k. Under a permutation K this peak is unique at the next prescribed memory, and the replicator drives α toward the corresponding vertex. (b) During a transition: as α leaves ek, w(α) blends and θ∗(α) deviates from ϕ(k); δ(θ) is far from every signature, so the softmax q̃ is approximately uniform; r remains near ek, and V rises on the transient, closing the gate g. Both effects keep the replicator quiescent. (c) Plateau consolidation: as α approaches the next vertex the fast layer relocks and g reopens, but for a window of length ∼ τr the routing target K⊤r still favors the previous memory, so the replicator drives α toward the new vertex rather than prematurely advancing; once r catches up, the routing rotates forward and the next transition begins. (d) Closed-loop memory sequence: composition of vertex-to-next-vertex flows traces a memory sequence in (θ, r, α) whose combinatorial type is determined by K; for K a cyclic permutation this memory sequence is a heteroclinic cycle traversed indefinitely.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 604}, {\"url\": \"assets/figures/arxiv/2607.18439v1/fig-004.webp\", \"caption\": \"Table 1: Comparison of sequential memory architectures. N : network size; m: number of cycle constraints; ∆ϕ: minimum resolvable phase step (controlled by γ). Bits/pattern for our model follows from (9).\", \"page\": 12, \"index\": 4, \"width\": 980, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.18439v1/fig-003.webp\", \"caption\": \"Figure 3: Numerical simulation of the three-timescale system (6) on G5 5 with M = 10 patterns and cyclic routing, showing one full traversal cycle in steady state (post warm-up). (A) Active pattern j∗(t); each color corresponds to one catalog entry. (B) All 25 edge phase differences δe(t) (solid, colored by cycle) and per-pattern targets ϕj∗ e\", \"page\": 13, \"index\": 3, \"width\": 954, \"height\": 730}, {\"url\": \"assets/figures/arxiv/2607.18439v1/fig-002.webp\", \"caption\": \"Figure 4: Mean dwell time as a function of τr (β = 15, τ = 80 fixed; N = 6, M = 5, cyclic routing). Solid line: closed-form upper bound (7). Markers: empirical mean over independent trials. The bound correctly predicts the linear scaling with τr but overestimates the empirical values by a roughly constant offset, attributable to two conservative assumptions in the derivation: (i) recognition lag and replicator activation are treated as strictly sequential, whereas they overlap in practice; and (ii) the formula uses the εtol-exit criterion, while the simulation detects transitions at the earlier argmaxj αj switch.\", \"page\": 14, \"index\": 2, \"width\": 736, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2607.18439v1/fig-006.webp\", \"caption\": \"Figure 5: Numerical simulation of context-dependent routing (M = 4 patterns on the disjoint-cycle substrate G5 5 , n = N = 25; same parameters as Figure 3 except τr = 200). Left: selected pattern j∗(t) = argmaxj αj(t) as a step function; background shading identifies the active pattern by color. Center: attention weights αj(t). Right: soft routing matrices K(s) (Dirichlet-sampled, zero diagonal); dominant entries 0.59–0.95 encode each context’s successor graph with naturally varied non-successor entries. (A) K = K(0) (weekday): autonomous cycle 0→ 1→ 2→ 3→ 0 → · · · , mean dwell ≈ 460. (B) K = K(1) (weekend): same catalog, same oscillator weights, different cycle 0→2→3→1→0→· · · , mean dwell ≈ 555. (C) Mid-run switch K(0)→K(1) at t = 3200 (dashed line): blue region follows the weekday order; after the switch the system immediately redirects — from pattern 3 it goes to 1 (weekend: 3→ 1) instead of 0 (weekday: 3→ 0), then continues 1→ 0→ 2→· · · following K(1), with no change to any stored pattern or oscillator weight.\", \"page\": 16, \"index\": 6, \"width\": 976, \"height\": 766}]"
motivation: 真实智能体需要处理记忆序列而非孤立快照，现有模型常将过渡结构硬嵌入记忆基底，缺乏灵活性和学习能力。
method: 构建三层多时间尺度耦合系统：Kuramoto振子层存储相位模式，滞后层确保可靠停留时间，注意层学习上下文驱动的序列路由。
result: 理论证明了每层的指数稳定性和鲁棒性，数值模拟展示了成功的序列记忆检索。
conclusion: 该架构实现了连续、高容量、可学习的序列记忆，过渡路由独立可优化，为神经形态计算提供新范式。
---

## 摘要
Hopfield网络确立了静态记忆可以被存储为递归动力系统的能量极小值，然而真正的智能体必须导航记忆序列而非孤立的快照。生物大脑皮层通过时间尺度的分离来解决这一问题：快速的突触动力学编码个体状态，而缓慢的神经调质过程控制状态之间的转换。受这种多时间尺度组织的启发，我们提出了一种完全连续的序列记忆架构，具有指数级存储容量，并且其转换结构由一个独立的路由矩阵承载——该矩阵与存储模式解耦，由输入上下文驱动，可自由选择、优化或从数据中学习，而非硬编码到记忆基底中，因而是可学习的。具体来说，我们构建了一个自主的三时间尺度动力系统，包含三个耦合层：一个快速的Kuramoto层，以指数稳定的锁相配置存储相位模式；一个中间的滞后层，确保可靠的停留时间；以及一个慢速注意力层，路由序列转换。我们对每一层的稳定性和鲁棒性进行了完整的理论分析，并通过序列记忆检索的数值模拟验证了整个系统。

## Abstract
The Hopfield network established that static memories can be stored as energy minima of a recurrent dynamical system, yet real intelligent agents must navigate \emph{sequences} of memories rather than isolated snapshots.Biological cortex addresses this through a separation of timescales: fast synaptic dynamics encode individual states while slow neuromodulatory processes govern transitions between them. Inspired by this multi-timescale organization, we propose a sequential memory architecture that is fully continuous, admits exponential storage capacity, and is learnable in the sense that the transition structure is carried by a separate routing matrix -- decoupled from the stored patterns, driven by the input context, and free to be chosen, optimized, or learned from data rather than hard-wired into the memory substrate. Specifically, we construct an autonomous three-timescale dynamical system with three coupled layers: a fast Kuramoto layer that stores phase patterns as exponentially stable phase-locked configurations, an intermediate hysteresis layer that enforces reliable dwell times, and a slow attention layer that routes sequential transitions. We provide a complete theoretical analysis of the stability and robustness of each layer, and we validate the full system through numerical simulations of sequential memory retrieval.