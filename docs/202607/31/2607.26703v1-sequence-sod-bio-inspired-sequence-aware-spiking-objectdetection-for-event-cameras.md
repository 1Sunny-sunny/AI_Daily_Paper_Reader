---
title: "Sequence-SOD: Bio-inspired Sequence-aware Spiking ObjectDetection for Event Cameras"
title_zh: "Sequence-SOD: 面向事件相机的仿生序列感知脉冲目标检测"
authors: "Katharina Bendig, René Schuster, Didier Stricker"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26703v1"
tags: ["query:sr"]
score: 9.0
evidence: 用于事件相机目标检测的脉冲神经网络，具有时间动态特性
tldr: 事件相机以稀疏事件流感知场景，脉冲神经网络(SNN)虽能利用时间信息，但现有检测器常孤立处理事件区间并重置网络状态，未充分挖掘连续流中的时序线索。本文提出Sequence-SOD，一种序列感知的SNN对象检测器，通过保留跨区间的膜电位，让检测由演化的神经状态驱动，在Gen1数据集上将mAP从23.38提升至26.88，同时保持40Hz预测频率和脉冲计算的能效优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26703v1/fig-001.webp\", \"caption\": \"Fig. 3 Visualization of the predictions on the Gen1 Automotive Detection Dataset [43] with different training sequence lengths along with the ground truth bounding boxes, showing the network behavior in different scenarios (varying density of events and varying number of objects)\", \"page\": 7, \"index\": 1, \"width\": 952, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2607.26703v1/fig-003.webp\", \"caption\": \"Table 3 Comparison to State-of-the-Art on the test split of the Gen1 Automotive Detection Dataset. Information that is approximated, does not apply, or is not available is marked with a star *. The results for the ⋄ marked papers were taken from Gehrig and Scaramuzza [24]\", \"page\": 9, \"index\": 3, \"width\": 1031, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2607.26703v1/fig-002.webp\", \"caption\": \"Fig. 4 Average Spike Rates over all time steps for each convolutional layer\", \"page\": 10, \"index\": 2, \"width\": 654, \"height\": 360}]"
motivation: 现有SNN检测器处理孤立事件区间并重置状态，未能有效利用连续事件流中的时间信息。
method: 提出Sequence-SOD，将事件累加成短区间并序列化输入SSD型脉冲DenseNet，在序列内跨区间保留膜电位，使检测由连续演化的神经状态驱动。
result: 在Gen1 Automotive Detection数据集上，序列感知训练使mAP从单区间训练的23.38提升至26.88（含事件数据增强），预测频率达40Hz。
conclusion: 序列感知训练通过更好地利用时间线索，成为提升事件驱动SNN检测性能的有效补充方向。
---

## 摘要
事件相机遵循视网膜启发的感知原理，以高时间分辨率和宽动态范围异步报告局部亮度变化。脉冲神经网络（SNNs）通过脑启发的动力学，使用稀疏脉冲和泄漏膜电位随时间整合信息，与这些稀疏事件流相辅相成。然而，许多SNN目标检测器处理孤立的事件区间，并为每个预测重置网络状态，从而未能充分利用连续事件流中的时间信息。我们提出Sequence-SOD，一种序列感知的SNN目标检测器，它处理包含多个时间点标签的扩展事件序列。事件被累积成短时间间隔，离散化为时间步，然后按顺序输入到一个SSD风格的脉冲DenseNet，同时保留序列内各间隔的膜电位，使得检测由演化的神经状态驱动，而不是独立重置的输入窗口。在Gen1 Automotive Detection Dataset上，序列感知训练将mAP从单区间训练的23.38提升到无增强的25.30，并在使用事件数据增强时提升到26.88。该模型实现理论预测频率为40 Hz。在扩展事件序列上训练和评估SNN目标检测器，提高了它们利用时间线索的能力，同时保留了稀疏脉冲计算的能效优势。结果凸显了序列感知训练作为基于事件的SNN检测架构改进的补充方向。

## Abstract
Event cameras follow a retina-inspired sensing principle, reporting local intensity changes asynchronously with hightemporal resolution and a wide dynamic range. Spiking Neural Networks (SNNs) complement these sparse event streams through brain-inspired dynamics, using sparse spikes and leaky membrane potentials to integrate information over time. However, many SNN object detectors process isolated event intervals with a single label and reset the network state after each prediction, thereby underusing temporal information in continuous event streams. We introduce Sequence-SOD, a sequence-aware SNN object detector that processes extended event sequences containing labels at multiple time points. Events are accumulated into short intervals, discretized into temporal steps, and fed sequentially to an SSD-style Spiking DenseNet while preserving membrane potentials across intervals within a sequence, so that detection is driven by an evolving neural state instead of independently reset input windows. On the Gen1 Automotive Detection Dataset, sequence-aware training improves mAP from 23.38 for single-interval training to 25.30 without augmentation and to 26.88 withevent-data augmentation. The model achieves a theoretical prediction frequency of 40 Hz. Training and evaluating SNN object detectors on extended event sequences improves their ability to exploit temporal cues while preserving the energy-efficiency benefits of sparse spiking computation. The results highlight sequence-aware training as a complementary direction to architectural improvements for event-based SNN detection.

---

## 论文详细总结（自动生成）

好的，以下是基于论文内容的详细中文总结。

### 1. 论文的核心问题与整体含义
*   **研究背景**：事件相机受视网膜启发，能异步感知亮度变化，具有高时间分辨率和高动态范围，非常适合自动驾驶等场景。脉冲神经网络（SNN）因其稀疏脉冲、膜电位累积等脑启发机制，天然适合处理这类稀疏事件流。
*   **核心问题**：现有的SNN目标检测器大多将连续的事件流分割成孤立的短时间窗口进行处理，并在每次预测后**重置网络的内部状态（膜电位）**。这种做法割裂了事件的连续性，**未能充分利用事件流中携带的宝贵时序信息**，限制了检测性能。
*   **整体含义**：论文旨在解决SNN检测器对时序信息利用不足的问题，提出一种**序列感知**的训练和评估策略，让SNN的网络状态（膜电位）在更长的事件序列上持续演进，从而利用历史信息辅助当前时刻的检测，提升整体性能。

### 2. 论文提出的方法论
*   **核心思想**：将连续的事件流处理成包含多个时间区间的长序列，**在整个序列处理过程中保留脉冲神经元的膜电位**，仅在独立序列之间重置。这使得网络能够通过其内部状态整合过去的信息，并根据膜电位的泄漏机制自然遗忘过时信息。
*   **关键技术细节**：
    1.  **序列化数据构建**：
        *   将原始事件流累积成时长为 $\Delta t = 125 \text{ ms}$ 的时间区间。
        *   每个区间内的正负极性事件分别被构建成一个形状为 $(T, 2, H, W)$ 的事件直方图，其中 $T=5$ 是时间步，$2$ 代表极性通道。
        *   一个训练序列由 $s=5$ 个这样的连续时间区间组成，确保序列内包含至少一个标注框。
    2.  **网络架构与处理流程**：
        *   **架构**：采用SSD（Single Shot MultiBox Detector）风格的脉冲DenseNet-121作为骨干网络，使用可训练膜时间常数 $\beta$ 的参数化LIF（PLIF）神经元。
        *   **序列处理**：将一个序列内的每个时间区间的 $T$ 个时间步依次输入网络。
        *   **状态保持**：**关键创新点**——在一个序列的连续多个区间之间，SNN各层的膜电位**不会被重置**，保持连续演变。
        *   **预测与损失**：对于有标签的区间，将网络在该区间内 $T$ 个时间步的输出**平均**后得到最终预测结果，并计算检测损失（Focal Loss + Smooth L1 Loss）；对于无标签区间，仅前向传播更新状态，不计算损失。
*   **公式与算法流程**：
    *   **LIF神经元模型**：膜电位 $U[t]$ 的更新公式为 $U[t] = \beta U[t-1] + WX[t] - S_{out}[t-1]\theta$，分别代表衰减、输入电流和发放脉冲后的重置。
    *   **事件直方图构建**：将一个区间内的每个事件 $e_i=(x_i, y_i, t_i, p_i)$ 通过公式 $\tau_i = \lfloor \frac{t_i - t_a}{t_b - t_a} \cdot T \rfloor$ 分配到对应的时间步索引，并通过克罗内克函数 $\delta(\cdot)$ 累加到直方图张量 $E(\tau, p, x, y)$ 的相应位置。
    *   **算法流程**：输入序列包含 $s$ 个 $\Delta t = 125 \text{ ms}$ 的事件区间 -> 遍历序列，对每个区间不重置SNN状态，输入其 $T=5$ 个时间步 -> 对有标签的区间，累加并平均 $T$ 步输出后计算损失 -> 对无标签区间，仅前向传播。

### 3. 实验设计
*   **数据集**：论文实验**仅使用Gen1 Automotive Detection Dataset**，该数据集包含39小时的驾驶场景事件相机记录，分辨率为304×240，提供了车辆和行人两类目标的包围框标签，但标注频率较低（1-4Hz）。
*   **Benchmark 与对比方法**：
    *   **核心基准 (Baseline)**：与 **ODSNN (Cordone et al.)** 直接对比，因为两者使用了相同的SSD-Spiking DenseNet架构，唯一区别是ODSNN每次处理并重置状态，而本文方法进行序列感知训练。
    *   **与其他先进方法对比**：在结果表格中，将提出的方法（S-SOD）与一系列ANN、RNN、Transformer、Hybrid SNN-ANN及纯SNN检测器进行性能比较，如 RED, RVT-B, HsVT-B, EMS-YOLO, SFOD 等。论文特别指出，与其他纯SNN方法的比较主要是为了展示趋势，其架构上的差异是正交于本研究的。

### 4. 资源与算力
*   **文中明确未提及训练所使用的GPU型号、数量及具体训练时长。**
*   仅提到由于基于GPU模拟SNN时间步的计算方式使得训练长序列在内存和时间上存在挑战，导致**批量大小（batch size）被限制为14**。

### 5. 实验数量与充分性
*   **实验数量**：
    *   **核心消融实验**：对比了序列长度（$s=1$ vs $s=5$）在不同测试序列长度下的表现，这是验证核心思想的关键实验。
    *   **数据增强实验**：探究了水平翻转、缩放等事件数据增强方法对序列感知训练的增益效果。
    *   **基准性能对比**：在Gen1数据集上与多种主流方法进行了性能（mAP）和参数量（Params）的比较。
    *   **能效分析实验**：基于标准CMOS工艺和实测的脉冲发放率，估算了所提SNN及等效ANN的推理能耗。
*   **充分性与公平性**：
    *   **充分性**：实验设计逻辑清晰，通过消融实验直接证明了**序列感知策略本身带来的性能提升**（mAP从23.38升至25.30），并进一步展示了结合数据增强的提升效果。
    *   **公平性**：与**核心基准ODSNN的比较是公平的**，因为网络架构完全相同。与其他方法的比较，论文也审慎地指出了架构差异带来的影响，表明其贡献点在于**训练和评估策略**，而非网络结构。

### 6. 论文的主要结论与发现
*   **序列感知训练有效提升性能**：在相同的SSD-Spiking DenseNet架构下，通过维持跨区间的膜电位进行序列感知训练，检测mAP显著优于传统的单区间训练方式（23.38 -> 25.30）。
*   **数据增强带来额外增益**：在序列感知训练基础上应用事件数据增强（尤其是缩放），可以进一步提升性能至26.88 mAP。
*   **理论预测频率高**：该模型以125 ms事件窗口和25 ms内部时间步运行，理论上可实现40 Hz的预测频率。
*   **能效优势显著**：与等效的ANN相比，SNN因其稀疏计算特性，在推理能耗上具有两个数量级的理论优势（约116倍）。
*   **策略与架构互补**：研究结论指出，序列感知训练是提升事件相机SNN检测性能的一个有效补充方向，与网络架构的改进是相辅相成的。

### 7. 优点
*   **研究视角新颖**：从**数据流处理和网络状态管理**的角度切入，而非设计更复杂的网络结构，巧妙地利用了SNN的内在动力学（膜电位）作为持续记忆，思路具有启发性。
*   **实验论证清晰**：通过与架构完全相同的单区间训练模型对比，隔离并验证了“序列感知”这一单一变量带来的性能提升，论证力度强。
*   **实现简单高效**：该方法不改变底层模型架构，仅改变训练和评估时的数据加载与状态重置策略，易于实现，且能保持理论上的高预测频率和SNN的能效优势。

### 8. 不足与局限
*   **数据集单一**：所有实验仅在Gen1 Automotive Detection Dataset上完成，该数据集场景和目标类别较少，无法充分证明该方法的泛化能力。
*   **架构单一且基准性能较低**：该方法仅在一个较老旧的SSD-Spiking DenseNet架构上进行了验证，且其最优结果（26.88 mAP）仍显著低于许多ANN或混合方法，可能的原因是SNN基础架构能力有限，限制了序列感知策略的发挥上限。
*   **训练效率问题**：论文承认，基于GPU模拟SNN进行长序列训练会伴随高昂的显存和时间成本（例如批量大小受限），这可能导致该方法在更长序列或更大模型上的扩展性受限。
*   **关键参数固定**：诸如序列长度（$s=5$）、时间区间（$\Delta t=125 \text{ ms}$）和时间步（$T=5$）等关键超参数被固定，缺乏对这些参数影响的系统性消融研究。

（完）
