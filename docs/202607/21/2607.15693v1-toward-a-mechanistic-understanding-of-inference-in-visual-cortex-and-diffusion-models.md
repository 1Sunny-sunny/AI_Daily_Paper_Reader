---
title: Toward a mechanistic understanding of inference in visual cortex and diffusion models
title_zh: 迈向对视觉皮层和扩散模型中推断的机制性理解
authors: "Zeyu Yun, Alexander Belsten, Dasheng Bi, Zahra Kadkhodaie, Yubei Chen, Bruno A. Olshausen"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15693v1"
tags: ["query:sr"]
score: 9.0
evidence: 将V1推理建模为扩散模型，提供神经编码的机制理解
tldr: 该研究旨在理解视觉皮层与扩散模型的推断机制，提出一种基于稀疏编码和非因子成对交互先验的递归动力学模型，通过去噪得分匹配训练，在自然图像上学习到的交互矩阵与V1皮层水平连接结构高度相似。模型展现出优秀的去噪性能，能恢复模糊轮廓，其内部机制可直接通过雅可比分解解释，并发现潜在变量自发形成分层表示以增强图像全局一致性。该工作在神经科学和机器学习间架起桥梁，为理解视觉推断的神经回路与扩散模型生成机制提供了可测试的假设和机理性见解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15693v1/fig-001.webp\", \"caption\": \"Figure 2: Emergence of V1-like connectivity and denoising performance. (A) Denoising comparison across increasing noise levels (rows) showing clean, noisy, factorial, and non-factorial reconstructions. (B) Visualization of learned latent interactions for a subset of dictionary elements. Each column is centered on one reference basis function Φi shown in the top row. The yellow/black bar indicates its position and preferred orientation. Rows below show needle plots of its interactions with other basis functions Φj : excitatory and inhibitory components of the learned pairwise matrix Mij , followed by excitatory and inhibitory components of the Gram matrix Gij = Φ⊤ i Φj . Each colored bar is placed at the spatial location and orientation of Φj , and color saturation indicates interaction magnitude.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-002.webp\", \"caption\": \"Figure 3: Mechanistic decomposition of the denoising Jacobian. (A) Dominant eigenvectors of the pixel-space Jacobian J(x) for the non-factorial sparse coding model (σ = 0.17). Boxes denote zoom regions for panel C. (B) The model’s denoised output D(x, σ) on a hidden contour stimulus. (C) Paired pixel-space and latent-space eigenvectors from the highlighted regions. Latent activations are shown as needle plots (orientation denotes the neuron’s preferred receptive field; color denotes activation intensity), revealing that participating neurons strictly align with the completed contour. (D) Unrolling a single column of the latent Jacobian (Equation 8). Perturbing a single active neuron (gold needle) yields unconstrained raw spread (Wei) with off-axis leakage. Subsequent columns show higher-order recurrent terms ((ΣW )nei), demonstrating how sparse gating strictly confines the excitatory (red) and inhibitory (blue) spread to the active collinear context, culminating in the fully integrated impulse response projected into pixel space. (see Figure S3 S4 for visualization of more basis.)\", \"page\": 7, \"index\": 2, \"width\": 826, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-004.webp\", \"caption\": \"Figure 4: Semantic generalization and the emergence of detached neurons. (A-B) Dominant eigenvectors of the pixel-space Jacobian J(x) for a standard U-Net (A) and our non-factorial sparse coding model (B), showing coordinated semantic deformations. (D) Learned dictionary elements (Φ) sorted by norm magnitude. (E) Dictionary norm histogram revealing a structural bifurcation: ∼30% of the population learns near-zero weights, forming functionally decoupled \\\"detached neurons.\\\" (F) Sub-population reconstructions: detached neurons yield no visible features, whereas attached neurons capture local geometry. (G) Denoising PSNR drops catastrophically at high noise when detached neurons are ablated (replaced by spatial or global means). (H) Qualitative ablation results across increasing noise levels.\", \"page\": 8, \"index\": 4, \"width\": 826, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-005.webp\", \"caption\": \"Figure 5: State-dependent excitation and dynamic geometric routing. (A) The learned scaling factor σ2γ(σ) that balances prior vs likelihood contribution in the network’s recurrent drive (Φ⊤Φ+ σ2γ(σ)M ). As visual ambiguity (σ) increases, the network upweights the prior M , relying more heavily on learned co-occurrence statistics. (B) Context-dependent excitatory spread for individual target neurons across varying visual stimuli. The top row displays eight noisy line contexts overlapping the target receptive fields. Subsequent rows visualize the target neurons’ (black needle) excitatory spread for neighboring neurons (red needle), gated by Σ. When context is misaligned with a neuron’s preferred orientation, the collinear spread is suppressed.\", \"page\": 9, \"index\": 5, \"width\": 826, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-003.webp\", \"caption\": \"Figure 6: Generation Performance. (A) Denoising PSNR curves of our non-factorial sparse coding model versus a parameter-matched U-Net across varying noise levels. (B) Reverse diffusion sampling trajectories. (C) Final generated samples.\", \"page\": 10, \"index\": 3, \"width\": 826, \"height\": 187}]"
motivation: 旨在理解视觉皮层中感知推断的神经计算机制，并阐明扩散模型能从有限数据生成新图像的内部工作原理。
method: 基于带有非受限成对交互矩阵的非因子先验，将稀疏编码推断扩展为一般递归动力学模型，并用去噪得分匹配和隐式微分进行训练。
result: 学习到的交互矩阵反映了V1水平连接的结构，模型去噪性能接近标准扩散模型，且可直观揭示递归动态如何赋予自然结构变形高概率，并涌现出潜在变量断开输入的分层表示。
conclusion: 为神经科学提供了关于递归回路功能连接的具体假设，为机器学习揭示了扩散模型通过潜在交互矩阵实现全局一致性和生成泛化的内在机制。
---

## 摘要
我们描述了一个初级视觉皮层（V1）中感知推断的模型，它等价于一个最小扩散模型，其功能可从其参数轻易理解。该模型基于稀疏编码，其中潜变量上的非因子化先验采用无约束的成对交互矩阵形式，将标准稀疏编码推断扩展到一个通用的循环动力系统。我们利用去噪分数匹配目标和隐式微分高效地训练这些循环动力学。在自然图像上训练后，学习到的交互矩阵反映了 V1 浅层中连接具有相似朝向调谐神经元的水平连接的结构。该模型展现出异常出色的去噪性能，能在极端视觉模糊的情况下恢复图像特征，如延伸的轮廓，在泛化状态下几乎匹配标准黑箱扩散架构的行为。由于模型的简洁性，网络的雅可比矩阵可直接根据潜变量间的交互矩阵进行分解，从机制上揭示了循环动力学如何为一族连续的自然结构形变分配高概率。有趣的是，在这个回路中，很大一部分潜变量学会了完全断开与视觉输入的连接，实质上形成了一个层级表征，似乎用以强化图像特征间的全局一致性。总之，该模型和结果桥接了了两个截然不同的领域：对神经科学而言，它生成了关于感知推断任务中循环神经回路功能连接性的具体、可检验的假设；对机器学习而言，它阐明了扩散模型学习到的内部机制，这些机制使它们能从有限的训练集中生成无限多的新颖图像。

## Abstract
We describe a model of perceptual inference in primary visual cortex (V1) equivalent to a minimal diffusion model whose function can be readily understood from its parameters. The model is based on sparse coding with a non-factorial prior over latent variables in the form of an unconstrained, pairwise interaction matrix, extending standard sparse coding inference to a general recurrent dynamical system. We efficiently train these recurrent dynamics using a denoising score-matching objective and implicit differentiation. After training on natural images, the learned interaction matrix mirrors the structure of horizontal connections in superficial layers of V1 that link neurons of similar orientation tuning. This model exhibits exceptionally good denoising performance, restoring image features such as extended contours amid extreme visual ambiguity, nearly matching the behavior of standard, black-box diffusion architectures in generalization regime. Owing to the model's simplicity, the network's Jacobian can be decomposed directly in terms of the interaction matrix between latent variables, revealing mechanistically how the recurrent dynamics assign high probability over a continuous family of natural structural deformations. Intriguingly, within this circuit, a large fraction of latent variables learn to disconnect from visual input altogether, essentially forming a hierarchical representation that appears to enforce global consistency among image features. Together, the model and results bridge two distinct domains: for neuroscience, it generates concrete, testable hypotheses regarding functional connectivity in recurrent neural circuits during perceptual inference tasks; for machine learning, it elucidates the internal mechanisms learned by diffusion models that allow them to generate infinitely many novel images from a finite training set.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

该工作的核心问题是：**如何在机制层面理解视觉皮层中的感知推断，以及扩散模型从有限数据生成新图像的内部工作原理**。研究旨在构建一个既能解释 V1 脑区递归回路功能连接，又能等同为一个最小扩散模型且参数可解释的模型。这样，一方面可为神经科学提供关于视觉推断回路的可检验假设，另一方面能为机器学习揭示扩散模型泛化和生成的内在机制，从而在两个领域间建立桥梁。

### 2. 论文提出的方法论

- **核心思想**：将稀疏编码的推断过程扩展为一个通用的递归动力系统，通过引入**非因子化的成对交互先验**（即潜变量之间的无约束交互矩阵）来捕捉图像结构间的依赖，使模型等价于一个最小扩散模型。
- **关键技术细节**：
  - 基础模型为稀疏编码，潜变量 $z$ 的推断不再独立，而是通过一个非限制的成对交互矩阵 $M$（即 $z$ 的先验为 $p(z) \propto \exp(-\frac{1}{2} z^\top M z)$）进行耦合。
  - 推断动态由递归神经网络实现，形式上可写为 $z_{t+1} = f(z_t, x, \Phi)$，其中 $x$ 为输入，$\Phi$ 为字典。
  - 训练目标为**去噪得分匹配**（denoising score matching），对带噪声的图像 $x_\sigma = x + \sigma \epsilon$，网络学习预测得分 $\nabla_{x_\sigma} \log p(x_\sigma)$。
  - 训练中使用**隐式微分**（implicit differentiation）以高效计算梯度，因为递归推断达到平衡点，无需通过整个动力学过程反向传播。
- **公式/算法流程概览**：
  - 推断方程：$z^* = (\Phi^\top \Phi + \sigma^2 \gamma(\sigma) M)^{-1} \Phi^\top x_\sigma$，其中 $\gamma(\sigma)$ 是一个调节先验与似然相对权重的尺度因子。
  - 去噪输出：$D(x_\sigma, \sigma) = \Phi z^*$。
  - 雅可比可分解为 $J = \frac{\partial D}{\partial x} = \Phi (\Phi^\top \Phi + \sigma^2 \gamma M)^{-1} \Phi^\top$，其结构由字典 $\Phi$ 和交互矩阵 $M$ 决定，使得去噪机制可解释。

### 3. 实验设计

- **数据集**：自然图像（文中未指明具体数据集，可能为常见的自然图像补丁或小型数据集）。
- **Benchmark 与对比方法**：
  - 主要对比标准黑箱扩散架构，具体为**参数匹配的 U‑Net 扩散模型**。
  - 比较指标：不同噪声水平下的去噪 PSNR，以及生成样本质量。
  - 对模型自身进行消融：移除“断开连接”的潜变量（detached neurons），观察去噪性能变化。
- **实验场景**：
  - 去噪任务：展示从极高噪声（极端视觉模糊）中恢复轮廓等结构。
  - 雅可比分析：可视化像素-空间和潜空间的主导特征向量，揭示递归动态如何赋予连续形变高概率。
  - 语义泛化：比较模型与 U‑Net 产生的协调形变，展示潜在表示中自然出现的分层结构。

### 4. 资源与算力

论文未明确提及使用的硬件资源、GPU 型号、数量或训练时长。由于模型规模较小（基于稀疏编码和递归动态），推测训练所需算力远低于大型扩散模型，但无具体数据支持。

### 5. 实验数量与充分性

- 从给出的图注可推断至少包含以下实验组：
  1. 不同噪声水平下的去噪重建（图 2，对比非因子先验与因子先验模型）。
  2. 学习到的交互矩阵可视化及其与 V1 水平连接的类比（图 2）。
  3. 雅可比矩阵的机制分解，揭示轮廓补全和方向选择性扩散（图 3）。
  4. 语义泛化与“断开连接”神经元的发现及消融实验（图 4）。
  5. 状态依赖的激励传播与动态几何路由（图 5）。
  6. 生成性能比较：PSNR 曲线、扩散采样轨迹和生成样本（图 6）。
- 实验设计较为全面，既评估了去噪和生成性能，又通过消融和雅可比分解验证了可解释性和内部机制。对比方法仅参数匹配的 U‑Net，但考虑到论文侧重机制理解而非大规模 benchmark 竞争，这种对比在给定范围内是公平和充分的。但缺乏与更多现代扩散模型变体的比较，也未在标准数据集（如 CIFAR、ImageNet）上报告定量指标。

### 6. 论文的主要结论与发现

- 学习到的潜变量交互矩阵 $M$ 自然再现了 V1 浅层水平连接的特征：相似朝向调谐的神经元之间形成兴奋性共线连接。
- 模型在极端噪声下的去噪能力接近标准 U‑Net 扩散模型，且可通过雅可比矩阵分解清楚解释：递归动态将激励严格限制在活跃的共线上下文内，从而完成轮廓补全等结构化去噪。
- 递归网络中自发涌现出“断开连接”的潜变量群体，它们几乎不再受视觉输入驱动，形成一种分层表征，用以增强图像特征的全局一致性；消融这些神经元会严重损害高噪声下的去噪性能。
- 训练得到的尺度因子 $\gamma(\sigma)$ 随噪声增大而提升先验权重，表明网络在学习根据模糊程度自适应平衡数据似然与结构先验。

### 7. 优点：方法或实验设计上的亮点

- **极简可解释**：模型仅由字典和成对交互矩阵构成，雅可比矩阵可直接分解，所有去噪行为可从参数解析，打破了扩散模型的黑箱性。
- **双向桥接**：成功将视觉神经科学中稀疏编码和水平连接的假说与扩散模型机制关联，为递归回路功能连接提供具体可检验假设。
- **发现分层自发涌现**：揭示潜变量部分断开输入而形成层级表示，为自监督学习中表示形成提供新视角。
- **高效训练过程**：利用隐式微分避免了递归动力学的展开梯度，训练成本低。

### 8. 不足与局限

- **数据集与规模不明**：未明确说明训练图像的种类、数量和分辨率，可能限于二维小补丁，限制了应用的现实视觉任务范围。
- **对比方法单一**：仅与参数匹配的 U‑Net 比较，缺少与更先进的扩散模型或概率推断模型的系统定量基准测试。
- **仅适用于初级视觉特征**：模型设计基于 V1 的方向选择性，难以直接推广到高层语义或复杂自然图像理解任务。
- **未充分讨论生物学对应细节**：虽然交互矩阵类似水平连接，但网络动力学和训练方式与真实皮层仍存在差距，如脉冲时序、反馈连接等未考虑。
- **算力信息缺失**：难以评估计算可扩展性和复现成本。

（完）
