---
title: Scaling Interpretable Transformers with Parity Bottleneck Layers
title_zh: 利用奇偶瓶颈层扩展可解释Transformer
authors: "Andrew Mack, Kraig Yuheng Tou, Mark Henry, Zhengxun Wu, Lauren Greenspan"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20652v1"
tags: ["query:sr"]
score: 7.0
evidence: 提出带奇偶瓶颈的可解释Transformer用于模型透明性
tldr: 语言模型常使用稀疏自编码器（SAE）事后恢复叠加特征，但通过构造训练可解释模型因过完备瓶颈的高成本而不切实际。本文提出ParityTransformer，利用深度奇偶瓶颈（DPB）以无参数代数字典替代每层学习的过完备基，通过层级结构的多级专家混合高效实现稀疏性，并针对硬件优化缩减训练成本。该架构在稀疏探测任务上与SAE相当或更优，且在特征吸收、操控性和因果干预上表现更佳，其特征原生集成于前向传播，克服了SAE可能探测非实际使用特征的问题，为设计可解释模型提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20652v1/fig-001.webp\", \"caption\": \"Table 1: Evaluation suite for the 20B-token ParityTransformer alongside validation-loss-matched GPT baselines. Bold marks the best value within each scale group.\", \"page\": 6, \"index\": 1, \"width\": 834, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-003.webp\", \"caption\": \"Table 3: Training-throughput overhead vs the dense baseline, at matched architecture and identical hardware (4× NVIDIA H200 SXM).\", \"page\": 7, \"index\": 3, \"width\": 732, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-006.webp\", \"caption\": \"Figure 2: Edit success rate (fraction of completions flipped to the target) for varying number of features edited (k) across three established circuits in the mechanistic interpretability literature. Our method (blue) consistently outperforms post-hoc SAEs\", \"page\": 8, \"index\": 6, \"width\": 650, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-008.webp\", \"caption\": \"Figure 4: Poisoned-document retrieval precision at k = 100 across layers for raw activations, Parity Bottleneck features, and SAE features, for triggered queries (L) and background queries (R). Bars show mean precision over five runs and error bars show one standard deviation. The dashed line marks the poisoning rate in the retrieval corpus.\", \"page\": 9, \"index\": 8, \"width\": 751, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-002.webp\", \"caption\": \"Figure 5: SAE explained variance across layers for the post-hoc SAEs trained on the fine-tuned model. Each SAE is trained independently on activations from transformer.h.{layer}.mlp.\", \"page\": 21, \"index\": 2, \"width\": 741, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-004.webp\", \"caption\": \"Figure 6: Poisoned-document retrieval precision for additional values of k. We report triggered-query and background-query precision for raw activations, Parity bottleneck features, and SAE features. Bars show mean precision over five repeated runs, and error bars denote one standard deviation.\", \"page\": 22, \"index\": 4, \"width\": 743, \"height\": 1097}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-007.webp\", \"caption\": \"Figure 7: Steering examples\", \"page\": 27, \"index\": 7, \"width\": 829, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2607.20652v1/fig-005.webp\", \"caption\": \"Table 5: Canonical models used in the paper.\", \"page\": 28, \"index\": 5, \"width\": 822, \"height\": 267}]"
motivation: 现有可解释Transformer训练受限于每层过完备瓶颈的巨大内存和计算开销，难以实用化。
method: 设计了ParityTransformer架构，其核心为深度奇偶瓶颈（DPB），采用无参数代数字典和分层混合专家机制高效实现稀疏激活。
result: ParityTransformer在多项指标上不弱于事后SAE，并在特征吸收、操控和因果干预上展现优势，且特征直接源于模型计算。
conclusion: 该工作证明可构造原生可解释的Transformer，其内部表示在设计上即可解读，迈出了从后验解析到先验构造的关键一步。
---

## 摘要
语言模型被认为表现出叠加现象，即在其残差流中表示的特征数量远超维度数。稀疏自编码器（SAE）旨在事后恢复这些特征，但通过构造即可解释的模型训练一直不切实际，因为每层过完备瓶颈在内存和计算上都极其昂贵。为克服这一问题，我们引入了ParityTransformer，这是一种GPT-2规模的架构，其中间表示在设计上高效且广/稀疏。在每一层，深度奇偶瓶颈（DPB）用无参数代数字典取代了学习的过完备基，提供了确定性的非相干性保证，并消除了在大规模上阻碍每层可解释瓶颈的内存需求。DPB是一种层次结构化的稀疏瓶颈，利用多级专家混合方法高效地强制稀疏性：一种硬件感知的实现，将激活稀疏训练与稠密训练的成本差距缩小至可管理的可解释性税。实验表明，ParityTransformer在稀疏探测任务上至少与事后SAE表现相当，而在特征吸收、操控有效性和细粒度因果干预的度量上表现更优。由于后续计算仅作用于经过稀疏瓶颈幸存的特征，ParityTransformer的特征在构造上就是模型前向传播的原生特征，从而回应了SAE是否探测到模型在计算中实际使用的特征这一问题。我们将此视为迈向训练内部表示在设计上即可解释而非事后恢复的模型的一步。

## Abstract
Language models are thought to exhibit the phenomenon of superposition, representing many more features than dimensions in their residual streams. Sparse autoencoders (SAEs) are designed to recover such features post-hoc, but training models that are interpretable by construction has remained impractical, as a per-layer over-complete bottleneck is prohibitively expensive in both memory and compute. To overcome this issue, we introduce the ParityTransformer, a GPT-2-scale architecture whose intermediate representations are efficient and wide / sparse by design. At each layer, a Deep Parity Bottleneck (DPB) replaces a learned over-complete basis with a parameter-free algebraic dictionary, providing a deterministic incoherence guarantee and eliminating the memory requirements that have prevented per-layer interpretable bottlenecks at scale. A DPB is a hierarchically structured sparse bottleneck which efficiently enforces sparsity using a multi-level mixture-of-experts approach: a hardware-aware implementation that closes the cost gap between activation sparse and dense training to a manageable interpretability tax. Empirically, ParityTransformers perform at least as well as post-hoc SAEs on sparse probing tasks, while out-performing on measures of feature absorption, steering effectiveness, and fine-grained causal interventions. Because subsequent computation acts only on features that survive the sparse bottleneck, the ParityTransformer's features are native to the model's forwards pass by construction, addressing the question of whether SAEs probe features the model actually uses during computation. We see this as a step toward training models whose internal representations are interpretable by design rather than recovered post hoc.