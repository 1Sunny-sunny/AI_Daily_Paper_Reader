---
title: Towards simultaneous decoding of kinetic and kinematic movement parameters during grasp and lift task by noninvasive brain imaging
title_zh: 通过非侵入式脑成像在抓取与提升任务中实现运动学与动力学参数的同时解码
authors: "Parth G. Dangi, Yogesh Kumaar Meena"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24081v1"
tags: ["query:sr"]
score: 9.0
evidence: 从EEG解码运动和动力学参数用于脑机接口
tldr: 研究旨在通过非侵入性脑电图信号同时解码抓取与提举任务中的运动学和动力学参数，以提升脑机接口的直观控制能力。提出偏最小二乘回归、多层感知器和注意力回归器三种模型，在WAY EEG GAL数据集上对比评估。注意力回归器在多参数解码中取得最佳性能（R²=0.8，延迟29.2ms），但单参数解码能力下降；多层感知器表现更一致但精度较低（R²=0.49）。结果表明注意力模型具有应用于实时多命令脑机接口的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24081v1/fig-003.webp\", \"caption\": \"Fig. 1: The proposed framework describes a method for preprocessing EEG signals and extracting features with the EEGForceMap approach [9]. It employs a regression setup to decode multiple parameters, where N denotes the outputs from individual regressor models and P represents the total number of outputs. The regression techniques used include partial least squares (PLS), multi-layer perceptron (MLP), and transformer-based regressor (TBR).\", \"page\": 2, \"index\": 3, \"width\": 1052, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2607.24081v1/fig-002.webp\", \"caption\": \"TABLE I: List of parameters from WAY-EEG-GAL dataset selected for simultaneous decoding (GAL - Grasp and Lift)\", \"page\": 3, \"index\": 2, \"width\": 948, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2607.24081v1/fig-001.webp\", \"caption\": \"Fig. 2: A sample of scatter plots showing autocorrelation across different movement parameters’ ERDS activity in µ band for between-group (top), and within-group (bottom) parameters (Az2: rotation of index finger along Z-axis; Fx1: force applied by the thumb along the X-axis; Fx2: force applied by the index finger along the X-axis)\", \"page\": 4, \"index\": 1, \"width\": 454, \"height\": 735}]"
motivation: 需要同时解码多个运动参数以扩展脑机接口的实用性和控制维度。
method: 提出并比较三种回归模型（偏最小二乘、多层感知器、注意力回归器）从EEG中解码多类运动参数。
result: 注意力回归器在多参数解码时表现最优（R²=0.8），但单参数解码性能下降；多层感知器更稳定但精度较低（R²=0.49）。
conclusion: 注意力模型有望实现实时多命令脑机接口，促进更直观控制设备的发展。
---

## 摘要
脑机接口（BMI）可以帮助活动受限的个体，如中风幸存者或截肢者。开发BMI的一个关键挑战是扩展其可用性和控制能力，这可以通过精确解码多个运动学和动力学参数来实现。为此，我们提出了三种回归模型：偏最小二乘回归器、多层感知机和基于注意力的回归器，以从脑电图（EEG）信号中解码多个运动参数。我们在WAY EEG GAL数据集上评估了这些模型，重点关注它们在受试者特定和受试者独立条件下的表现，并采用了两种策略：一个模型解码所有参数，以及为每个参数分别建立模型的基线方法。在所有回归器中，基于注意力的回归器取得了最佳性能，$R^2$为0.8，延迟为29.2毫秒，展示了同时多参数解码的显著改进。然而，其在单参数解码中的性能有所下降。多层感知机在两种解码类型中表现出更高的一致性，但精度较低（$R^2$=0.49）。这些发现凸显了基于注意力的模型在实时多命令BMI系统中的潜力，并有助于开发更直观的控制设备。

## Abstract
Brain-machine interfaces (BMIs) can assist individuals with limited mobility, such as stroke survivors or amputees. One of the key challenges in developing BMIs is expanding their usability and control, which can be achieved by accurately decoding multiple kinematic and kinetic parameters. To address this, we propose three regression models: partial least squares regressor, multilayered perceptron, and attention based regressor, to decode multiple movement parameters from EEG signals. We evaluated these models on the WAY EEG GAL dataset, focusing on their performance under subject specific and subject independent conditions with two strategies: a single model for all parameters and a baseline with separate models for each parameter. Among all regressors, the attention based regressor achieved the best performance, with an $R^2$ of 0.8 and a latency of 29.2 milliseconds, demonstrating significant improvement in simultaneous multi parameter decoding. However, its performance dropped for single parameter decoding. The multi layered perceptron showed more consistent but lower accuracy across both decoding types ($R^2$ = 0.49). These findings highlight the potential of attention based models for real time multi command BMI systems and contribute to the development of more intuitive control devices.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：脑机接口（BMI）通过解码脑电信号来控制外部设备，能帮助运动障碍人群。现有非侵入式BMI多基于分类器，输出命令数量有限，难以实现对假肢或外骨骼的精细、直觉控制。
- **核心问题**：如何从非侵入式脑电图（EEG）中**同时、连续地解码多个运动学（位置、角度）和动力学（力）参数**，以提升BMI的控制维度和自然度。已有工作多聚焦于解码单一参数，或仅解码同一类型参数，未能同步复原包含力与运动的完整抓取‑提举动作。
- **整体含义**：若能实现高精度、低延迟的多参数同步解码，将大幅推动直觉化、多自由度假肢控制和康复训练的发展。

### 2. 论文提出的方法论
- **整体框架**：沿用了 EEGForceMap 的预处理与特征提取管道，并集成三种回归器来实现多参数解码。
- **信号处理与特征**：
  - 从 WAY‑EEG‑GAL 数据集提取覆盖前运动‑顶叶网络的9个通道。
  - 仅提取事件相关去同步/同步（ERDS）特征，重点增强 Mu 频段（9‑11 Hz）和 Theta 频段（4‑7 Hz）的贡献。
- **回归模型**：
  1. **偏最小二乘回归器（PLS）**：自适应选择主成分数，训练时最小化均方误差（MSE）。
  2. **多层感知机（MLP）**：3层全连接（512‑256‑128 节点），使用 Adam 优化器。
  3. **基于注意力的回归器（Transformer‑based Regressor, TBR）**：4层编码器，模型维度32，4个注意力头，前馈网络维度256；输入序列长度为50个时间步。
- **解码策略**：
  - **整体解码器（total decoder）**：一个模型同时输出全部24个运动参数。
  - **聚合解码器（aggregate decoder）**：为每个参数单独训练一个模型（共24个），再聚合所有预测结果，作为基线。
- **训练设置**：伪在线滑动窗口（0.1 s 窗口，0.05 s 步长），训练/测试/验证比为7:2:1；对 MLP 和 TBR 施加30%输入特征Dropout以防过拟合，训练100个epoch。

### 3. 实验设计
- **数据集**：WAY‑EEG‑GAL，包含12名受试者在不同重量和表面条件下执行抓取‑提举任务时的 EEG 与运动参数记录。本研究固定表面条件，仅变化重量，选取了24个运动参数（6个力分量、9个位置、9个角度）。
- **评估场景**：
  - 受试者特定（subject‑specific）：单受试者数据训练和测试。
  - 受试者独立（subject‑independent）：留一被试交叉验证（LOSO）。
- **比较方法**：
  - 三种回归器（PLS、MLP、TBR）分别在整体解码器和聚合解码器两种策略下进行对比。
  - 性能指标：决定系数 $R^2$（精度）、均方误差 MSE（预测稳定性）和延迟（预处理+特征提取+预测的总时间）。
- **补充分析**：对ERDS特征进行了相关性分析，比较同一类型参数内部与不同类型参数之间的特征相似性。

### 4. 资源与算力
- 文中**未明确说明**所使用的 GPU 型号、数量、单次训练时长等算力细节。仅给出了训练轮次（100 epoch）、序列长度（50）、Dropout 比例（30%）等超参数信息。因此，无法从文本中推算所需计算资源。

### 5. 实验数量与充分性
- **实验组数**：至少包含 3（回归器）× 2（解码策略）× 2（受试者条件）= 12 种主要配置，并对每种配置计算了本文所设的全部指标。此外还有特征空间的相关性分析实验。
- **充分性与客观性**：
  - 实验设计将不同回归范式和策略进行系统对比，并同时考察了精度、误差和实时性，较为全面。
  - 采用统一的预处理管道和滑动窗口，保证了比较的公平性。
  - 但实验仅在一个公开数据集上进行，且未与最近其他先进解码模型（如卷积网络、循环网络等）直接比较；受试者数量有限（12人），LOSO 验证虽增强了泛化性评估，但结论的外推性仍受数据集规模制约。

### 6. 论文的主要结论与发现
- **TBR 在同时多参数解码中表现最优**：在受试者特定条件下，整体解码策略的 $R^2$ 达到 0.8，MSE 低至 4.55，延迟约 29.2 ms，具备实时应用潜力。
- **TBR 的强依赖特性**：当采用聚合解码器单独预测每个参数时，TBR 性能急剧下降（$R^2\approx 0.01$），说明该模型利用了跨参数的时间相关性。
- **MLP 的稳定性**：MLP 在两种策略下表现更一致，但整体精度较低（$R^2\approx 0.49$），且单参数解码精度受训练集大小影响。
- **PLS 不适用于此任务**：传统 PLS 回归在多参数解码中精度极低（$R^2\approx 0.13$），延迟高，难以实用。
- **神经表征的关联性**：ERDS 特征分析显示，运动参数在神经层面并非独立编码，而是存在中高程度的相关性，这解释了多参数同步解码的优势和挑战。

### 7. 优点
- **方法创新**：首次将 Transformer 注意力机制用于非侵入式 EEG 的多参数、多类型（动力学+运动学）运动解码，并验证了其显著优势。
- **系统对比**：在同一框架下比较了传统、深度学习和注意力三种回归范式，揭示了各自在多参数与单参数解码场景下的特性，结论具有指导意义。
- **实时性考量**：不仅关注精度，还测量了端到端延迟，为实际闭环部署提供了依据。
- **可解释性分析**：通过特征相关性分析解释了模型性能差异的神经基础，而非仅报告数字。

### 8. 不足与局限
- **数据集单一**：仅在 WAY‑EEG‑GAL 单一数据集上验证，该数据集采集自抓取‑提举任务，可能不适用于连续运动或更复杂的自然动作场景。
- **受试者样本量小**：仅12名受试者，模型的个体差异鲁棒性和统计效力有待扩展样本后进一步检验。
- **对比基线不完备**：未纳入卷积神经网络、长短期记忆网络等已在 EEG 解码中广泛应用的深度学习回归器，所得“TBR 最优”的结论仅限于当前对比方法内。
- **离线评估**：实验为伪在线分析，缺乏真实闭环 BMI 下的用户适应性验证和反馈影响评估。
- **聚合解码下的 TBR 失效未深究**：文中指出了 TBR 在单参数预测时精度骤降，但未深入分析模型机制或提出改进方案。
- **应用限制**：解码的24个参数均来自抓取‑提举任务，未涉及手臂伸展等前置动作，实际控制假肢时仍需拓展解码范围。

（完）
