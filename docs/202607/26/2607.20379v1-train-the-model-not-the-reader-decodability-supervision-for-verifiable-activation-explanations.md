---
title: "Train the Model, Not the Reader: Decodability Supervision for Verifiable Activation Explanations"
title_zh: 训练模型，而非读者：面向可验证激活解释的可解码性监督
authors: Hiskias Dingeto
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20379v1"
tags: ["query:sr"]
score: 6.0
evidence: 通过重构解释隐藏激活
tldr: 本文揭示了基于重建的激活解释评分无法保证个体声明的真实性，标准方法易产生共适应私有代码。为此，提出RECAP方法，通过协同训练辅助预测器使指定内容可解码，从而支持独立探针验证解释的真实性。实验表明，RECAP可消除私有代码，提高解释真实性，并对抗谎言保持鲁棒，为可解释性与AI安全提供了可检查的机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20379v1/fig-005.webp\", \"caption\": \"Figure 1: The two architectures. (a) A natural-language autoencoder (prior work) (Fraser-Taliente et al. 2026): AV maps an activation to text, AR maps it back, and reconstruction quality is the test; only AV and AR train. The score can stay high while the text asserts an ungrounded specific (red “mirror”). (b) RECAP (this work) trains the target model. Top: co-trained linear heads keep designated content decodable against external targets. Bottom: after training, an independent fresh probe (not the heads) recovers the content (AUC 0.95) and a fresh verbalizer states it truly.\", \"page\": 2, \"index\": 5, \"width\": 1032, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2607.20379v1/fig-001.webp\", \"caption\": \"Figure 2: Decoding content from a real model’s hidden state (one held-out Pile (Gao et al. 2020) passage; fresh probes read the layer-6 state at the marked position). On the RECAP-trained model the probes recover the trailing-window content words and the offset-8 token; on the control the readout is scrambled. Same probe recipe and data; only the target model differs.\", \"page\": 6, \"index\": 1, \"width\": 920, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2607.20379v1/fig-002.webp\", \"caption\": \"Figure 3: One representative held-out story, two target models (seed 1). Each stated specific is marked true or false against the exact tuple and flip-grounded (∆r̃; grounded at τ=0.05). The control explanation passes the test at r̃=0.94 (per-example score; run-level means are 0.71–0.86) while the reconstruction depends on three false claims (codes); the RECAP-trained model’s explanation is grounded-and-true on every pre-tap slot and its false post-tap claim shows ∼0 grounding.\", \"page\": 11, \"index\": 2, \"width\": 445, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2607.20379v1/fig-003.webp\", \"caption\": \"Figure 5: Decodability exceeds verbalizability at scale (Pythia-160M, designated content). A fresh probe reading the reconstructor’s decode-point hidden state (black) recovers the designated content across operating points; the reconstruction verbalizer’s generated precision–recall curve (green, two reconstructor seeds) lies below it, and its operating point sits near 0.60 precision. The content is present and probe-decodable; free-form generation conveys only part of it. The control verbalizer (grey) is at the floor.\", \"page\": 15, \"index\": 3, \"width\": 400, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2607.20379v1/fig-004.webp\", \"caption\": \"Table 11: Recipe choices varied in the program, the values tested, and where the result is reported (main-text sections unless noted); the lower block lists the scale reader-side ablations behind the verbalizability result (§5.4).\", \"page\": 15, \"index\": 4, \"width\": 508, \"height\": 742}]"
motivation: 现有重建式评分无法可靠评估解释的忠实性，高重建分数不意味着声明真实，且易出现模型与解释器共谋的私有编码。
method: 提出RECAP，在目标模型旁训练线性头，强制其保持指定内容可解码，并利用独立探针评估解释声明的真实性。
result: RECAP使解释真实性提升，消除私有代码，对抗性谎言检测AUC达到0.95（对照仅0.51），且代价低至0.001 nat。
conclusion: RECAP实现了可验证的激活解释，使内部内容可独立检查，对构建可信、可审计的AI系统有重要价值。
---

## 摘要
自然语言自编码器通过重建来评分隐藏激活的解释：若能从解释中重新生成激活，则该解释被视为忠实。该测试在结构上对单个错误声明不敏感：如果翻转某一声明并不改变重建结果，则该声明永远不会受到惩罚。我们展示了该测试以两种方式通过，但两者都不忠实。在发布的Qwen-2.5-7B语言生成器上，解释的重建效果显著高于随机水平，但仅约2%的具体声明依赖于重建，因此该分数追踪的是主旨而非具体事实。在精确的合成真值条件下，标准方法在5/5次运行中发展出协同适应的私有编码（重建所依赖的错误措辞），且不改变目标模型的修正措施也无济于事。我们贡献了两种审计协议，即基于事实与真值的交叉检验和评估器交换，以及RECAP（通过协同训练的辅助预测器实现可读编码）：与目标模型一同训练的线性头，以保持指定内容的可解码性。在RECAP训练的沙盒模型上，新的语言生成器真实地陈述了指定内容，且编码消失，代价仅为+0.001纳特。这在预训练的Pythia-160M上得到复现：内容变得可以可靠地通过探针解码，尽管新的语言生成器仅部分地传达该内容（真实性0.44-0.46，而对照组接近零）。对于可解释性而言，高重建率并不能认证单个声明的真实性。对于AI安全，RECAP使指定的内部内容能够通过探针独立验证，而不是由模型可以操纵的散文来断言：独立探针对语言生成器的真实声明的评分高于其虚假声明（AUC 0.96，而没有RECAP时为0.82）。面对通过编辑解释来最大化重建分数同时说谎（压制约87%的谎言惩罚）的对手，RECAP探针仍然标记出谎言（AUC 0.95），而对照探针则降至随机水平（0.51）。

## Abstract
Natural-language autoencoders score explanations of hidden activations by reconstruction: an explanation is deemed faithful if the activation can be regenerated from it. The test is structurally insensitive to individual false claims: if flipping a claim does not change the reconstruction, the claim is never penalized. We show the test is passed in two ways, neither faithful. On a released Qwen-2.5-7B verbalizer, explanations reconstruct well above chance while ~2% of specific claims are reconstruction-dependent, so the score tracks gist, not specific facts. Under exact synthetic ground truth, the standard recipe develops co-adapted private codes (false wording the reconstruction depends on) in 5/5 runs, and fixes that leave the target model unchanged do not help. We contribute two audit protocols, the grounded-vs-true cross and the evaluator swap, and RECAP (Readable Encodings via Co-trained Auxiliary Predictors): linear heads trained alongside the target model to keep designated content decodable. On RECAP-trained sandbox models, fresh verbalizers state the designated content truly and the codes vanish, at a +0.001-nat cost. This replicates on a pretrained Pythia-160M: the content becomes reliably probe-decodable, though a fresh verbalizer conveys it only in part (truth 0.44-0.46 vs a near-zero control). For interpretability, high reconstruction does not certify individual claims. For AI safety, RECAP makes designated internal content independently checkable against probes rather than asserted by prose a model can game: an independent probe scores the verbalizer's true claims above its false ones (AUC 0.96, vs 0.82 without RECAP). Against an adversary that edits an explanation to maximize the reconstruction score while lying (suppressing ~87% of its lie penalty), the RECAP probe still flags the lies (AUC 0.95) while the control probe collapses to chance (0.51).