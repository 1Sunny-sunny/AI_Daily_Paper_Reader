---
title: Addressing Limited Data in Auditory Attention Decoding with Diffusion Generative Models
title_zh: 利用扩散生成模型应对听觉注意力解码中的数据稀缺问题
authors: "David Rannaleet, Victor Gunnarsson, Bo Bernhardsson, Martin A. Skoglund, Emina Alickovic"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18345v1"
tags: ["query:sr"]
score: 9.0
evidence: 使用扩散模型的EEG听觉注意力解码
tldr: 在听觉注意解码（AAD）中，真实脑电图数据稀缺限制了听力助听器短时间窗口下的性能。本研究使用扩散概率模型生成合成语音诱发脑电图数据，通过数据增强方式扩充训练集。实验表明，结合合成数据显著提升了注意定位分类的AAD性能，验证了该方法缓解数据限制、提高模型鲁棒性的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18345v1/fig-001.webp\", \"caption\": \"Fig. 1: JSD between real and synthetic EEG for two models. The topoplot shows channel-wise JSD, and two channels are highlighted with corresponding histograms (red: real, blue: synthetic). The largest differences appear near zero.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 285}]"
motivation: 真实世界脑电图数据稀少，导致基于短时间窗口的听觉注意解码性能难以提升。
method: 采用扩散概率模型学习脑电图数据结构，生成逼真的合成数据用于数据增强。
result: 在注意定位任务中，加入合成数据训练的模型性能显著优于仅用实测数据（p<0.05）。
conclusion: 扩散模型数据增强有效缓解了训练数据不足，增强了听力助听器短窗口AAD模型的鲁棒性。
---

## 摘要
有限的训练数据制约了用于助听器（HAs）中听觉注意力解码（AAD）的深度学习模型。AAD利用脑电图（EEG）数据解码听者的注意力，从而实现对特定声源的实时跟踪。然而，由于真实世界的语音诱发EEG数据稀缺，在助听器常见的短时间窗口（≤1秒）内实现高AAD性能具有挑战性。为了解决这一问题，我们探索使用扩散概率模型（DPMs）生成合成语音诱发EEG数据。DPMs通过去噪过程学习底层复杂的数据结构，并能生成逼真的样本，适用于数据增强。我们评估了在注意力焦点（LoA）分类任务中使用合成EEG数据增强数据集的效果。实验结果表明，DPMs能够生成逼真的EEG信号，并且与仅在实测EEG数据上训练的模型相比，加入合成数据显著提升了AAD性能（p<0.05）。这些结果凸显了基于扩散的数据增强在缓解训练数据限制、提高助听器应用中短窗口AAD模型稳健性方面的潜力。

## Abstract
Limited training data constrains deep learning models for Auditory Attention Decoding (AAD) in hearing aids (HAs). AAD uses electroencephalogram (EEG) data to decode listener's attention, enabling real-time tracking of specific sound sources. However, achieving high AAD performance with short time windows typical in HAs (<=1s) is challenging due to the scarcity of real-world speech-evoked EEG data. To address this issue, we investigate diffusion probabilistic models (DPMs) for generating synthetic speech-evoked EEG data. DPMs learn the underlying complex data structure through a denoising process and can generate realistic samples suitable for data augmentation. We evaluate the use of synthetic EEG data for augmenting datasets in locus-of-attention (LoA) classification tasks. Our experiments demonstrate that DPMs can generate realistic EEG signals and that incorporating synthetic data significantly improves AAD performance compared to models trained solely on measured EEG data (p<0.05). These results highlight the potential of diffusion-based data augmentation to mitigate training data limitations and improve the robustness of short-window AAD models in HA applications.

---

## 论文详细总结（自动生成）

好的，以下是根据论文内容生成的结构化、深入且客观的中文总结。

### 1. 论文的核心问题与整体含义

-   **研究动机**：听觉注意力解码（AAD）旨在通过脑电图（EEG）信号解码听者在多声源环境中的注意力目标，是实现新一代智能助听器的关键技术。然而，深度学习模型在此任务上，尤其是在助听器所需的短时间窗口（≤1秒）内的性能，严重受限于真实世界语音诱发EEG数据的稀缺性。
-   **核心问题**：如何解决因数据量不足导致的AAD模型训练困难与性能瓶颈？
-   **整体含义**：本研究提出利用扩散概率模型（DPMs）作为数据增强工具，通过生成高质量的合成EEG数据来扩充训练集，旨在缓解数据限制，提升短窗口AAD模型的性能和稳健性，最终推动其在助听器中的实际应用。

### 2. 论文提出的方法论

-   **核心思想**：使用DPMs学习真实语音诱发EEG信号的复杂分布，然后从中采样生成逼真的、具有特定任务标签（如注意左/右声源）的合成EEG数据，并将其作为额外训练样本加入AAD分类器的训练过程。
-   **关键技术细节**：
    -   **扩散模型架构**：
        -   采用**去噪扩散隐式模型（DDIM）**，因其无需顺序执行所有时间步，能实现更高效的样本生成。
        -   基础网络采用适用于图像生成的**U-Net**变体，但其卷积核调整为\(3 \times 3\)，以适应一维时间序列（EEG）的处理需求。
        -   通过**正弦位置嵌入**将扩散时间步信息注入U-Net。
    -   **损失函数**：
        -   **V-预测(V-prediction)**：作为一种替代方案，模型预测目标定义为 \(v \equiv \alpha_t \epsilon - \sigma_t x\)，而非传统地仅预测噪声 \(\epsilon\)（Epsilon损失）。
        -   **频谱损失(Spectral Loss)**：引入基于短时傅里叶变换（STFT）的频谱损失 \(\frac{1}{N} \| |STFT(x)| - |STFT(\hat{x})| \|_2^2\)，以更好地捕捉EEG信号的频域特征。最终损失是多个STFT分辨率下频谱损失的平均值，并与基础的均方误差（MSE）损失进行加权平衡。
    -   **训练细节**：使用余弦调度器 \(\beta_t\)，训练1000个扩散步长，应用AdamW优化器，并采用动态阈值剪裁以抑制合成样本中的极端离群值。

### 3. 实验设计

-   **数据集**：使用一个包含34名听力受损受试者的专有数据集。受试者在6个扬声器（前、侧、后）构成的多声源环境中执行双说话人（男/女声）听力任务。实验记录了64通道EEG数据，最终用于分析的有31名受试者的2420个试次。
-   **数据预处理与任务**：EEG数据经预处理（滤波、降采样至256Hz、去伪迹）后被切分为1秒的时间窗口。任务是**注意力焦点（LoA）分类**，即基于1秒EEG片段，判断受试者注意的左（-30°）还是右（+30°）前方声源。
-   **对比方法与基准**：
    -   **基准（Baseline）**：仅使用真实训练数据训练的LoA分类器（EEGNeX架构）。
    -   **简单增强基线**：向15%的真实训练数据添加高斯噪声（\(\mathcal{N}(0, 0.15)\)）来作为数据增强。
    -   **提出的方法**：将真实训练数据与不同比例（15%， 30%， 60%， 100%原始训练集大小）的、由不同DPM配置生成的合成EEG数据进行混合，来训练分类器。DPM配置包括：Epsilon损失、V-预测损失、频谱损失。分类器EEGNeX的架构与超参数保持与基准一致，以确保对比公平。

### 4. 资源与算力

-   论文中**未明确提及**训练所用GPU的型号、数量及具体训练时长。仅说明了扩散模型训练的批次大小为64，训练了100个epoch，分类器同理。生成45000个1秒EEG样本所需的时间未知。

### 5. 实验数量与充分性

-   **实验数量**：实验覆盖了较全面的配置组合。评估了1个基准模型、1个噪声增强模型和9个扩散模型增强配置（3种损失函数 × 4种合成数据添加比例）。每个配置独立运行了20次以评估均值和置信区间。
-   **实验充分性与公平性**：
    -   **充分性**：通过多配置、多次运行的实验设计，并对结果进行统计检验（Dunnett‘s test），实验设计较为合理和严谨。这足以评估不同扩散模型和混合比例对性能影响的稳定性和显著性。
    -   **公平性**：所有分类器（包括基准模型）均使用相同的架构（EEGNeX）、数据预处理、训练/验证/测试集划分和超参数，确保了对比的公平性。数据集划分策略（同一块内的试次分配到不同集）也避免数据泄露。

### 6. 论文的主要结论与发现

-   **生成数据质量**：定量评估（Jensen-Shannon Distance）表明DPM能够生成与实际EEG数据分布高度相似的合成信号，尤其是在与注意力相关的额-中央脑区。
-   **数据增强有效性**：仅添加噪声的增强方法导致分类性能显著下降。相比之下，基于扩散模型的数据增强能够提升AAD性能。其中，使用Epsilon损失的模型添加60%合成数据和频谱损失模型添加100%合成数据时，分类准确率的提升在统计上显著（\(p < 0.10\)），相较于基准模型提升了约0.7-0.8个百分点。
-   **结论**：扩散生成模型是一种有效的数据增强工具，能够缓解AAD任务中训练数据稀缺的问题，对提升短窗口下的解码精度和鲁棒性具有潜力。

### 7. 优点

-   **方法新颖性**：率先探索了扩散概率模型在语音诱发EEG数据增强中的应用，为解决AAD数据稀缺问题提供了新思路。
-   **技术实现深入**：研究对比了多种扩散模型目标函数（Epsilon、V-pred、Spectral），并引入了频谱损失以更好地保留EEG的时频特征，探索较为深入。
-   **端到端生成**：模型直接生成1秒的64通道EEG片段，而非特征，保留了完整的时空信息。
-   **统计验证严谨**：采用多次独立运行和Dunnett’s检验，为性能提升提供了统计支持。

### 8. 不足与局限

-   **性能提升幅度有限**：尽管统计显著，但准确率提升的绝对值较小（约0.7%），其实际应用效益有待评估。
-   **数据集单一**：所有实验仅在包含特定听力受损老年人的单个数据集上进行，且任务局限于水平面的左/右定位。模型和结论的泛化能力尚需在更多样化、更大规模的数据集和更复杂的任务上验证。
-   **技术限制**：
    -   使用的U-Net架构可能并非处理多通道时间序列数据的最优选择。
    -   未考虑条件生成，即未利用声音刺激作为条件来指导EEG生成，可能限制合成数据的任务特异性。
    -   计算成本高，训练多个复杂的扩散模型非常耗时。
    -   未检查和评估生成样本中是否存在“记忆”或“复制”现象，这可能引入偏差。
-   **潜在偏差风险**：假设每个1秒片段都完美反映了注意力标签，但与真实注意力状态之间可能存在错配，这可能影响模型训练的上限。

（完）
