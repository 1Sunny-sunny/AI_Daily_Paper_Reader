---
title: Multimodal Pretraining for Generalizable EEG Representation Learning
title_zh: 用于可泛化脑电图表征学习的多模态预训练
authors: "Targol Bakhtiarvand, Jugal Kalita, Adham Atyabi"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21384v1"
tags: ["query:sr"]
score: 9.0
evidence: 多模态EEG基础模型用于解码，直接相关
tldr: 现有脑电图模型常局限于特定数据集和任务，泛化性不足。本研究提出一种多模态脑电图基础模型，融合基于Mamba的原始信号编码器、Vision Transformer风格的时频编码器及轻量文本编码器，通过掩码建模、跨视图对比对齐和时序一致性等自监督目标进行预训练，无需标注数据即可学习癫痫相关表示。在CHB-MIT等多个数据集上微调后，模型在癫痫检测任务中达到领先水平，并支持留一被试评估与可解释定位，展现出强大的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21384v1/fig-001.webp\", \"caption\": \"Fig. 1. Architecture of the proposed multimodal EEG foundation model. (A) Input: raw EEG windows and CWT scalograms. (B) Dual Encoder: Mamba-based raw encoder, TF Transformer encoder, and retrievalaugmented text branch, each projecting to 256-D. (C) Fusion and pretraining objectives. For downstream seizure detection, only channel adapters and classification head (7.5% of parameters) are fine-tuned.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2607.21384v1/fig-002.webp\", \"caption\": \"TABLE II LOSO RESULTS PER SUBJECT. EVAL BAL.ACC IS COMPUTED ON THE VALIDATION SUBJECT; TEST BAL.ACC ON THE HELD-OUT TEST SUBJECT.\", \"page\": 4, \"index\": 2, \"width\": 425, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2607.21384v1/fig-003.webp\", \"caption\": \"Fig. 5. GradCAM-based interpretability analysis for a seizure window from subject chb22 (predicted probability = 0.717). Top-left: Raw EEG signals (23 channels) with GradCAM saliency overlay (red indicates high importance), highlighting salient transient events. Top-right: Attention pooling weights over time from the raw branch, showing increasing focus toward later segments. Bottom-left: Time-frequency GradCAM map (frequency × time), with high saliency concentrated in mid-to-high frequency bands corresponding to ictal activity. Bottom-right: Attention weights from the time-frequency branch, confirming frequency-localized importance. These results demonstrate that the model focuses on physiologically meaningful EEG patterns.\", \"page\": 5, \"index\": 3, \"width\": 512, \"height\": 318}]"
motivation: 现有脑电图模型通常局限于特定数据集和任务，难以在不同场景下泛化。
method: 开发了一种多模态基础模型，结合Mamba原始信号编码器、ViT时频编码器和文本编码器，通过掩码建模、跨视图对比对齐和时序一致性损失进行自监督预训练。
result: 在CHB-MIT基准上，单模型AUROC达0.874，集成模型达0.878，留一被试平衡准确率0.558，并实现可解释癫痫定位。
conclusion: 所提出的多模态基础模型能实现鲁棒的跨数据集癫痫检测和定位，具有良好的泛化能力。
---

## 摘要
用于癫痫的脑电图模型通常仅限于特定数据集和任务。这种有限的方法可能使得将这些模型应用于不同数据集或各种情况下具有挑战性。然而，最近在基础模型和自监督学习方面的研究表明，一个适应性的脑电图骨干网络可以支持一系列与脑电图相关的任务。在本研究中，我们开发了一个多模态脑电图基础模型，该模型结合了基于 Mamba 架构的原始信号编码器、用于时频数据的 Vision Transformer（ViT）风格的编码器，以及一个轻量级文本编码器，所有这些都在一个共享的嵌入空间中。预训练过程依赖于几种创新技术，例如掩码建模、跨视图对比对齐和时序一致性损失。这些方法旨在无需标记数据即可创建丰富的、与癫痫发作相关的表征。为了评估我们预训练模型的有效性和泛化能力，我们在经典的 CHB-MIT 癫痫检测基准和额外的癫痫检测数据集上对其进行了微调，并进行了广泛的实验比较了不同的模型变体。在标准的 CHB-MIT 划分上，我们最佳的单模型实现了 0.874 的 AUROC，而集成变体达到了 0.878 AUROC，代表了该基准上的最先进性能。除了标准的训练-测试划分外，我们还评估了留一受试者（LOSO）协议下的性能，这在先前的脑电图癫痫建模工作中很少报告，并凸显了患者独立癫痫检测的困难，在 19 个受试者上的平均 LOSO 平衡准确率为 0.558。跨数据集和评估设置，我们的多模态基础模型实现了稳健的癫痫检测，并能够直接适应新的癫痫检测场景，同时还支持可解释的癫痫定位。

## Abstract
Electroencephalography (EEG) models used for epilepsy are often limited to specific datasets and tasks. This limited approach can make it challenging to apply these models across different datasets or in various situations. However, recent studies in foundation models and self-supervised learning suggest that an adaptable EEG backbone could support a range of EEG related tasks. In this study, we have developed a multimodal EEG foundation model that combines a raw signal encoder based on the Mamba architecture, a Vision Transformer (ViT)-style encoder for time-frequency data, and a lightweight encoder for text, all within a shared embedding space. The pretraining process relies on several innovative techniques, such as masked modeling, cross-view contrastive alignment, and temporal consistency losses. These methods are designed to create rich, seizure-relevant representations without requiring labeled data. To assess the efficacy and generalization of our pretrained model, we fine-tuned it on the canonical CHB-MIT seizure detection benchmark and additional seizure detection datasets, and conducted extensive experiments comparing different model variants. On the standard CHB-MIT split, our best single model achieved an AUROC of 0.874, and an ensemble variant reached 0.878 AUROC, representing state-of-the-art performance on this benchmark. In addition to standard train-test splits, we evaluated performance under a leave-one-subject-out (LOSO) protocol, which is rarely reported in prior EEG seizure modeling work and highlights the difficulty of patient-independent seizure detection, with a mean LOSO balanced accuracy of 0.558 across 19 subjects. Across datasets and evaluation settings, our multimodal foundation model enabled robust seizure detection and straightforward adaptation to new seizure detection scenarios, while also supporting interpretable seizure localization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- 现有的脑电图（EEG）癫痫检测模型通常针对特定数据集和任务进行训练，跨数据集、跨患者、跨采集环境的泛化能力严重不足。
- 大多数监督模型依赖昂贵的专家标注，且学习到的表征难以迁移到新场景；多数现有工作未采用严格的留一受试者（LOSO）评估，导致性能估计过于乐观。
- 最近的基础模型与自监督学习进展表明，一个可适配的通用 EEG 骨干网络有可能支撑多种下游任务。
- 本文旨在构建一个多模态 EEG 基础模型，通过联合建模原始信号、时频图像和文本信息，在不使用标注的条件下预训练出富含癫痫发作信息的表征，从而在多个数据集上实现稳健且可泛化的癫痫检测。

### 2. 论文提出的方法论
**核心思想**：将原始 EEG、连续小波变换（CWT）时频图和文本上下文对齐到同一个嵌入空间，利用多视图互补信息学习癫痫相关表征。

**关键技术细节**：
- **输入**：15 秒不重叠窗口，重采样至 256 Hz，经带通滤波（0.5‒70 Hz）和 z-score 归一化。原始信号形状为 $X_{\text{raw}}\in\mathbb{R}^{C\times T}$（$T=3840$），时频图 $X_{\text{tf}}\in\mathbb{R}^{C\times F\times T}$ 由 CWT（Morlet 小波，$F=64$ 个对数间隔频带）生成。
- **编码器架构**：
  - 原始信号分支：1D 卷积块嵌入（块大小 64）得到 $N=60$ 个 token，送入 Mamba 骨干网络，生成 token 特征 $z_{\text{raw}}$ 和池化后的嵌入 $e_{\text{raw}}\in\mathbb{R}^{256}$。
  - 时频分支：用 ViT 风格的 Conv2D 块嵌入，经轻量 Transformer 编码，得到 $z_{\text{tf}}$ 和 $e_{\text{tf}}\in\mathbb{R}^{256}$。
  - 融合：$e_{\text{fused}} = \text{MLP}([e_{\text{raw}}; e_{\text{tf}}])$，维度 256。
  - 文本分支：从 FAISS 索引的 EEG 语料检索 top-k 文本段落，用冻结的句子编码器（384 维）提取并投影到 256 维，与 EEG 嵌入通过对比损失对齐。
- **预训练目标**（加权组合）：
  $$\mathcal{L} = \lambda_m \mathcal{L}_{\text{mask}} + \lambda_f \mathcal{L}_{\text{future}} + \lambda_c \mathcal{L}_{\text{nce}} + \lambda_t \mathcal{L}_{\text{text}}$$
  - 掩码重建损失（$\mathcal{L}_{\text{mask}}$）：类似 MAE，随机掩码 patch 并重建。
  - 未来预测损失（$\mathcal{L}_{\text{future}}$）：鼓励时序一致性。
  - 对比 InfoNCE 损失（$\mathcal{L}_{\text{nce}}$）：对齐原始信号与时频分支的嵌入。
  - 文本对比损失（$\mathcal{L}_{\text{text}}$）：对齐 EEG 融合嵌入与检索文本的嵌入。
- **微调策略**：仅更新 7.5% 参数（通道适配器和分类头），冻结预训练骨干，使用二元 focal loss（$\gamma=2.0$）处理严重类别不平衡。

### 3. 实验设计
**数据集**：
- 预训练：TUH EEG 和 SEED-DV（未标注数据）。
- 下游癫痫检测：CHB-MIT（24 例难治性小儿癫痫头皮 EEG）。
- 其他用于验证的统一预处理的数据集：SEED-DV（情感标签）等，但主要评估集中在 CHB-MIT。

**Benchmark 与评估协议**：
- **LOSO 协议**：19 名受试者做留一验证，临床最严格，评估跨患者零样本泛化。指标为测试平衡准确率、TPR、TNR。
- **BrainRVQ 固定划分**：受试者 1‒19 训练，20‒21 验证，22‒23 测试，与已发表基础模型（EEGNet、ST-Transformer、BENDR、BIOT、LaBraM、CBraMod、BrainRVQ）对比，指标为平衡准确率、AUC-PR、AUROC。
- **5 折交叉验证集成**：在 chb22‒23 测试受试者上进一步评估鲁棒性，报告 AUROC、AUC-PR、平衡准确率等。
- **可解释性分析**：GradCAM 显著性图和癫痫发作起始定位（受试者 chb22）。

### 4. 资源与算力
- 论文未明确说明 GPU 型号、数量或具体训练时长。
- 仅给出优化设置：AdamW，学习率 $3\times10^{-4}$，权重衰减，批量大小 32，训练 20,000 步，逐步衰减学习率；微调时批量大小 8，梯度累积等效 32，训练 5 个 epoch。
- 整体计算资源需求不明，但参数高效微调（仅更新 7.5% 参数）客观上降低了微调阶段的算力开销。

### 5. 实验数量与充分性
- **多协议评估**：包含 3 种互补的评估设置（LOSO、固定划分、交叉验证集成），从零样本泛化到完整微调均有覆盖。
- **多方法对比**：对比了 7 种以上先前方法（EEGNet、ST-Transformer、BENDR、BIOT、LaBraM、CBraMod、BrainRVQ），均使用相同固定划分。
- **消融与变体分析**：虽未详列消融，但比较了参数高效微调 vs 全参数微调的隐式效果，以及单模型与单集成模型。
- **可解释性实验**：提供了 GradCAM 和发作定位的可视化案例。
- **充分性与公平性**：LOSO 评估遵循临床建议，揭示了严重的跨受试者下降，基准对比采用文献已有协议，比较公平。但 LOSO 仅覆盖 19 名受试者，few-shot 实验仅覆盖 5 名受试者，早期检测仅测试 1 名受试者，规模和统计稳定性仍需加强。

### 6. 论文的主要结论与发现
- 提出的多模态基础模型在固定划分上以仅 7.5% 的参数量微调即达到最先进的平衡准确率（0.781）和 AUC-PR（0.709），相较 BrainRVQ 的 AUC-PR 提升 52.5%，证明预训练表征的有效性。
- 严格 LOSO 测试下平均平衡准确率仅 0.558，TPR 0.570，客观暴露出跨患者癫痫检测的重大挑战，呼吁行业以受试者级别划分作为标准评估。
- 集成模型 AUROC 达到 0.878，超越 BrainRVQ 的 0.871。
- GradCAM 和初始定位分析表明模型关注生理上有意义的癫痫放电模式，并能在 chb22 上提前约 45 秒检测到发作前兆。

### 7. 优点
- **多模态对齐设计**：首次将原始 EEG、时频表示和文本上下文联合对齐，捕捉互补的发作信号。
- **参数高效微调**：仅更新 7.5% 参数即取得优异性能，避免了灾难性遗忘，适合部署。
- **严格的 LOSO 评估**：提供真实世界中跨患者泛化的下界，增强了结果的可信度和临床参考价值。
- **可解释性分析**：通过显著性图和发作定位，增强了模型决策的透明度，有助于临床信任。
- **综合基准对比**：覆盖多种主流方法，对比客观且结果具有说服力。

### 8. 不足与局限
- **LOSO 覆盖不全**：LOSO 仅评估 19 名受试者，少数受试者（如 chb17）出现 TNR 完全崩溃，未深入分析失败原因。
- **少样本校准效果差**：使用 5%‒40% 数据校准几乎无提升，表明模型需大量标注数据才能有效适应新患者，实时个性化能力受限。
- **早期检测仅个案验证**：发作起始定位仅在一个受试者上展示，缺乏统计显著性，难以直接推广。
- **文本分支的领域知识薄弱**：文本分支依赖通用 EEG 知识语料，并非患者特异性临床报告，可能限制语义对齐的丰富度。
- **计算资源信息缺失**：未报告 GPU 细节和总训练时间，不利于复现和资源估计。
- **跨数据集泛化未深入**：主要在 CHB-MIT 上评估，尽管预处理统一，但未报告模型在其他外部独立数据集上的完整结果。

（完）
