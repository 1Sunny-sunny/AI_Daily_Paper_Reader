---
title: A discrete generative model of neuronal spiking activity on microelectrode arrays
title_zh: 微电极阵列上神经元脉冲活动的离散生成模型
authors: "Md Sayed Tanveer, Mohammed A. Mostajo-Radji, Ge Wang"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23907v1"
tags: ["query:sr"]
score: 8.0
evidence: 微电极阵列上神经元脉冲活动的生成模型
tldr: 针对高密度微电极阵列数据稀疏且观测电极多变的问题，本文提出离散生成模型：用残差向量量化自编码器学习共享时空基元词表，因子化掩码Transformer预测活动位置与基元。在31个人脑类器官和海马体数据上，基元复用性高，重建平均精度为平坦分词器的5.2倍，生成优于基线1.4-2.6倍，形成可复用、无实验特定参数的表示。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.23907v1/fig-004.webp\", \"caption\": \"Figure 1: The pipeline. Empty patches bypass the quantizer; the three-level residual ladder is flattened to V = 961 motifs. Assay and clip context condition the activity and motif priors by the routes defined in Section 4.3, and the motif prior additionally reads the activity field emitted by the first prior.\", \"page\": 4, \"index\": 4, \"width\": 829, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2609.23907v1/fig-003.webp\", \"caption\": \"Table 1: Training chronology. Each stage is frozen before the next begins. The supervision column identifies what each stage learns; full schedules and checkpoint criteria are in Appendix F.\", \"page\": 6, \"index\": 3, \"width\": 810, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2609.23907v1/fig-002.webp\", \"caption\": \"Figure 2: What a motif represents, and whether it is reused. (A) The most-used entries, each the mean real voxel patch assigned to it, not a decoder rendering; time collapsed above, six frames below. (B) Entries used by at least k assays, and pairwise vocabulary overlap by preparation type, both rarefied.\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2609.23907v1/fig-001.webp\", \"caption\": \"Figure 3: Effect of conditioning for each model. Hollow marker, that model’s own random-context null; filled marker, full context; the connector is the random-to-full change. Coinciding markers mean unused conditioning in A, C and D; B scores obedience to the descriptor supplied, so a flat B is expected (Table S7). Panels are oriented so rightward is better and have independent x axes. All five context rungs appear in Table S7.\", \"page\": 8, \"index\": 1, \"width\": 829, \"height\": 408}]"
motivation: 现有神经活动生成模型假设固定排序神经元，难以处理高密度微电极阵列产生的稀疏、跨实验变化的二进制脉冲数据。
method: 提出离散生成模型，用残差向量量化自编码器学习共享时空基元词表，并用因子化掩码Transformer预测活动位置和出现的基元。
result: 在31个人脑类器官和海马体数据上，基元广泛复用且跨组织重叠高，重建平均精度比平坦分词器高5.2倍，生成任务平均精度高1.4-2.6倍。
conclusion: 该模型无需实验特定参数，建立了紧凑可复用的神经活动表示，可扩展用于多种神经样本的生成建模。
---

## 摘要
神经活动的生成模型有助于表征组织动力学、比较实验条件，并模拟群体活动，其应用范围涵盖疾病与药物反应研究以及闭环实验。然而，现有方法通常假设一组固定的已分类神经元，而高密度微电极阵列会产生极其稀疏、全阵列范围的二值脉冲体数据，其中观测到的电极子集因实验而异。我们引入一种离散生成模型，使用共享的时空基元词汇表来表示这种活动。残差向量量化自编码器学习基元词汇表，而分解式掩码Transformer预测活动的发生位置以及每个活动位置出现的基元。我们在涵盖人脑类器官和急性离体人海马组织的31项实验上评估了该模型。学习到的基元被广泛重用：实验身份仅解释了基元使用熵的9%，跨组织类型的基元重叠与组织类型内部的重叠相当。当独立于生成先验评估表示质量时，我们的方法在体素级重建平均精度上达到了匹配扁平标记器的5.2倍。在掩码补全和自由生成任务中，完整模型在位点级平均精度上达到匹配生成基线的1.4-2.6倍，并在所有四类生成指标上均优于该基线。这些结果建立了一种紧凑、可复用的全阵列脉冲活动表示，无需学习特定于实验的参数，为跨多种神经制备的生成建模提供了可扩展的基础。

## Abstract
Generative models of neural activity could help characterize tissue dynamics, compare experimental conditions, and simulate population activity for applications ranging from disease and drug-response studies to closed-loop experimentation. Existing approaches, however, typically assume a fixed set of sorted neurons, whereas high-density microelectrode arrays produce extremely sparse, array-wide binary spike volumes in which the observed subset of electrodes varies across assays. We introduce a discrete generative model that represents this activity using a shared vocabulary of spatiotemporal motifs. A residual vector-quantized autoencoder learns the motif vocabulary, while a factorized masked transformer predicts where activity occurs and which motif appears at each active location. We evaluate the model on 31 assays spanning human brain organoids and acute \emph{ex vivo} human hippocampal tissue. The learned motifs are broadly reused: assay identity explains only $9%$ of the entropy in motif use, and motif overlap across tissue types is comparable to overlap within them. When representation quality is evaluated independently of the generative prior, our approach achieves $5.2\times$ the voxel-level reconstruction average precision of a matched flat tokenizer. For masked completion and free generation, the full model achieves $1.4$--$2.6\times$ the site-level average precision of the matched generative baseline and outperforms it across all four families of generation metrics. These results establish a compact, reusable representation for array-wide spiking activity without learned assay-specific parameters, providing a scalable foundation for generative modeling across diverse neural preparations.

---

## 论文详细总结（自动生成）

# 论文总结：《微电极阵列上神经元脉冲活动的离散生成模型》

## 1. 论文的核心问题与整体含义

- **背景**：高密度微电极阵列（HD-MEA）能以单电极分辨率记录数万个位点上的神经脉冲活动，用于类器官、离体脑片等神经制备的动态表征、闭环实验和药物/疾病研究。
- **核心问题**：现有神经活动生成模型通常假设一组固定的、已排序神经元，并结合单位级或会话级参数；而 HD-MEA 产生的是**全阵列范围的、极端稀疏的二值脉冲体数据**，且每次实验观测到的电极子集都不同。这种数据难以用传统固定单元模型直接刻画。
- **整体含义**：论文提出一种**离散生成模型**，用一个共享的时空基元词表表示全阵列脉冲活动，并采用“先预测活动位置，再预测该位置出现哪个基元”的因子化先验，从而**不需要学习任何特定于实验的参数**，为跨多种神经制备的生成建模提供可扩展基础。

## 2. 论文提出的方法论

- **整体流程**：残差向量量化自编码器（tokenizer）学习共享时空基元词表；因子化掩码 Transformer 先验在该词表上建模基元排列；随后进行推理期适配以修正 teacher-forcing 与推理之间的分布失配。

- **输入表示**：
  - 一个 clip 是二值体积 $X\in\{0,1\}^{T\times H\times W}$，其中 $T=48$ 帧（每帧 6 ms）、$H=120$、$W=224$，总体素约 $1.29\times10^6$，体素占用率仅 $1.62\times10^{-4}$。
  - 体积被切成 $(6,15,14)$ 的时空 patch，得到 $8\times8\times16=1024$ 个 token 位点。

- **空白路由（blank route）**：
  - 训练数据中 88.9% 的 patch 无脉冲；tokenizer 对空 patch 直接路由到专用空白嵌入，绕过内容量化器，仅 8.3% 的内容 token 进入残差量化。训练后 91.7% 的 token 走空白路由。

- **残差向量量化（residual VQ）**：
  - 对内容 patch 的编码器输出 $h$，使用三层条件残差量化：
    $$
    z_1 = Q_1(h),\quad
    z_2 = Q_2(h-z_1 \mid z_1),\quad
    z_3 = Q_3(h-z_1-z_2 \mid z_1,z_2),
    $$
    $$
    \tilde z = z_1 + z_2 + z_3.
    $$
  - 三层分别有 32、8、4 个码本条目，构成 $32\times8\times4=1024$ 个叶节点路径；每个残差路径物化为三码向量之和，去重后形成 $V=961$ 个独特基元，另有一个独立空白 token。

- **条件信息**：
  - **全局条件**：每个 assay 有一个固定、种子生成、L2 归一化的 64 维 $\pm1$ 码 $g_r$；Stage 1 学习一个映射 $c_g=f_g(g_r)$，预测该 assay 的活动位点支持图。
  - **局部条件**：每个 clip 有九个**不可学习**的标量描述子 $\ell(X)$（对数平均发放密度、质量二阶矩、活跃位点比例、时间趋势等）；Stage 3 学习映射 $c_\ell=f_\ell(\ell(X))$，匹配文本子直方图。

- **因子化先验**：
  - 将 token 场 $Z$ 分解为活动场 $A$（哪些位置非空）和基元场 $M_Z$（
