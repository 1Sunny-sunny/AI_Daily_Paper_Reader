---
title: Relational Knowledge Distillation Brings DNN Representations Close Enough to Humans to Be Aligned Without Supervision
title_zh: 关系知识蒸馏使深度神经网络表征足够接近人类，从而无需监督即可对齐
authors: "Yuria Shimizu, Soh Takahashi, Takato Horii, Masafumi Oizumi"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.27877v1"
tags: ["query:sr"]
score: 8.0
evidence: 通过关系知识蒸馏使深度网络表征接近人类心理表征，提升神经模型可解释性
tldr: 现有DNN表征与人类心理表征相似性不足。本研究用关系知识蒸馏（RKD）将人类相似性判断的关系结构迁移至预训练DNN，并用Gromov-Wasserstein最优传输（GWOT）进行无监督细粒度对齐评估。在非重叠测试集上，RKD微调后DNN实现了个体对象级对齐，改进源于全局类别结构更接近人类，局部最近邻重叠率基本不变，表明关系迁移可实现无监督对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.27877v1/fig-004.webp\", \"caption\": \"Figure 1: Evaluating relational transfer from humans to DNNs by unsupervised comparison at the fine-grained, individual-object level. (a) Supervised comparison evaluates a human representation (teacher, Rl) and a DNN representation (student, Rm) under a pre-specified stimulus pairing (ti ↔ si): the object correspondence is given, not inferred. (b) Unsupervised comparison assumes no such pairing; the objectto-object correspondence is estimated from each space’s internal distance structure alone. (c) Unsupervised comparison evaluates the estimated correspondence at the fine-grained, individual-object level. Each representation is summarized as a dissimilarity matrix, and the method estimates a transport plan between the human and DNN dissimilarity matrices. Scoring this plan against the ground-truth identity of each individual object yields fine-grained matching. Scoring the same plan instead against the coarse category of each object yields coarse-grained matching (in this example, 1/6 and 4/6, respectively). (d) The evaluation dataset has two levels of granularity: each individual object at the fine-grained level belongs to one coarse-grained category. (e) When relational transfer brings the human and DNN representational structures closer, the transport plan reflects this correspondence as a pronounced identity diagonal, so individual objects become matchable.\", \"page\": 4, \"index\": 4, \"width\": 790, \"height\": 715}, {\"url\": \"assets/figures/arxiv/2608.27877v1/fig-002.webp\", \"caption\": \"Figure 2: Pipeline for relational transfer from humans to DNNs and its evaluation. (a) Training. We performed relational transfer using ImageNet stimuli. We obtained a human psychological embedding (teacher) from human similarity judgments on ImageNet images. We used a pre-trained DNN embedding, taken from the vision encoder’s projection head, as the student. We then fine-tuned this DNN using Relational Knowledge Distillation (RKD; Park et al., 2019) to bring the relations among objects in the DNN embedding closer to those in the human psychological embedding. Specifically, we minimized a distance-wise loss (LD) and an angle-wise loss (LA) defined over the relations among objects. (b) Evaluation. To test generalization, we evaluated relational transfer using THINGS stimuli, after removing concepts that overlapped with the ImageNet training data. We then compared the relational structure of the human, pre-trained-DNN, and fine-tuned-DNN embeddings using an unsupervised comparison method, Gromov– Wasserstein optimal transport. To investigate what drove the fine-grained alignment, we further probed the underlying representational change. One analysis addressed the local neighborhood structure, and the other addressed the global structure. Within the global structure, we focused on how the coarse categories were arranged relative to one another.\", \"page\": 7, \"index\": 2, \"width\": 814, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2608.27877v1/fig-005.webp\", \"caption\": \"Table 1: RSA and GWOT results for five CLIP ViT-B/16 variants before and after RKD on the 1,249-object THINGS test set. Values in the RSA column are Spearman correlations (ρ) between human and DNN dissimilarity matrices. Values in the GWOT columns are matching accuracies. For each human object, the top-1 and top5 columns consider, respectively, the single DNN object and the five DNN objects that receive the most transport mass from it. Fine-grained (individual-object-level) matching counts only the identical object as correct, whereas coarse-grained (coarse-category-level) matching counts any object from the same coarse category as correct. Chance-level values are listed for reference.\", \"page\": 19, \"index\": 5, \"width\": 766, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2608.27877v1/fig-006.webp\", \"caption\": \"Figure 3: Unsupervised comparison between the human and DNN representations before and after RKD, for the CLIP ViT-B/16 (LAION-2B) model on the 1,249 THINGS test objects. In (a–e), the 1,249 objects follow the same ordering, in which objects are sorted by coarse category. This ordering is annotated on both axes of (a–e), where the color bars indicate coarse-category membership and the grid lines mark the category boundaries (legend at right). (a–c) Dissimilarity matrices for CLIP before RKD (a), the human embedding (b), and CLIP after RKD (c). (d, e) GWOT transport plans Γ between the human and DNN dissimilarity matrices, before RKD (d) and after RKD (e). Rows are human source objects, and columns are DNN target objects. (f, g) For four randomly selected human source objects (identical in both panels), the five DNN target objects receiving the largest transport mass in Γ: before RKD (f) and after RKD (g). In each row, the source object is shown on the left, and the five targets follow in order of decreasing transport mass.\", \"page\": 20, \"index\": 6, \"width\": 758, \"height\": 796}, {\"url\": \"assets/figures/arxiv/2608.27877v1/fig-003.webp\", \"caption\": \"Figure 4: k-nearest-neighbor overlap rate between the human and DNN representations on the 1,249-object THINGS test set, before and after RKD. (a, b) Mean overlap rates for k = 5 and k = 10, respectively. In each panel, each pair of bars gives the mean overlap rate for one CLIP ViT-B/16 variant, named on the horizontal axis; the blue bar (Before) shows the rate before RKD and the red bar (After) shows the rate after RKD. Error bars denote 95% confidence intervals of the mean across the 1,249 query objects (±1.96 × SEM). The dotted horizontal line marks the chance overlap rate (0.4% for k = 5 and 0.8% for k = 10). (c–f) Five-nearest-neighbor examples for four randomly selected THINGS query objects, the same four objects as in Fig. 3f, g. In each panel, the query object is shown on the left. The three rows give the five nearest neighbors retrieved under the human embedding (Human), under CLIP ViT-B/16 (LAION-2B) before RKD (Before), and under the same model after RKD (After). Within each row, neighbors are ordered from left to right by increasing dissimilarity from the query (Top-1 to Top-5).\", \"page\": 23, \"index\": 3, \"width\": 808, \"height\": 667}, {\"url\": \"assets/figures/arxiv/2608.27877v1/fig-001.webp\", \"caption\": \"Figure 5: Arrangement among the coarse categories in the human and DNN representations before and after RKD, for the CLIP ViT-B/16 (LAION-2B) model. (a–c) Three-dimensional projections of the 794 THINGS test objects across 20 coarse categories, excluding the heterogeneous others category: (a) CLIP before RKD, (b) human psychological embedding, and (c) CLIP after RKD. The projections were obtained with principal component analysis and placed in a shared coordinate frame by Procrustes alignment. Each object is colored by its coarse category according to the legend at the right of the top row. (d–f) 20× 20 inter-coarse-category dissimilarity matrices: (d) CLIP before RKD, (e) human psychological embedding, and (f) CLIP after RKD. The color bars along the left and bottom edges indicate the coarse category of each row and column, using the same legend as in (a–c). Brackets below the matrices indicate Spearman correlations between the DNN and human inter-coarse-category dissimilarity matrices before and after RKD.\", \"page\": 25, \"index\": 1, \"width\": 800, \"height\": 544}]"
motivation: 现有DNN表征与人类心理表征的相似性不足，需要缩小差距以实现细粒度对齐。
method: 使用关系知识蒸馏（RKD）将人类相似性判断的关系结构迁移到预训练DNN，并用Gromov-Wasserstein最优传输（GWOT）进行无监督细粒度对齐评估。
result: 在训练数据非重叠的测试集上，RKD微调后的DNN实现了个体对象级别的无监督对齐，改进源于更接近人类的全局类别距离排序，但局部最近邻重叠率基本不变。
conclusion: 关系迁移使预训练DNN的全局结构足够接近人类，从而无需监督即可实现细粒度对齐。
---

## 摘要
将深度神经网络（DNN）的内部表征与人类心理表征联系起来，对于将 DNN 用作人类视觉的计算模型非常重要。现有 DNN 表征与人类心理表征仍不够相似；人类心理表征无法直接观测，因此通常通过对物体图像的大规模相似性判断来测量。缩小这一差距的一种自然方法是直接将人类表征的关系结构迁移到 DNN 中，此前研究已报告人类-DNN 表征相似性有所提高。然而，这种改进是否在更严格评估下仍然成立，尚有两个方面未经验证：个体物体层面的细粒度对齐，以及推广到源自与训练数据无关的数据集的人类嵌入。在此，我们采用一种无监督比较方法——Gromov-Wasserstein 最优传输（GWOT），它仅从内部距离结构估计人类-DNN 对应关系，从而检验细粒度对齐。我们进一步在一个与训练数据不重叠的精选概念测试集上评估泛化性。我们表明，使用关系知识蒸馏（RKD）这一既有关系迁移方法对预训练 DNN 进行微调，可使 DNN 足够接近人类，从而在该测试集上实现个体物体层面的对齐。我们还表明，这一改进源于更接近人类的全局结构，体现在粗粒度类别之间距离的排序上，而局部人类-DNN 最近邻重叠率基本保持不变。这些发现表明，来自人类的关系迁移使预训练 DNN 的全局结构足够接近人类结构，从而能够在无监督条件下实现细粒度的人类-DNN 对齐。

## Abstract
Linking the internal representations of deep neural networks (DNNs) to human mental representations is important for using DNNs as computational models of human vision. Existing DNN representations remain insufficiently similar to human mental representations, which are not directly observable and are therefore commonly measured through large-scale similarity judgments of object images. A natural approach to narrowing this gap is to directly transfer the relational structure of human representations into DNNs, and previous studies have reported improved human-DNN representational similarity. However, whether this improvement holds under stricter evaluation remains untested in two respects: fine-grained alignment at the individual-object level, and generalization to a human embedding derived from a dataset independent of the training data. Here, we employ an unsupervised comparison method, Gromov-Wasserstein optimal transport (GWOT), which estimates human-DNN correspondences from the internal distance structure alone and thereby tests fine-grained alignment. We further assess generalization on a curated test set of concepts non-overlapping with the training data. We show that fine-tuning pre-trained DNNs with Relational Knowledge Distillation (RKD), an established relational transfer method, brings DNNs close enough to humans to be aligned at the individual-object level on this test set. We also show that this improvement is driven by a more human-like global structure, as reflected in the ordering of distances among coarse categories, while the local human-DNN nearest-neighbor overlap rate remains largely unchanged. These findings indicate that relational transfer from humans brings the global structure of pre-trained DNNs close enough to the human structure to enable fine-grained human-DNN alignment without supervision.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 论文的核心问题与整体含义

- **研究背景与动机**：将深度神经网络（DNN）的内部表征与人类心理表征联系起来，有助于把 DNN 用作人类视觉的计算模型。但现有 DNN 表征与人类心理表征之间仍存在明显差距。
- **人类表征的测量**：人类心理表征无法直接观测，通常通过大规模相似性判断得到人类心理嵌入（psychological embedding）。
- **已有方法的不足**：此前研究尝试把人类表征的关系结构迁移到 DNN，并报告了人类—DNN 表征相似性有所提高。但更严格的评估仍需验证两点：
  - 个体物体层面的细粒度对齐；
  - 迁移结果能否泛化到与训练数据无关的数据集。
- **整体含义**：本文表明，使用关系知识蒸馏（RKD）将人类关系结构迁移到预训练 DNN，可以使 DNN 的全局结构足够接近人类，从而在没有任何监督配对信息的情况下，实现个体物体级别的对齐。

## 2. 论文提出的方法论

- **核心思想**：
  - 将人类相似性判断中的关系结构直接迁移到预训练 DNN 的表征中，而不是依赖有监督的逐对象配对。
  - 在评估阶段，采用无监督比较方法，仅根据两个表征空间各自的内部距离结构来估计对象对应关系。

- **关键技术细节**：
  - **教师表征**：由人类对 ImageNet 图像的大规模相似性判断得到的人类心理嵌入。
  - **学生表征**：预训练 DNN 的视觉编码器 projection head 输出。
  - **关系知识蒸馏（RKD）损失**：微调 DNN，使 DNN 嵌入中对象间的关系更接近人类心理嵌入中的关系。损失由两部分组成：
    - 距离损失 $L_D$，约束对象间距离关系；
    - 角度损失 $L_A$，约束对象间角度关系。
  - **无监督对齐评估**：使用 Gromov–Wasserstein 最优传输（GWOT）比较人类和 DNN 的内部距离结构，估计人类对象与 DNN 对象之间的传输计划 $\Gamma$。
  - **对齐评分**：
    - 细粒度匹配：传输计划中对应到同一具体对象才算正确；
    - 粗粒度匹配：对应到同一粗粒度类别即可算正确。
  - **结构归因分析**：
    - 局部结构：比较人类与 DNN 的 $k$-最近邻重叠率；
    - 全局结构：比较粗粒度类别之间的距离排序。

## 3. 实验设计

- **训练数据与场景**：
  - 使用 ImageNet 刺激获得人类心理嵌入作为教师；
  - 使用预训练 DNN 嵌入作为学生；
  - 通过 RKD 进行关系迁移微调。
- **测试数据与泛化评估**：
  - 使用 THINGS 数据集中的刺激进行测试；
  - 移除了与 ImageNet 训练数据重叠的概念；
  - 主测试集包含 1,249 个对象；
  - 全局结构分析中使用 794 个对象，覆盖 20 个粗粒度类别，排除了异质的 “others” 类别。
- **对比对象与模型**：
  - 对比人类嵌入、预训练 DNN 嵌入、RKD 微调后的 DNN 嵌入；
  - 使用五个 CLIP ViT-B/16 变体（例如 LAION-2B 版本）。
- **评估指标**：
  - RSA：人类与 DNN 不相似矩阵之间的 Spearman 相关系数 $\rho$；
  - GWOT 匹配精度：top-1 和 top-5 的细粒度、粗粒度匹配准确率；
  - $k$-近邻重叠率：$k=5$ 和 $k=10$；
  - 粗类别间不相似矩阵的 Spearman 相关系数。

## 4. 资源与算力

- **计算资源说明**：提供的论文文本中未明确说明 GPU 型号、GPU 数量、训练时长或总计算量。因此无法从现有材料中总结具体算力规模。
- **可确认信息**：方法涉及对多个 CLIP ViT-B/16 变体进行 RKD 微调，但文中未给出硬件配置和训练成本细节。

## 5. 实验数量与充分性

- **主要实验组数**：
  - 使用五个 CLIP ViT-B/16 变体进行 RKD 前后对比；
  - 在一个 1,249 对象的 THINGS 测试集上进行无监督对齐评估；
  - 额外进行局部结构分析（$k$-NN 重叠率）；
  - 额外进行全局结构分析（粗类别间距离排序和可视化）。
- **充分性评价**：
  - 实验设计采用了训练数据与测试数据非重叠的泛化测试，评估比较严格；
  - 指标覆盖了整体相似性、细/粗粒度匹配、局部和全局结构，多维且互补；
  - 涉及五个模型变体，一定程度上增强了结果稳健性。
- **潜在不足**：
  - 模型架构种类单一：全部为 CLIP ViT-B/16，结论是否适用于其他视觉 backbone 尚不清楚；
  - 文本未报告多次重复实验或随机种子信息，结果稳定性无法判断；
  - 缺少系统消融实验，例如单独分析 $L_D$ 与 $L_A$ 的贡献、不同超参数、不同迁移强度等；
  - 未与其他关系迁移方法进行直接比较。

## 6. 论文的主要结论与发现

- 经过 RKD 微调后，预训练 DNN 在非重叠 THINGS 测试集上实现了个体物体级别的无监督对齐。
- 这种改进主要来自更接近人类的全局结构，具体表现为粗类别之间距离排序更接近人类，而局部 $k$-最近邻重叠率基本保持不变。
- 结果表明，来自人类的关系迁移能够使预训练 DNN 的全局结构足够接近人类结构，从而无需监督配对即可实现细粒度的人类—DNN 对齐。

## 7. 优点

- **无监督对齐评估**：使用 GWOT，仅依靠内部距离结构估计对象对应关系，避免了预设配对带来的偏差。
- **严格的泛化测试**：测试集概念与训练数据不重叠，更能说明关系迁移的泛化性。
- **多维度评估体系**：同时使用 RSA、细/粗粒度匹配精度、局部 $k$-NN 重叠率、全局粗类别距离排序，分析全面。
- **模型变体对照**：在五个 CLIP ViT-B/16 变体上验证，增强了结论的可重复性。
- **机制性解释**：将对齐改善归因于全局结构而非局部邻域，提供了对关系迁移作用方式的理解。

## 8. 不足与局限

- **算力信息缺失**：论文未报告计算资源消耗，难以评估方法的实际成本。
- **模型架构单一**：仅测试 CLIP ViT-B/16 系列，结论可能不适用于卷积网络、不同规模 ViT 或其他预训练范式。
- **训练与测试域差异**：教师关系来自 ImageNet，评估在 THINGS 上进行；尽管概念不重叠，但图像分布、类别粒度和拍摄风格差异仍可能影响结果。
- **可重复性信息不足**：未看到多次随机运行、置信区间以外的统计检验或不同初始化的稳定性分析。
- **消融研究有限**：缺少对 RKD 损失各组成部分、损失权重、训练样本数量、迁移强度的系统消融。
- **人类表征的固有噪声**：人类嵌入来自相似性判断，可能包含个体差异、测量噪声或文化偏差，导致教师表征本身不完全稳定。
- **应用范围限制**：研究集中在静态物体图像表征的对齐，未涉及动态视觉、场景理解或更复杂的认知任务。

（完）
