---
title: "RAMamba-Net: A Reliability-Aware and Mamba-Based Multimodal Fusion Network for Auditory Attention Detection"
title_zh: RAMamba-Net：一种面向听觉注意力检测的可靠性感知与基于Mamba的多模态融合网络
authors: "Xingyi He, Ziwei Wang, Dongrui Wu"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11372v1"
tags: ["query:sr"]
score: 8.0
evidence: 时序EEG解码识别注意说话人用于神经导向BCI
tldr: "针对听觉注意解码中EEG信息不完整及现有EEG-EOG融合方法存在的弱跨模态交互、低效时序建模和鲁棒性差等问题，本文提出RAMamba-Net。该网络采用Mamba增强的带感知卷积Transformer捕捉EEG频带特征与长程依赖，双分支时空编码器建模EOG时序与通道关系，并通过跨模态注意力和可靠性感知模块实现显式交互与样本级权重分配。在两个AAD基准上，RAMamba-Net较单模态基线准确率提升5.76%，且鲁棒性和表示判别性显著增强。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11372v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of the proposed RAMamba-Net framework.\", \"page\": 3, \"index\": 5, \"width\": 1052, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2609.11372v1/fig-003.webp\", \"caption\": \"Table 1: Average unimodal classification accuracies (%) of 13 deep models on AVGC and DTU datasets.\", \"page\": 5, \"index\": 3, \"width\": 498, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2609.11372v1/fig-001.webp\", \"caption\": \"Figure 2: t-SNE visualization of feature distributions on AVGC and DTU.\", \"page\": 6, \"index\": 1, \"width\": 500, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2609.11372v1/fig-002.webp\", \"caption\": \"Figure 3: Counterfactual robustness under modality-specific noise perturbations on AVGC and DTU.\", \"page\": 6, \"index\": 2, \"width\": 500, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2609.11372v1/fig-004.webp\", \"caption\": \"Table 3: Ablation study of the proposed RAM modules on AVGC and DTU.\", \"page\": 7, \"index\": 4, \"width\": 500, \"height\": 410}]"
motivation: 现有AAD方法在自然视听场景下融合EEG和EOG时，存在跨模态交互弱、时序建模效率低及对样本变化鲁棒性不足等问题。
method: 提出RAMamba-Net，结合Mamba增强的带感知卷积Transformer处理EEG、双分支时空编码器处理EOG，并引入跨模态注意力和可靠性感知模块进行显式融合与样本级权重估计。
result: "在两个AAD基准上，RAMamba-Net相较单模态基线准确率提升5.76%，且具有更鲁棒的解码和更具判别性的特征表示。"
conclusion: 显式跨模态交互和可靠性感知模块能有效对齐多模态信息并抑制不可靠证据，从而显著提升AAD性能和鲁棒性。
---

## 摘要
听觉注意力解码（AAD）通过生理信号识别被关注的说话人，支持神经调控助听设备和自然的人机交互。脑电图（EEG）是AAD的主要模态，但在自然视听场景中提供的证据不完整，这促使了EEG与眼电图（EOG）的融合。现有方法仍受限于跨模态交互弱、时序建模效率低以及对样本变化鲁棒性差。为解决这些局限，我们提出RAMamba-Net，一种面向AAD的可靠性感知、基于Mamba的多模态融合网络。RAMamba-Net采用Mamba增强的频带感知卷积Transformer来捕捉频带特异的EEG模式和长程时序动态。双分支时空编码器对EOG时序和通道间依赖进行建模。跨模态注意力实现显式模态交互。然后引入可靠性感知模块，估计样本级别的模态权重，用于特征和预测一致性，从而增强多模态融合。在两个AAD基准上的实验表明，RAMamba-Net有效利用了互补的EEG-EOG信息，相比单模态基线准确率提升5.76%，并具有更稳健的解码和判别性表征。进一步分析表明，显式跨模态交互改善了多模态对齐，而可靠性感知模块抑制了不可靠的模态证据，并对信号扰动和参数变化具有鲁棒性。

## Abstract
Auditory attention decoding (AAD) identifies the attended speaker from physiological signals, supporting neuro-steered hearing devices and natural human-machine interaction. Electroencephalography (EEG) is the dominant modality for AAD but provides incomplete evidence in naturalistic audio-visual scenes, motivating EEG and electrooculography (EOG) fusion. Existing approaches remain limited by weak cross-modal interaction, inefficient temporal modeling, and low robustness to sample variations. To address the limitations, we propose RAMamba-Net, a reliability-aware Mamba-based multimodal fusion network for AAD. RAMamba-Net employs a Mamba-enhanced band-aware convolutional Transformer to capture band-specific EEG patterns and long-range temporal dynamics. A dual-branch temporal-spatial encoder models EOG temporal and inter-channel dependencies. Cross-modal attention enables explicit modality interaction. Then, a reliability-aware module is introduced to estimate sample-wise modality weights for feature and prediction consistency, thereby enhancing multimodal fusion. Experiments on two AAD benchmarks demonstrate that RAMamba-Net effectively exploits complementary EEG-EOG information, yielding accuracy gains of 5.76% over unimodal baselines, together with more robust decoding and discriminative representations. Further analyses show that explicit cross-modal interaction improves multimodal alignment, while the reliability-aware module suppresses unreliable modality evidence and is robust to signal perturbation and parameter variation.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

- 论文面向**听觉注意力解码（Auditory Attention Decoding, AAD）**，即从生理信号中推断听者当前关注的说话人，服务于神经调控助听设备、目标说话人增强和自然人人机交互。
- 核心背景是“鸡尾酒会问题”：在多人同时说话的嘈杂声学环境中，听障人群难以聚焦目标说话人。
- **EEG 是 AAD 的主导模态**，但存在信噪比低、易受伪影干扰、跨被试和跨会话泛化差等问题；在自然视听场景中，仅靠 EEG 可能无法完整反映注意力行为。
- **EOG 记录眼动/眼电活动**，已有研究表明注视会跟随注意的语音，因此 EOG 可作为 EEG 的互补信息。但眼动信号也可能引入混淆模式，不应被简单丢弃或直接拼接。
- 现有 EEG-EOG 融合方法主要存在三方面不足：
  1. **跨模态依赖建模不足**：缺少双向、token 级的显式交互；
  2. **时序建模效率不高**：卷积偏局部，自注意力对长序列计算成本高；
  3. **对样本级模态质量变化不鲁棒**：固定权重或直接拼接难以适应不同样本中 EEG/EOG 质量的波动。
- 论文提出 **RAMamba-Net**，通过模态专用编码、跨模态交互和样本级可靠性估计，构建统一的多模态 AAD 框架。

## 二、论文提出的方法论

RAMamba-Net 由骨干网络和 **RAM（Reliability-Aware Module）优化模块**组成。

### 1. 输入表示

- EEG 输入：$X_{EEG}\in\mathbb{R}^{K\times C_{EEG}\times T}$，其中 $K$ 为频带数，$C_{EEG}$ 为 EEG 通道数，$T$ 为时间点数。
- EOG 输入：$X_{EOG}\in\mathbb{R}^{C_{EOG}\times T}$。

### 2. EEG 编码器：Mamba 增强的频带感知卷积 Transformer

- 借鉴 FAConformer 的频带感知卷积 Transformer 架构，并在前端和中端引入 **Mamba 块**。
- 对第 $k$ 个 EEG 频带，其表示由下式得到：

$$
H_k = T^{band}_k\left( M^{mid}_k\left( S_k\left( M^{front}_k\left( X^{EEG}_k \right) \right) \right) \right)
$$

其中：
- $M^{front}_k(\cdot)$ 和 $M^{mid}_k(\cdot)$ 分别为前端和中端 Mamba 块；
- $S_k(\cdot)$ 为频带专用 stem 模块；
- $T^{band}_k(\cdot)$ 为 Transformer 编码器。
- Mamba 块通过选择性状态空间传播时间信息：

$$
h_t = \bar{A}_t h_{t-1} + \bar{B}_t x_t, \quad y_t = C_t h_t
$$

- 前端 Mamba 强化原始频带 EEG 的时间结构，中端 Mamba 进一步丰富高层特征动态。
- 各频带表示沿频带维度拼接后，通过频率感知注意力模块 $R_{EEG}(\cdot)$ 重映射为统一 EEG token 表示 $Z_{EEG}$。

### 3. EOG 编码器：双分支时空编码器

- 借鉴 DBConformer，采用**并行时间分支和空间分支**。
- 时间分支捕捉细粒度时序依赖，空间分支建模通道间依赖：

$$
Z^{temporal}_{EOG}=E_t(X_{EOG}),\quad Z^{spatial}_{EOG}=E_s(X_{EOG})
$$

- 两个分支的输出沿特征维拼接并投影到共享嵌入空间，得到 $Z_{EOG}$。
- 该设计保留 EOG 的时间和空间归纳偏置，并生成可与 EEG 进行跨模态交互的维度对齐 token。

### 4. 跨模态交互

- 采用**双向跨模态注意力**：每个模态的 token 作为 query，另一模态提供 key 和 value。
- 对模态 $m\in\{EEG,EOG\}$，其对偶模态为 $\bar m$，跨模态注意力输出为：

$$
H_{m\leftarrow \bar m}=\mathrm{softmax}\left(\frac{Q_m K_{\bar m}^\top}{\sqrt{d_h}}\right)V_{\bar m}
$$

- 注意力输出通过残差连接、层归一化和前馈网络进行细化：

$$
Z_m^{cm}=Z_m+H_{m\leftarrow \bar m}+\mathrm{FFN}\left(\mathrm{LN}\left(Z_m+H_{m\leftarrow \bar m}\right)\right)
$$

- 双向更新保持各模态自身信息，同时吸收互补证据，促进模态对齐。

### 5. RAM 可靠性感知优化

- 从各模态表示中通过 **stop-gradient + mean pooling** 得到样本级描述 $z_m$：

$$
z_m=\mathrm{MeanPool}\left(\mathrm{stopgrad}(Z_m)\right)
$$

- **特征一致性**：基于余弦相似度，归一化到 $[0,1]$：

$$
s_f=\frac{1}{2}\left(1+\frac{z_{EEG}^\top z_{EOG}}{\lVert z_{EEG}\rVert\lVert z_{EOG}\rVert}\right)
$$

- **预测一致性**：将 $z_m$ 通过轻量单模态分类器得到概率 $P_m$，再用 Jensen-Shannon 散度计算：

$$
s_p=1-\frac{D_{KL}(P_{EEG}\l
