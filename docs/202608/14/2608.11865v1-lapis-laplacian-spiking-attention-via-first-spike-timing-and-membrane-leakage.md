---
title: "Lapis: Laplacian Spiking Attention via First-Spike Timing and Membrane Leakage"
title_zh: Lapis：基于首脉冲时序与膜泄漏的拉普拉斯脉冲注意力
authors: "Kaiwen Tang, Jiaqi Zheng, Zixuan Zhu, Yiqun Wang, Zhanglu Yan, Weng-Fai Wong"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11865v1"
tags: ["query:sr"]
score: 6.0
evidence: 脉冲注意力使用首脉冲时序和膜泄漏进行标记评分
tldr: "脉冲视觉Transformer的注意力评分仍沿用密集点积，未利用脉冲计时。本文提出Lapis脉冲注意力，基于首脉冲时间编码，用查询与键首脉冲延迟向量的L1距离评分，经拉普拉斯核映射为亲和度，其指数衰减匹配LIF膜响应，归一化简化为位移，仅需减法、绝对值和累加。CIFAR-10达96.56% top-1，ImageNet-1K注意力能耗降低14.5倍，6位模型83.25% top-1。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11865v1/fig-003.webp\", \"caption\": \"Figure 1: Rethinking query-key scoring with Lapis. ANN attention compares dense activations, while representative spiking attention compares binary states at one timestep. Lapis instead scores from first-spike timing: channel-wise latency differences are accumulated into a single temporal distance, which a leaky trace converts into an affinity by decaying for that duration.\", \"page\": 2, \"index\": 3, \"width\": 1022, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2608.11865v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of the Lapis block and attention module. A Lapis block follows a residual Transformer structure with Lapis attention and a spiking feed-forward network. Within Lapis attention, the query and key branches provide first-spike latencies whose pairwise ℓ1 distances are mapped to Laplacian affinities. A power-of-two row denominator enables shift-based normalization. The value branch passes through an independent spiking neuron before weighted accumulation. The inset shows the equivalent discrete leakage view: initializing a unit state and applyingDij decay steps yields βDij , equal to the corresponding Laplacian affinity under a unit simulation step.\", \"page\": 4, \"index\": 1, \"width\": 786, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2608.11865v1/fig-002.webp\", \"caption\": \"Table 1: ImageNet-1K classification at 224×224. DT-SNN denotes direct training, while A2S denotes ANN-to-SNN conversion. Energy is the estimated per-image inference energy under the operation-level models used by the corresponding studies. For Lapis, T is the length of the TTFS coding window, with at most one spike per neuron, rather than the number of recurrent simulation steps. †Derived from SpikeZIP-TF’s reported 19.85 W using 64 steps of 1 ms.\", \"page\": 6, \"index\": 2, \"width\": 918, \"height\": 419}]"
motivation: 现有脉冲注意力评分未直接利用脉冲计时，仍沿用密集点积或离散算子。
method: 提出Lapis，基于首脉冲时间编码，用查询与键首脉冲延迟向量的L1距离经拉普拉斯核映射为亲和度，消除乘法。
result: "CIFAR-10达96.56% top-1（与点积差0.53点），ImageNet-1K注意力能耗降低14.5倍，6位模型83.25% top-1、3.28mJ/图。"
conclusion: Lapis以极低算术能量实现接近点积的准确率，高效适用于脉冲视觉Transformer。
---

## 摘要
自注意力已成为脉冲视觉Transformer的核心，但其查询-键打分仍主要继承自稠密网络。现有的脉冲变体要么简化点积打分，要么用离散算子替代它，但脉冲时序作为脉冲网络的本征变量，并未直接定义token之间的关系。我们提出 Lapis，一种脉冲注意力机制：在首脉冲时间编码下，通过查询与键的首脉冲延迟向量之间的 L1 距离为每对 token 打分，并通过拉普拉斯核将该距离映射为亲和度。该核的指数衰减与泄漏积分发放膜的冲激响应相匹配，因此累积的延迟差决定了膜迹的衰减，而行归一化在二的幂取整下可简化为移位操作。因此打分仅需减法、绝对值和累加，并消除了查询与键通道之间的所有乘法。在匹配的主干网络和训练策略下，Lapis 在 CIFAR-10 上达到 96.56% 的 top-1 准确率，与点积打分相差 0.53 个百分点。在 ImageNet-1K 上，与稠密点积注意力相比，它将注意力路径的估计算术能耗降低了 14.5 倍。部署的 6 位模型以每张图像 3.28 mJ 的估计算术能耗达到 83.25% 的 top-1 准确率。

## Abstract
Self-attention has become central to spiking vision transformers, yet its query-key scoring is still largely inherited from dense networks. Existing spiking variants either simplify dot product scoring or replace it with discrete operators, but spike timing, the native variable of a spiking network, does not directly define how tokens are related. We propose Lapis, a spiking attention mechanism that scores each token pair by the L1 distance between its query and key first-spike latency vectors under time-to-first-spike coding, and maps this distance to an affinity through a Laplacian kernel. The kernel's exponential decay matches the impulse response of a leaky integrate-and-fire membrane, so the accumulated latency difference determines the decay of a membrane trace, while row normalization reduces to a bit shift under power-of-two rounding. Scoring therefore needs only subtraction, absolute value, and accumulation, and removes all multiplication between query and key channels. Under a matched backbone and training schedule, Lapis reaches 96.56% top-1 accuracy on CIFAR-10, within 0.53 points of dot-product scoring. On ImageNet-1K, it reduces the estimated arithmetic energy of the attention path by 14.5x relative to dense dot-product attention. The deployed 6-bit model attains 83.25% top-1 accuracy at an estimated arithmetic energy of 3.28mJ per image.