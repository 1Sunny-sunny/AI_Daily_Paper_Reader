---
title: "Quantum-Inspired Hybrid Neural Networks for Neural Decoding: A Controlled Ablation Study of Learnable Quantum Sidecar Integration"
title_zh: 量子启发的混合神经网络用于神经解码：可学习量子侧车集成的受控消融研究
authors: "Diana Legziel Levy, Menachem Finkelstein, Peter Chin, Eilon Vaadia, Sarel Cohen"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22475v1"
tags: ["query:sr"]
score: 9.0
evidence: 从多神经元脉冲栅格中解码神经群体活动
tldr: 本研究将参数化量子电路作为ResNet-50骨干的残差侧车模块，用于31类神经群体解码（想象手写分类）。在严格控制下比较四种变体，发现骨干梯度训练投影可小幅提升准确率并改变表征结构，测量引导训练改善表征几何。消融表明简单浅层架构最有效。采用4量子比特无噪声模拟，不声称量子优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22475v1/fig-001.webp\", \"caption\": \"Table 1: Multi-seed comparison of five variants across four seeds. R = inter/intra cosine centroid ratio. CKA = Linear CKA between baseline and quantum features; lower indicates greater structural reorganization. πstar = learned star-topology weight at convergence. BackboneGrad: input projection trained via backbone pathway only (no gradient through quantum circuit; see Section 3). Geometry-loss: CE + feature geometry + angle geometry objectives (see Section 5.2); R column shows the geometry improvement.\", \"page\": 3, \"index\": 1, \"width\": 1054, \"height\": 700}, {\"url\": \"assets/figures/arxiv/2608.22475v1/fig-002.webp\", \"caption\": \"Table 2: Ablation and variant results (seed 777 unless noted∗). Reference: Quantum-BackboneGrad 86.32%. R = inter/intra cosine centroid ratio. ∆ = Top-1 relative to reference.\", \"page\": 4, \"index\": 2, \"width\": 960, \"height\": 622}, {\"url\": \"assets/figures/arxiv/2608.22475v1/fig-003.webp\", \"caption\": \"Figure 2: Left: per-class representation effects are topology-dependent, not frequency-driven. Right: the star-topology preference is a consistent emergent property of backbone-coupled training.\", \"page\": 6, \"index\": 3, \"width\": 1054, \"height\": 363}]"
motivation: 探索量子电路作为神经解码模型中可学习残差模块的潜力，并系统评估不同集成策略对表征和性能的影响。
method: 在固定数据划分、种子和优化器条件下，将参数化量子电路作为ResNet-50的残差侧车，比较基线、冻结投影、骨干梯度训练投影和测量引导四种变体，并进行九变体消融。
result: "骨干梯度投影在3/4种子中平均准确率提升0.19%并降低与基线特征的CKA相似度，测量引导改善表征几何且不降低准确率，浅层架构最有效。"
conclusion: 量子侧车模块可温和重组神经网络表征，但无量子优势，需在实用硬件约束下谨慎设计。
---

## 摘要
我们研究了将参数化量子电路（PQC）作为残差侧车模块集成到 ResNet-50 主干网络中，用于 31 类神经群体解码——从多神经元脉冲光栅对想象手写进行分类。在严格控制的条件下（固定的数据划分、随机种子和优化器），我们比较了四种模型变体：基线、具有冻结输入投影的量子侧车、具有主干梯度训练投影的量子侧车，以及一种测量引导变体，它将角度编码与电路测量结果对齐。主干梯度变体在 3/4 个随机种子下提高了准确率（平均 +0.19%，95% 置信区间 [-1.10%, +1.48%]），并且一致地降低了与基线特征的线性 CKA 相似度（Δ=-0.025，4/4 个种子），表明表征发生了真正的结构性重组。一项包含九个变体的消融研究确定简单浅层架构是最有效且可复现的配置。测量引导训练持续改善表征几何，同时不降低准确率。所有结果均使用 4 量子比特上的无噪声状态向量模拟，这一设置的选择是为了反映当前近期超导硬件的实际限制；本文不声称相对经典方法具有量子计算优势。

## Abstract
We study parameterized quantum circuits (PQCs) integrated as residual sidecar modules within a ResNet-50 backbone for 31-class neural population decoding---imagined handwriting classification from multi-neuron spike rasters. Under strictly controlled conditions (fixed data splits, seeds, and optimizer), we compare four model variants: baseline, quantum sidecar with frozen input projection, quantum sidecar with backbone-gradient-trained projection, and a measurement-guided variant that aligns angle encodings with circuit measurement outcomes. The backbone-gradient variant improves accuracy in 3/4 seeds (+0.19% mean, 95% CI [-1.10%, +1.48%]) and consistently reduces Linear CKA similarity to baseline features ($Δ=-0.025$, 4/4 seeds), indicating genuine structural reorganization of representations. A nine-variant ablation identifies simple shallow architectures as the most effective and reproducible configuration. Measurement-guided training consistently improves representation geometry without reducing accuracy. All results use noiseless statevector simulation on 4 qubits, a regime chosen to reflect the practical constraints of current near-term superconducting hardware; no quantum computational advantage over classical methods is claimed.

---

## 论文详细总结（自动生成）

# 量子启发的混合神经网络用于神经解码：可学习量子侧车集成的受控消融研究

## 1. 论文的核心问题与整体含义

- **研究背景**：脑机接口（BMI）中的神经群体解码不仅需要高准确率，还需要学习到的表征具备良好的几何质量，以支持跨会话鲁棒性和应对神经漂移。深度网络可能在准确率上表现良好，但学到几何次优的特征空间。
- **核心问题**：参数化量子电路（PQC）通过量子纠缠实现拓扑约束的全局特征混合，这种架构先验与经典 MLP 学习到的相关性在结构上不同。论文研究一个 **浅层 4 量子比特残差侧车模块** 嵌入强经典骨干（ResNet-50）后，能否在严格控制条件下产生可测量、可复现的表征几何效应与分类性能变化。
- **整体含义**：在近期超导硬件的实际约束下，量子侧车模块能够以较小幅度重组网络表征，但总体准确率提升有限；论文明确 **不声称任何量子计算优势**，并强调电路容量是主要瓶颈。

## 2. 方法论

### 2.1 混合残差架构

- 以 ResNet-50 为骨干，在 layer3 特征图 $x \in \mathbb{R}^{C \times H \times W}$（其中 $C=1024$）处插入量子侧车作为残差变换：

$$x' = x + f_{\text{quantum}}(x)$$

- 修改后的特征图继续流经后续骨干层。论文比较了 layer3、layer4 和最终池化特征三种放置位置，layer3 效果最好，因为后续层仍能对电路输出进行共适应。

### 2.2 量子侧车与角度编码

- 对 layer3 特征做全局平均池化得到 $h \in \mathbb{R}^{1024}$。
- 使用两个可学习线性投影分别生成两组 4 维角度向量：

$$\theta_A = W_A h + b_A \in \mathbb{R}^4, \quad \theta_B = W_B h + b_B \in \mathbb{R}^4$$

- 每个角度向量驱动一个 4 量子比特电路，采用固定拓扑：链式（最近邻 CNOT）或星型（qubit 0 作为中心 hub 连接其余量子比特）。
- 测量每个量子比特的 $\mathbb{E}[Z_i]$，并投影回 $\mathbb{R}^{1024}$ 与残差相加。
- 拓扑混合通过软混合权重实现：

$$f_{\text{quantum}} = \pi_A f_A + \pi_B f_B, \quad \pi_k = \frac{e^{\alpha_k}}{\sum_j e^{\alpha_j}}$$

### 2.3 关键训练变体

- **Frozen**：输入投影参数固定在随机初始化，不更新。
- **BackboneGrad**：投影参数可更新，但由于量子模拟不可微，投影只通过经典骨干路径接收梯度，即损失 → FC → layer4 → layer3 → pooled → proj_in；没有梯度从电路输出流回投影，因此投影学习到的是“帮助骨干适应量子模块”的压缩，而不是“最大化电路贡献”的角度。
- **Parameter-shift**：使用参数移位规则提供精确量子梯度：

$$\partial_\theta \langle Z \rangle = \frac{1}{2}\left[\langle Z\rangle\left(\theta+\frac{\pi}{2}\right) - \langle Z\rangle\left(\theta-\frac{\pi}{2}\right)\right]$$

  从而区分“梯度断开”与“电路容量限制”两种可能的瓶颈。
- **Measurement-guided**：不施加外部几何目标，而是奖励投影生成与电路实际测量结果对齐
