---
title: Does EEG Foundation Models Transfer to Speech? A Benchmark on Overt and Imagined Speech Decoding
title_zh: EEG基础模型能否迁移至语音？一项针对外显与想象语音解码的基准研究
authors: "Owais Mujtaba Khanday, Mohamed Baha Ben Ticha, Sanae Belfrouh, Marc Ouellet, Jose A. Gonzalez-Lopez"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27268v1"
tags: ["query:sr"]
score: 9.0
evidence: 评估EEG基础模型在显性和想象语音解码上的表现
tldr: EEG基础模型在其他生理信号任务上表现优异，但其在非侵入式语音解码（包括公开和想象语音）上的迁移能力尚未评估。本研究首次系统基准测试两个流行基础模型与三个强卷积基线，在统一协议下对两个语音数据集进行微调。结果显示，大规模通用预训练未带来一致优势，甚至不如轻量CNN，揭示当前基础模型在语音生产任务上的局限性，并强调开发语音特定基础模型的必要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27268v1/fig-003.webp\", \"caption\": \"Figure 1: Per-subject leave-one-subject-out accuracy on UGRMINDVOICE for the three tasks of increasing linguistic difficulty (stacked top to bottom). Each box shows the distribution over the 14 held-out subjects; dots are individual subjects, the diamond marks the mean, and the dashed line marks chance (1/classes).\", \"page\": 3, \"index\": 3, \"width\": 448, \"height\": 769}, {\"url\": \"assets/figures/arxiv/2607.27268v1/fig-002.webp\", \"caption\": \"Table 2: Main benchmark on UGR-MINDVOICE: foundation models vs. convolutional baselines. Acc = accuracy (%), W-F1 = weighted F1, κ = Cohen’s kappa. Best per column in bold. Chance accuracy is 1/classes (κ = 0).\", \"page\": 4, \"index\": 2, \"width\": 769, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2607.27268v1/fig-001.webp\", \"caption\": \"Table 3: BCIC2020-T3 imagined speech (5 classes), within-subject and leave-one-subject-out (LOSO). Bal.Acc = balanced accuracy (%), W-F1 = weighted F1, κ = Cohen’s kappa; mean±SD over 15 subjects. Chance accuracy = 20% (κ = 0). Best within-subject value per column in bold. Under LOSO no model exceeds chance (all p > 0.18).\", \"page\": 4, \"index\": 1, \"width\": 945, \"height\": 275}]"
motivation: 探明通用EEG基础模型能否有效迁移至极具挑战的语音解码任务。
method: 在统一预处理与微调协议下，用两个语音数据集对比两个基础模型（LaBraM、EEGMamba）与三个卷积基线（EEGNet等）。
result: 通用EEG预训练模型在语音任务上未表现出相对于16K参数CNN的稳定优势。
conclusion: 现有通用EEG基础模型尚不能有效迁移至语音解码，需开发语音专用预训练模型。
---

## 摘要
基于数千小时预训练的EEG基础模型在运动想象、癫痫检测、睡眠分期和情绪识别等任务上已展现出优于特定任务网络的性能，但它们在语音解码——这一最具挑战性的非侵入式脑机接口应用——上的迁移能力尚未得到检验。本研究首次对EEG基础模型进行系统性基准测试，使用两个语料库：UGR-MINDVOICE（外显和内隐的伊比利亚西班牙语）和BCI Competition 2020 Track 3（想象语音），与强大的卷积基线模型进行对比。在统一的预处理和微调协议下，我们比较了两种基础模型（LaBraM, EEGMamba）与三种已建立的基线模型（EEGNet, ShallowFBCSPNet, EEGConformer）。大规模EEG预训练在语音任务上相比一个16K参数的CNN并未展现出持续的优势，这表明当前的通用EEG预训练尚未迁移至语音产生任务，这促使我们开发语音专用的基础模型。

## Abstract
EEG foundation models pretrained on thousands of hours have shown large gains over task-specific networks for motor imagery, seizure detection, sleep staging, and emotion recognition, but their transfer to speech decoding-arguably the most demanding non-invasive BCI application-remains untested. We present the first systematic benchmark of EEG foundation models against strong convolutional baselines for speech decoding, using two corpora: UGR-MINDVOICE (overt and covert Iberian Spanish) and BCI Competition 2020 Track 3 (imagined speech). We compare two foundation models (LaBraM, EEGMamba) against three established baselines (EEGNet, ShallowFBCSPNet, EEGConformer) under a unified preprocessing and fine-tuning protocol. Large-scale EEG pretraining yields no consistent advantage over a 16K-parameter CNN on speech tasks, indicating that current general-purpose EEG pretraining does not yet transfer to speech production and motivating speech-specific foundation models.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机和背景**：

- **背景**：近年来，基于海量数据预训练的 EEG 基础模型（如 LaBraM、EEGMamba）在运动想象、癫痫检测、睡眠分期、情绪识别等多种脑电图分析任务中表现优异，显著超越了传统的特定任务网络。然而，语音解码被认为是非侵入式脑机接口中最具挑战性的应用，因为语音产生涉及的皮层动力学机制（如腹侧感觉运动区和颞上区）与基础模型预训练所基于的主流范式（如静息态、运动想象、临床事件）存在显著差异。
- **核心问题**：当前的通用 EEG 基础模型能否有效地将其学到的表征迁移到语音解码任务（包括外显、内隐和想象语音）？
- **整体含义**：本文首次对该问题进行系统性基准评估。结果表明，通用 EEG 预训练并未在语音解码任务上带来相对于紧凑型卷积神经网络的一致优势，这揭示出开发语音专用基础模型的必要性。

## 2. 论文提出的方法论
### 核心思想
在统一的预处理和微调框架下，对比大规模预训练的 EEG 基础模型与经典的卷积基线模型在外显、内隐及想象语音解码任务上的表现，以评估其迁移学习能力。

### 关键技术细节
- **模型架构**：
    - **基线模型**：
        - **EEGNet**：深度可分离卷积网络，通过时域卷积、深度空间卷积和可分离卷积进行特征提取，参数量约 16K。
        - **ShallowFBCSPNet**：模拟滤波器组共空间模式，包含时域和空间卷积、平方非线性、平均池化和对数变换，参数量约 162K。
        - **EEGConformer**：结合卷积前端和 Transformer 编码器，利用多头自注意力机制处理时序标签，参数量约 406K。
    - **基础模型**：
        - **LaBraM**：将 EEG 信号切分为通道片段，通过时域编码器编码后加入可学习的位置编码，再由 Transformer 编码器处理。其预训练的目标是预测被掩码片段的离散神经标签，这些标签由矢量量化标记器在傅里叶幅度和相位重建任务中训练得到。使用公开 LaBraM-Base 权重初始化，并端到端微调。
        - **EEGMamba**：将通道重组为片段，通过时域和基于 FFT 的频域模块编码，并用双向 Mamba2 块进行序列建模。其核心机制为状态空间模型：$y_t = \mathbf{C} h_t$，其中 $h_t = \mathbf{A} h_{t-1} + \mathbf{B} x_t$；连续参数 $\mathbf{A, B}$ 通过零阶保持法和可学习步长 $\Delta$ 进行离散化。预训练目标是通过均方误差重建被遮掩的片段。加载公开权重后全模型微调。

- **统一预处理与微调协议**：所有模型接收 EEG 时段张量，通过线性头输出任务类别 logits。卷积基线使用每通道 z-score 标准化输入；基础模型则使用 $\mu V \times 1/100$ 的缩放，以匹配其预训练时的输入尺度。训练采用 Adam 优化器、交叉熵损失、最多 100 个 epoch 和早停法（patience=10）。

## 3. 实验设计
### 数据集与场景
- **UGR-MINDVOICE**：一个全新的伊比利亚西班牙语语料库，包含15名母语者在 64 导联下的外显（大声说出）和内隐（默想）语音。任务层级递增，包括：
    - 3 分类的**语音模式**解码（外显/内隐/静息）。
    - 6 分类的**语义类别**解码（单词）。
    - 60 分类的**单词身份**解码。
- **BCIC2020-T3**：一个公开的 5 分类想象英语单词/短语数据集，包含15名被试的 64 导联 EEG。用于评估纯想象语音的解码，并提供了一个外部参考点。经核实，该数据集未出现在两个基础模型的预训练数据中。

### 基准模型
- 两个 EEG 基础模型：**LaBraM**（5.8M 参数）、**EEGMamba**（8.3M 参数）。
- 三个卷积基线模型：**EEGNet**、**ShallowFBCSPNet**、**EEGConformer**。

### 实验协议
- **主要协议**：严格的留一被试交叉验证（`LOSO`），即在每个数据集的每一折中，使用除一名被试外的所有数据训练模型，在该名被试数据上进行测试，确保训练和测试被试完全独立。
- **评估指标**：对于多分类任务，报告**平衡准确率**、**Cohen’s Kappa** 系数和**加权 F1 分数**。使用 Wilcoxon 符号秩检验比较模型性能和显著性。

## 4. 资源与算力
文中**并未明确提及**进行实验所使用的 GPU 型号、数量、单次训练时长或总训练时长。仅指出了模型架构的参数量（如 EEGNet 为 16.6K，EEGMamba 为 8.3M）。

## 5. 实验数量与充分性

**实验数量**：

- **主基准测试**：在 2 个数据集上，对 5 个模型进行了所有任务的 `LOSO` 评估，以及针对 BCIC2020-T3 的额外被试内评估。每个数据集的每个任务包含 14 或 15 折（等于被试数）。
- **模式对比实验**：针对 UGR-MINDVOICE，将每个模型在混合数据上训练后，分别在纯外显和纯内隐的测试集上进行评估和对比。
- **额外验证**：对 BCIC2020-T3 测试了 2 秒和 3 秒窗口长度。

**评估**：

- **客观性与公平性**：实验设计高度公平。所有模型在**统一预处理、统一微调协议和严格 LOSO 评估**下进行比较，这是评估模型在真实应用中泛化能力的金标准。特别注意避免了数据泄露，确保测试数据集不在基础模型的预训练语料库中。比较对象包括参数量相差数百倍的模型，能够清晰体现预训练带来的收益。
- **充分性**：实验覆盖了多语言（西班牙语、英语）、多语音模态（外显、内隐、想象）、多任务粒度（从 3 类到 60 类），并能从不同角度（被试内、跨被试）进行分析。统计检验使用合理。总体而言，实验论证是**充分且令人信服的**。

## 6. 主要结论与发现
- **通用预训练无迁移优势**：在语音解码任务上，无论是 LaBraM 还是 EEGMamba，其性能均未能一致且显著地超过仅有 16K 参数的 EEGNet。
- **任务难度差异显著**：只有最简单的“语音模式”解码任务取得了远高于随机水平的准确率（EEGNet 达 61.3%）。对于更难的语义类别和单词身份解码，所有模型的表现均接近随机水平。
- **外显与内隐语音无解碼差异**：辞汇内容的解码失败并非仅限于想象语音。在外显和內隐语音的测试集上，所有模型的解码准确率没有统计学差异，表明从两种模态中都极难提取辞汇信息。
- **跨被试挑战巨大**：在 BCIC2020-T3 数据集的跨被试协议下，所有模型均降至随机水平，表明当前技术无法实现跨被试的想象语音解码。
- 这些发现与基础模型在运动想象、癫痫检测等任务上的显著增益形成**鲜明对比**，揭示了语音产生的独特神经动力学需要更具针对性的预训练策略。

## 7. 优点
- **首创性**：这是**首个**系统性地对 EEG 基础模型在语音解码任务上进行的基准测试，填补了领域空白。
- **严谨的实验设计**：采用统一的预处理和微调流程，并使用可靠的公开数据，确保了比较的公平性。
- **严格的评估协议**：采用留一被试交叉验证评估模型的泛化能力，同时验证了数据无泄漏，确保了结论的可靠性。
- **全面的分析**：评估覆盖了多语言、多模态、多难度层级，并对结果进行了统计显著性检验。
- **清晰的“反面”结论**：明确指出了当前通用基础模型的局限性，为未来的研究方向（开发语音专用模型）提供了有价值的指导。

## 8. 不足与局限
- **计算资源未披露**：论文未提供实验的资源消耗情况，导致他人难以准确评估其训练成本或复现效率。
- **任务天花板效应**：在最具价值的辞汇解码任务上，所有模型的性能都接近随机水平，这使得模型间的差异难以被有效区分。当前结论主要基于一个相对简单的“语音模式”分类任务。
- **数据集局限**：仅在两个非侵入式 EEG 数据集上进行了测试，且 BCIC2020-T3 的想象词汇量很小（5 个）。对于更自然、大词汇量的语音解码场景，结论的普适性有待验证。
- **语言局限性**：仅涉及西班牙语和英语，模型在不同语言上的表现可能不同。
- **基础模型版本与微调**：仅微调了 LaBraM 和 EEGMamba 的完整模型，未探索更高效的微调方法或其他规模的版本。

（完）
