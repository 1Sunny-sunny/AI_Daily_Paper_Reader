---
title: What Does the Brain See? Multiview Neural Representations to Demystify the Brain-Visual Alignment
title_zh: 大脑看见了什么？多视角神经表征揭开脑-视觉对齐之谜
authors: "Salini Yadav, Taveena Lotey, Pravendra Singh, Partha Pratim Roy"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25718v1"
tags: ["query:sr"]
score: 9.0
evidence: 从EEG多视角表征进行零样本视觉解码
tldr: 针对零样本脑电解码视觉语义的挑战，本文提出统一多视图EEG表示学习框架，联合建模时域状态空间动态、小波频谱分解和注意力图结构，通过对比学习与视觉嵌入对齐，实现200路零样本分类。在THINGS-EEG基准上取得最优性能，并首次进行系统跨会话评估，证实显式捕捉多视图神经结构可提升语义对齐与泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25718v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed multiview EEG–Image decoding framework. Given a multichannel EEG trial, the EEG encoder constructs complementary temporal, spectral, and spatial representations using selective state-space modeling (S3M), adaptive learnable wavelet-inspired decomposition (ALWD), and an attention-modulated graph convolutional network (GCN). These representations are fused into a compact EEG embedding, which is aligned with pretrained visual representations for zero-shot semantic decoding.\", \"page\": 4, \"index\": 3, \"width\": 824, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2606.25718v1/fig-002.webp\", \"caption\": \"Figure 2: Zero-shot decoding performance across within-subject and cross-subject settings. The proposed method achieves higher median accuracy and lower performance variability compared to prior methods, indicating improved robustness and generalization.\", \"page\": 8, \"index\": 2, \"width\": 819, \"height\": 201}, {\"url\": \"assets/figures/arxiv/2606.25718v1/fig-001.webp\", \"caption\": \"Table 5: Ablation study evaluating the contribution of CPCA and RCR regularization terms under a within-subject setting.\", \"page\": 8, \"index\": 1, \"width\": 833, \"height\": 194}]"
motivation: 现有脑电-视觉对齐方法依赖整体嵌入，忽略了脑电信号中时、频、空互补结构的建模。
method: 提出多视图EEG编码器，融合状态空间时序建模、可学习小波频谱分解和电极图注意力，经对比学习与视觉语义对齐。
result: "在THINGS-EEG数据集上，被试内Top-1达54.8%，跨被试Top-1达15.3%，首次跨会话Top-1达40.8%，均达最优。"
conclusion: 显式建模多视图神经结构能有效提升基于脑电的视觉解码的语义对齐精度和跨情境泛化能力。
---

## 摘要
基于脑电图（EEG）的零样本视觉解码旨在从非侵入式神经记录中推断视觉语义，但由于EEG的低信噪比、非平稳性和有限的空间分辨率，仍具挑战性。现有的EEG-视觉对齐方法通常依赖于整体EEG嵌入，这可能掩盖视觉感知背后的互补时间、频谱和空间结构。我们引入了一个统一的多视角EEG表示学习框架，用于将大脑响应与视觉语义嵌入对齐。我们的方法构建了一个EEG编码器，联合建模三个互补视角：输入条件状态空间时间动态、可学习小波基频谱分解用于样本自适应频率建模，以及注意力调制图学习用于结构化电极交互。所得多视角EEG嵌入经融合后，在共享语义空间中利用对比学习和EEG特定正则化与预训练视觉表示对齐，实现了200路零样本视觉分类。在THINGS-EEG基准上的实验表明，我们的方法达到了最先进性能，在受试者内设置中取得了54.8%的Top-1准确率和85.6%的Top-5准确率，在跨受试者设置中取得了15.3%的Top-1准确率和45.4%的Top-5准确率。我们还首次提出了系统性的跨会话EEG-图像解码评估，实现了40.8%的Top-1和78.0%的Top-5准确率。这些结果表明，显式建模多视角神经结构提升了基于EEG视觉解码中的语义对齐和泛化能力。

## Abstract
Zero-shot visual decoding from electroencephalography (EEG) aims to infer visual semantics from non-invasive neural recordings, but remains challenging due to the low signal-to-noise ratio, non-stationarity, and limited spatial resolution of EEG. Existing EEG-vision alignment methods often rely on holistic EEG embeddings, which can obscure the complementary temporal, spectral, and spatial structure underlying visual perception. We introduce a unified multiview EEG representation learning framework for aligning brain responses with visual semantic embeddings. Our method builds an EEG encoder that jointly models three complementary views: input-conditioned state-space temporal dynamics, learnable wavelet-based spectral decomposition for sample-adaptive frequency modeling, and attention-modulated graph learning for structured electrode interactions. The resulting multiview EEG embeddings are fused and aligned with pretrained visual representations in a shared semantic space using contrastive learning with EEG-specific regularization, enabling 200-way zero-shot visual classification. Experiments on THINGS-EEG benchmark show that our method achieves state-of-the-art performance, with 54.8% Top-1 and 85.6% Top-5 accuracy in the within-subject setting and 15.3% Top-1 and 45.4% Top-5 accuracy in the cross-subject setting. We further present the first systematic cross-session EEG-image decoding evaluation, achieving 40.8% Top-1 and 78.0% Top-5 accuracy. These results suggest that explicitly modeling multiview neural structure improves both semantic alignment and generalization in EEG-based visual decoding.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **研究动机**：基于脑电图（EEG）的零样本视觉解码旨在从非侵入式神经信号中推断视觉语义，但EEG信号固有的低信噪比、非平稳性以及低空间分辨率使该任务极具挑战。
- **现存问题**：主流EEG‑视觉对齐方法通常使用整体化的EEG嵌入，忽视了EEG在时间、频谱和空间维度上的互补结构，导致语义对齐不稳定，尤其在跨被试和跨记录会话场景下泛化能力弱。
- **目标与含义**：本文旨在构建一个统一的多视角EEG表示学习框架，通过显式建模时频空三个视图的神经响应模式，学习更稳定、更具语义区分度的EEG嵌入，以提升与预训练视觉语义空间的对齐质量，从而实现更可靠的零样本视觉识别。

## 2. 方法论
### 2.1 核心思想
提出一种**多视角EEG‑图像对齐框架**，冻结图像编码器（CLIP‑ViT‑H‑14），重点设计三个并行、互补的EEG编码分支，分别捕捉时间动态、自适应频谱结构和电极间空间交互，再将融合后的EEG嵌入与视觉嵌入通过对比学习对齐至共享语义空间。

### 2.2 关键技术细节
- **时间建模（Temporal）**：采用**选择性状态空间模型（S 3M）**，其隐状态更新为
  $$h_t = \bar{A}_t \odot h_{t-1} + \bar{B}_t x_t,\quad \bar{A}_t = \exp(\Delta_t A)$$
  输出 $y_t = \sum_{k=1}^{d_s} C_{t,k} h_{t,k}$。后续经一维卷积和投影获得时间嵌入 $z_{\text{temporal}}$。
- **频谱建模（Spectral）**：设计**自适应可学习小波分解（ALWD）**。先用投影网络 $M$ 生成样本特定的滤波器选择权重，加权合成低通/高通滤波器和，通过跨步卷积得到低频分量 $X_{\text{low}}$ 和高频分量 $X_{\text{high}}$。两分量分别送入独立的S3M，再经由交叉频率注意力进行跨尺度交互，最后拼接、时序上采样并投影，得到频谱嵌入 $z_{\text{spectral}}$。
- **空间建模（Spatial）**：使用**注意力调制图卷积网络（GCN）**，将电极视为节点，动态邻接矩阵 $\mathbf{A} = \mathbf{A}_{\text{global}} \odot \mathbf{A}_{\text{att}}(\mathbf{X})$，结合可学习的全局结构和输入依赖的边注意力，计算
  $$z_{\text{spatial}} = \sigma(\mathbf{A} \mathbf{X} \mathbf{W}_{\text{GCN}}).$$
- **多视角融合与对齐**：拼接三组特征经投影头得到最终的EEG嵌入 $z_{\text{eeg}}$，图像嵌入 $z_{\text{img}}$ 由冻结的CLIP‑ViT‑H‑14提取。通过对称对比损失 $\mathcal{L}_{\text{align}}$ 进行跨模态对齐，并额外引入**重复一致性正则项（RCR）**和**类原型对比正则项（CPCA）**以增强EEG表示的稳定性和类级语义结构，最终损失为
  $$\mathcal{L} = \mathcal{L}_{\text{align}} + \lambda_{\text{RCR}}\mathcal{L}_{\text{RCR}} + \lambda_{\text{CPCA}}\mathcal{L}_{\text{CPCA}}.$$

## 3. 实验设计
- **数据集**：THINGS‑EEG，包含10名被试、4个记录会话，训练类别1,654个（每类10幅图像，各重复4次），测试类别200个（每类80次），训练与测试类别无重叠。
- **评估场景**：
  - **被试内（within-subject）**：每名被试独立训练与测试。
  - **跨被试（cross-subject）**：留一被试法，评估对未见被试的泛化。
  - **跨会话（cross-session）**：对每名被试，用三个会话训练，第四个会话测试，首次系统评估会话间分布偏移下的解码鲁棒性。
- **任务指标**：200路零样本分类，报告Top‑1和Top‑5准确率。
- **对比方法**：BraVL、NICE、NICE‑SA、NICE‑GA、ATM‑S、UMind、CognitionCapturer (CC)、UBP 等近期方法。

## 4. 资源与算力
- **硬件**：单块NVIDIA RTX 4500 Ada GPU（24 GB显存）。
- **训练配置**：50个epoch，批量大小64，AdamW优化器（学习率 $1\times10^{-4}$，权重衰减 $1\times10^{-4}$），固定随机种子以确保可比性。训练、验证、测试均使用同一批量大小。图像嵌入一次性预提取并复用，图像编码器冻结，有效降低训练开销。

## 5. 实验数量与充分性
- **主要实验组数**：
  - 三种评估设置（被试内、跨被试、跨会话）各完成一组完整对比实验，给出10名被试的详细结果及平均值。
  - 消融实验：单独移除时间、频谱、空间模块，分析各分支贡献；移除CPCA和RCR正则项（单独及同时），验证正则化效果。
  - 表征分析：计算视图间余弦相似度和CKA，可视化学习到的图连接矩阵和地形图，提供了被试级别和组平均的多角度分析。
- **充分性与公平性**：实验覆盖多个零样本设定和多个基线，消融充分，随机种子固定，评估指标统一。跨会话仅与UBP比较，但作为首次系统评估仍具参考价值。

## 6. 主要结论与发现
- 提出的多视角EEG表示学习框架在所有设置下均取得**最优性能**：被试内Top‑1 54.8%（+3.9%）、Top‑5 85.6%（+5.9%）；跨被试Top‑1 15.3%（+2.9%）、Top‑5 45.4%（+12.0%）；跨会话Top‑1 40.8%、Top‑5 78.0%。
- 显式建模时间、频谱、空间结构能够捕捉**互补信息**，三视图嵌入间相似度极低，表明有效解耦。
- 学习到的空间连接模式聚焦于**后部枕叶及顶枕区域**，符合视觉处理的神经基础，而非随机分布。
- 表征稳定性和语义对齐的增益在**跨场景泛化**（尤其是跨会话）中尤为突出，证明了结构化EEG编码器的重要性。

## 7. 优点
- **方法新颖性**：首次将受输入调节的状态空间模型、自适应小波分解和图注意力联合用于EEG‑视觉对齐，统一建模时频空多视角。
- **系统评估**：首次进行跨会话零样本EEG‑图像解码评估，补全了从被试内到真实部署场景的评价链路。
- **可解释性强**：通过视图间相似度、学习到的脑连接热图等分析，清晰展示了模型的效果来源。
- **性能突出**：在THINGS‑EEG基准上达到新SOTA，且多个评估指标提升幅度显著。
- **结构简洁**：图像编码器冻结，仅优化EEG编码器和投影层，训练效率高，易复现。

## 8. 不足与局限
- **数据集单一**：仅在THINGS‑EEG上验证，未在其他EEG‑视觉数据集测试泛化性，模型对电极布局、实验范式的迁移能力未知。
- **跨被试性能瓶颈**：跨被试Top‑1仅15.3%，虽优于基线但绝对性能仍偏低，离实际应用差距较大。
- **消融对比有限**：跨会话消融和与其他方法的对比不足（仅与UBP比较），正则项的具体公式和敏感度分析未在正文展开，缺少超参数影响的分析。
- **依赖预训练视觉模型**：使用CLIP‑ViT‑H‑14，可能引入视觉‑语言模型的偏差，且限制了纯粹开放词汇（无类别原型）解码的彻底探索。
- **实际噪声处理未讨论**：未明确提及对眼电、肌电等伪迹的处理，跨会话的鲁棒性可能在实际噪声更大的场景中打折扣。
- **计算及效率对比缺失**：虽使用RTX 4500 Ada，但未报告与其他方法的训练耗时、推理速度对比，难以全面评估工程实用性。

（完）
