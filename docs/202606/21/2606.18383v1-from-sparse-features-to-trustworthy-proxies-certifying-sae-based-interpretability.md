---
title: "From Sparse Features to Trustworthy Proxies: Certifying SAE-Based Interpretability"
title_zh: 从稀疏特征到可信代理：验证基于SAE的可解释性
authors: "Dibyanayan Bandyopadhyay, Asif Ekbal"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18383v1"
tags: ["query:sr"]
score: 8.0
evidence: 通过泛化框架验证基于SAE的可解释性
tldr: 研究针对稀疏自编码器（SAE）从语言模型提取特征的可解释性忠实问题，提出事后认证框架，用SAE重构激活生成稀疏代理，推导基于代理风险、重构误差、概念不匹配与稀疏复杂度的期望风险上界，作为忠实性准则。在GPT-2 Small、Gemma-2B和Llama-3-8B上验证界限非空，发现深层易认证且保真度高，特征洗牌实验可区分语义对齐与纯稀疏性，为SAE解释可靠性提供诊断工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18383v1/fig-001.webp\", \"caption\": \"Figure 2. Left: Llama-3-8B observed bounds across layers. The horizontal dotted line marks the 16.98-bit baseline. Layers 20, 24, 28, and 30 eventually become non-vacuous with increasing samples, whereas layers 4, 8, 12, and 16 do not, because their asymptotic floors remain above the baseline. Right: Output fidelity on Llama-3-8B as N grows. The key pattern is that later layers preserve the base model’s output behavior much more faithfully (also remains stable with increasing N ), which explains the depth-wise drop in proxy risk and reconstruction gap.\", \"page\": 6, \"index\": 1, \"width\": 904, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-008.webp\", \"caption\": \"Figure 4. Ablation: Semantic Specificity. Density of the per-sequence reconstruction gap. Green (Real SAE): The baseline error is tightly clustered near 0 bits, indicating high semantic fidelity. Red (Shuffled Features): Randomly permuting the active feature indices—while strictly preserving the per-sample sparsity k and activation magnitudes—drastically shifts the error distribution to the right (mean shifts of ≈ 6.5, 8.5, and 9.5 bits for GPT-2 Small, Gemma-2B, and Llama-3-8B, respectively).\", \"page\": 8, \"index\": 8, \"width\": 1003, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-007.webp\", \"caption\": \"Table 3. Global experimental settings shared across all model families.\", \"page\": 13, \"index\": 7, \"width\": 442, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-006.webp\", \"caption\": \"Figure 5. Layerwise analysis for GPT-2 small\", \"page\": 13, \"index\": 6, \"width\": 481, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-004.webp\", \"caption\": \"Table 5. Zero-shot downstream-task preservation under sparse semantic proxying. We report the accuracy of the base model M , the pool-restricted proxy hG, and the drop ∆Acc = Acc(M) − Acc(hG) on WinoGrande, PIQA, and HellaSwag. Later Llama layers preserve downstream behavior substantially better than earlier layers.\", \"page\": 14, \"index\": 4, \"width\": 858, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-005.webp\", \"caption\": \"Table 6. Layer-wise output fidelity on Llama-3-8B. Fidelity improves substantially with depth.\", \"page\": 14, \"index\": 5, \"width\": 917, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-002.webp\", \"caption\": \"Figure 6. Sensitivity of the sparse semantic generalization certificate to the Top-k sparsity level at fixed representative layers: GPT-2 Small (layer 6), Gemma-2B (layer 12), and Llama-3-8B (layer 30). Across k ∈ {16, 32, 64, 128}, the qualitative ordering remains unchanged, while the sample size required for non-vacuity decreases as k increases, most notably for Llama-3-8B.\", \"page\": 15, \"index\": 2, \"width\": 1017, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-003.webp\", \"caption\": \"Figure 7. Decomposition of the certificate under synthetic corruption. We visualize the contributions of empirical risk, reconstruction gap, pool mismatch, and complexity to the total bound (Top-k = 64). On uncorrupted text, the bound lies below the uninformed baseline (dashed line), whereas increasing random token corruption progressively weakens the certificate (15% corruption and 100% corruption). The degradation is driven primarily by empirical risk rather than by the complexity term.\", \"page\": 15, \"index\": 3, \"width\": 1017, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.18383v1/fig-009.webp\", \"caption\": \"Figure 8. Qualitative comparison of early and late SAE layers on two deduction prompts. We show (i) the prompt, (ii) the base–proxy agreement summary through KL, and (iii) the top active SAE concepts verbalized using feature-level logit-lens tokens. Lower KL and sharper token verbalizations at later layers indicate a more faithful sparse proxy. Note, all gold/base/proxy next-token strings are tokenizer subword pieces rather than necessarily whole words.\", \"page\": 16, \"index\": 9, \"width\": 1017, \"height\": 746}]"
motivation: 确定SAE提取的稀疏特征何时能忠实反映底层语言模型的行为。
method: 通过替换隐藏激活为SAE重构构建稀疏代理，推导期望风险上界作为认证准则。
result: 在多种模型上界限非空，深层认证更容易且局部保真度高，消融实验能区分真实语义对齐与表面稀疏。
conclusion: 该框架提供了可操作的SAE解释忠实性准则，有助于诊断解释可靠性。
---

## 摘要
稀疏自编码器（SAEs）越来越多地用于从语言模型（LMs）中提取可解释的特征，但一个核心问题依然存在：何时可以将基于SAE的解释视为底层冻结语言模型的忠实视图？我们通过一个事后泛化框架来研究这个问题，该框架通过稀疏代理来验证语言模型，该代理通过将原生隐藏激活替换为其预训练的SAE重构而得到。我们的框架利用四个可测量的量推导出基础模型期望风险的上界：代理风险、SAE重构差距、概念池不匹配和稀疏复杂度。我们将这一认证解释为解释忠实性的操作准则。特别地，非平凡上界表明提取的稀疏特征保留了有意义的预测信息，而小的重构和概念不匹配误差表明代理在行为上仍与原始模型接近。在实证中，我们展示了在GPT-2 Small、Gemma-2B和Llama-3-8B上，当使用实际的样本量时，该上界变得非平凡。对Llama-3-8B的逐层详细分析揭示了强烈的深度依赖性，深层更容易被认证，这与更强的局部保真度和较弱的下游误差放大有关。最后，通过特征混洗消融实验，我们表明这种分解能够区分真正的语义对齐与单纯的统计稀疏性，为基于SAE的解释何时变得不再可靠提供了一个有用的诊断工具。

## Abstract
Sparse autoencoders (SAEs) are increasingly used to extract interpretable features from language models (LMs), yet a central question remains: when can an SAE-based explanation be treated as a faithful view of an underlying frozen LM We study this through a post-hoc generalization framework that certifies the LM via a sparse proxy, obtained by replacing a native hidden activation with its pretrained SAE reconstruction. Our framework derives an upper bound on the base model's expected risk using four measurable quantities: proxy risk, SAE reconstruction gap, concept-pool mismatch, and sparse complexity. We interpret this certificate as an operational criterion for explanatory faithfulness. In particular, a non-vacuous bound indicates that the extracted sparse features retain meaningful predictive information, while small reconstruction and mismatch errors indicate that the proxy remains behaviorally close to the original model. Empirically, we show that the bound becomes non-vacuous on GPT-2 Small, Gemma-2B, and Llama-3-8B at practical sample sizes. A detailed layerwise analysis of Llama-3-8B reveals a strong depth dependence, with later layers becoming much easier to certify, associated with both stronger local fidelity and weaker downstream error amplification. Finally, through feature-shuffling ablations, we show that the decomposition distinguishes genuine semantic alignment from mere statistical sparsity, providing a useful diagnostic for when SAE-based explanations become less reliable.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **核心问题**：稀疏自编码器（SAEs）常用于从语言模型（LMs）中提取可解释特征，但其解释是否忠实反映底层冻结模型的行为，缺乏严格验证标准。
- **研究动机**：现有 SAE 工作多侧重定性分析，忽略了对解释可信度的量化评估。论文旨在提供一种操作准则，回答“何时可信任 SAE 解释”这一根本问题。
- **整体含义**：通过一个事后泛化框架，将 SAE 诱导的稀疏代理作为认证工具，追求解释的“有用性”（模型非平凡）和“忠实性”（代理行为接近原始模型），从而诊断 SAE 解释的可靠性。

## 2. 论文提出的方法论
- **核心思想**：利用预训练 SAE 重构冻结 LM 的隐藏激活，构建稀疏代理模型，并推导原始模型期望风险的泛化上界。该上界四分解为可测量项，作为忠实性认证准则。
- **关键技术细节**：
  - **SAE 组件**：编码器 $S_E$ 投影到 $m$ 维特征，通过 Top-K 稀疏化得到稀疏编码 $c(x)$，再由解码器 $S_D$ 重构激活 $\hat{h}(x)$。
  - **代理构建**：将重构激活 $\hat{h}(x)$ 输入下游层，形成池限制代理 $h_{G^*}$，其中概念池 $G^*$ 由校准数据的 Top-K 支持并集确定。
  - **风险上界**：
    $$R(M) \leq \hat{R}_S(h_{G^*}) + \hat{\epsilon}_{\text{loss}} + B \left(\hat{\eta} + \sqrt{\frac{\log(2/\delta)}{2N}}\right) + B \sqrt{\frac{P \log(em/P) + \log(2/\delta)}{2N}} + B \sqrt{\frac{\log(4/\delta)}{2N}}$$
    - $\hat{R}_S(h_{G^*})$：池限制代理的经验风险。
    - $\hat{\epsilon}_{\text{loss}}$：原始模型与无限制 SAE 代理间的损失差。
    - $\eta$：池不匹配概率，即评估时 Top-K 支持不在池内的比例。
    - $P = |G^*|$：概念池大小，控制稀疏复杂度。
    - $B = \log_2(V/\alpha)$：平滑损失上界，其中 $\alpha$ 是预测平滑参数。
  - **操作流程**：固定基础模型和 SAE，用校准流构建 $G^*$，然后在独立评估流上计算上界。界值非平凡且重构与不匹配项小时，认为代理可信。

## 3. 实验设计
- **数据集**：英文 C4 语料，切分为连续 32 长度令牌序列。
- **模型**：GPT-2 Small、Gemma-2B、Llama-3-8B，在特定层（如 GPT-2 层 6、Gemma-2B 层 12、Llama-3-8B 层 30）嵌入预训练 SAE。
- **基准**：随机猜测基线 $\log_2(V/\alpha)$（如 GPT-2 为 15.62 bits），非平凡界限定低于此值。
- **场景与对比**：
  - **主认证曲线**：评估不同样本量 $N$ 下的界限。
  - **层敏感度**：对比 Llama-3-8B 和 GPT-2 Small 的多个层。
  - **输出保真度**：测量原始模型与代理间的 KL 散度、Top-1 一致率等。
  - **消融实验**：特征混洗（随机排列活跃特征索引）、Top-k 参数变化（$k=16,32,64,128$）、合成输入破坏。
  - **下游任务保留**：在 WinoGrande、PIQA、HellaSwag 上评估代理准确度退化。
  - **定性分析**：对推理提示进行代理下一令牌预测和 SAE 特征可视化。

## 4. 资源与算力
- 论文未明确提及 GPU 型号、数量或训练时长。SAE 和基础模型均为预训练且冻结，认证过程仅涉及推理，故算力需求可能适中，但具体配置未说明。

## 5. 实验数量与充分性
- **实验组数**：约 7–8 组主要实验，包括认证曲线（3 模型）、层分析（2 模型多层级）、Top-k 敏感度（4 种设置）、消融（特征混洗、破坏、下游任务等）。
- **充分性**：实验覆盖多个模型规模和层位置，结合消融和下游验证，多角度检验认证框架的有效性与敏感性。设计客观，未发现明显偏见，但主要集中在预训练 SAE，未探索不同 SAE 架构的影响。

## 6. 论文的主要结论与发现
- 在 GPT-2 Small、Gemma-2B 和 Llama-3-8B 上，SAE 诱导的认证界在实用样本量下变得非平凡，成功认证模型非平凡性。
- Llama-3-8B 的逐层分析表明，深层代理更容易认证，这源于更强的局部 SAE 保真度和较弱的下游误差放大效应。
- 特征混洗消融证实认证对语义对齐敏感，可区分真实可解释性与表面统计稀疏性，为 SAE 解释可靠性提供诊断。
- 框架通过四分解量明确了信任失效的来源（如代理风险、重构误差），而非仅重建质量。

## 7. 优点
- **原则性框架**：将可解释忠实性转化为可量化的泛化认证问题，提出操作化准则。
- **可分解性**：上界拆分为风险、重构、不匹配和复杂度四项，便于定位解释失效环节。
- **实证全面**：实验涵盖多模型、层深、消融和下游任务，结论稳健。
- **诊断价值**：层分析与特征混洗展示了框架在实际中筛选可信解释的用处。

## 8. 不足与局限
- **实验覆盖有限**：仅使用 C4 英文数据和三种预训练 SAE，泛化到其他语言、任务或模型架构（如更大模型）未知。
- **依赖 SAE 质量**：认证有效性高度依赖 SAE 的预训练表现，未讨论 SAE 训练方法对结果的影响。
- **分布假设**：认证基于 i.i.d. 采样和特定分布，在分布偏移下（如破坏数据）认证可能失效。
- **计算成本**：虽未详述，但构建概念池和评估多个样本量需较多推理资源，尤其对大型模型。
- **误解风险**：框架仅从风险角度定义忠实性，不涉及因果或语义完备性，可能被过度解释为全面解释质量的证明。

（完）
