---
title: "Otters++: A Time-to-first-spike Based Energy Efficient Optical Spiking Transformer"
title_zh: "Otters++: 一种基于首个脉冲时间的高能效光学脉冲Transformer"
authors: "Zhanglu Yan, Jiayi Mao, Kaiwen Tang, Fanfan Li, Gang Pan, Tao Luo, Bowen Zhu, Qianhui Liu, Weng-Fai Wong"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13016v1"
tags: ["query:sr"]
score: 9.0
evidence: 时间到首次尖峰编码用于节能脉冲Transformer
tldr: "针对脉冲神经网络中TTFS编码因数字计算时间衰减项导致效率下降的问题，提出Otters++，利用光电突触的自然信号衰减直接实现TTFS时间项，免除数字计算。通过建立与量化神经网络的层功能等价性，采用器件真实前向与QNN直通梯度反向的混合训练及蒸馏，并感知器件噪声，将方法扩展至Transformer模型。在GLUE数据集上达到84.17%平均分，保持能量优势，验证物理TTFS计算的实用潜能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13016v1/fig-004.webp\", \"caption\": \"Fig. 1. Device and workflow: (a) the custom-fabricated In2O3 thin-film transistor (TFT); (b) measured decay curve of the device response; (c) Otters++ neuron workflow.\", \"page\": 4, \"index\": 4, \"width\": 959, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-005.webp\", \"caption\": \"Fig. 2. Architecture design for scores calculations.\", \"page\": 4, \"index\": 5, \"width\": 433, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-008.webp\", \"caption\": \"Fig. 3. Otters++-based transformer structure.\", \"page\": 5, \"index\": 8, \"width\": 464, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-001.webp\", \"caption\": \"Fig. 4. Noise decay function under measurement.\", \"page\": 7, \"index\": 1, \"width\": 520, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-003.webp\", \"caption\": \"Fig. 5. Details design of Otters++.\", \"page\": 8, \"index\": 3, \"width\": 520, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-007.webp\", \"caption\": \"Fig. 6. Per-layer energy comparison under the unified hardware cost model. Otters++ achieves the lowest per-layer energy among all compared methods.\", \"page\": 9, \"index\": 7, \"width\": 507, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-006.webp\", \"caption\": \"TABLE I PERFORMANCE COMPARISON ON THE GLUE BENCHMARK. ALL SCORES ARE ACCURACY, EXCEPT FOR STS-B (PEARSON CORRELATION). “*” INDICATES THAT THE MODEL SIZE WAS NOT REPORTED IN THE ORIGINAL PAPER. BOLD INDICATES THE BEST PERFORMANCE AMONG SNN MODELS. ONLY OTTERS++ QUANTIZES KV TO 1 BIT. “NOMINAL” DENOTES EVALUATION USING THE MEAN DEVICE RESPONSE, WHILE “VARIATION” DENOTES EVALUATION UNDER MEASURED RUN-TO-RUN DEVICE VARIATION SAMPLED FROM THE EXPERIMENTAL RESPONSE ENVELOPE.\", \"page\": 9, \"index\": 6, \"width\": 968, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2606.13016v1/fig-002.webp\", \"caption\": \"Fig. 8. Total energy versus number of communication hops. The shaded region denotes the Otters++ energy range under minimum and maximum analog-read assumptions; the inset zooms in on the low-hop regime.\", \"page\": 10, \"index\": 2, \"width\": 495, \"height\": 344}]"
motivation: 解决TTFS编码中显式计算时间衰减与权重乘法的能耗开销，将硬件衰减“缺陷”转化为计算能力。
method: 用定制In₂O₃光电突触衰减物理实现TTFS，建立SNN与QNN等价性，混合训练结合蒸馏并噪声感知。
result: "在GLUE上平均得分84.17%，优于先前脉冲Transformer且能耗更低。"
conclusion: 物理驱动的TTFS计算在真实硬件效应下高效、可训练且鲁棒，适用于大规模Transformer。
---

## 摘要
脉冲神经网络（SNNs）在能效推理方面前景广阔，而首个脉冲时间（TTFS）编码因其每个神经元最多发放一次而尤为引人注目。然而在实践中，这种优势常因计算时间衰减项并将其乘以突触权重的开销而削弱。我们通过将物理硬件中的一个“缺陷”——光电器件中的自然信号衰减——转化为 TTFS 的主要计算，即名为 Otters++ 的方法来解决该问题。具体而言，我们利用定制的 In₂O₃ 光电突触的实测衰减来直接实现 TTFS 时间项，从而无需显式的数字衰减计算。为了将此思想扩展到 Transformer 模型，我们在 Otters++ 与量化神经网络（QNN）之间建立了逐层功能等价性，并开发了一种混合训练方法，该方法在前向传播中使用忠实于器件的 SNN 计算，在反向传播中通过等效的 QNN 路径使用 QNN 直通梯度，并结合模型蒸馏。这避免了通过离散首个脉冲事件进行微分，并减少了直接 TTFS-SNN 训练中的过度稀疏性问题。我们进一步通过采样运行间变异，使训练感知到实测的器件噪声，并通过考虑器件共享和多跳通信完善系统级能耗模型。在 GLUE 数据集上，Otters++ 将平均得分提升至 84.17%，同时相对于先前的脉冲 Transformer 基线保持了明显的能耗优势。这些结果表明，基于物理现实的 TTFS 计算在真实硬件效应下能够高效、可训练且鲁棒。

## Abstract
Spiking neural networks (SNNs) are promising for energy-efficient inference, and time-to-first-spike (TTFS) coding is especially attractive because each neuron fires at most once. In practice, however, this benefit is often reduced by the cost of computing a temporal decay term and multiplying it by the synaptic weight. We address this issue by turning a physical hardware "bug," the natural signal decay in optoelectronic devices, into the main computation of TTFS, named Otters++. Specifically, we use the measured decay of a custom In$_2$O$_3$ optoelectronic synapse to directly realize the TTFS temporal term, removing the need for explicit digital decay computation. To scale this idea to Transformer models, we establish a layer-wise functional equivalence between the Otters++ and a quantized neural network (QNN), and develop a hybrid training method that uses device-faithful SNN computation in the forward pass and QNN straight-through gradients through the equivalent QNN path in the backward pass, together with model distillation. This avoids differentiation through discrete first-spike events and reduces the over-sparsity problem in direct TTFS-SNN training. We further make training aware of measured device noise by sampling run-to-run variation, and refine the system-level energy model by accounting for device sharing and multi-hop communication. On GLUE dataset, Otters++ improves the average score to 84.17\% while maintaining a clear energy advantage over prior spiking Transformer baselines. These results show that physically grounded TTFS computing can be efficient, trainable, and robust under realistic hardware effects.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：脉冲神经网络（SNN）的首个脉冲时间（TTFS）编码理论上极具能效（每个神经元最多发放一次），但实际部署中，仍需在数字域显式计算时间衰减项并与突触权重相乘，这一额外开销严重侵蚀了 TTFS 的稀疏优势。
- **整体含义**：论文提出 **Otters++**，其核心思想是 **将物理硬件中光电器件的自然信号衰减“缺陷”重新定义为一种计算资源**，直接用器件物理实现 TTFS 所需的时间调制，从而彻底避免数字的衰减函数求值与乘法。在此基础上，将这一思想扩展至 Transformer 模型，并建立一套可训练、抗噪声且系统级能效可评估的完整框架。

### 2. 方法论：核心思想与关键技术细节
- **物理 TTFS 神经元（Otters++ 神经元）**
  - 使用定制的 **In₂O₃（氧化铟）薄膜晶体管（TFT）** 作为光电突触，其衰减响应曲线为
    $$O(t) = I_0 \exp\!\left(-\big(\frac{t}{\tau}\big)^{\beta}\right) + I_{\text{offset}}$$
    该衰减直接作为突触后时间项。
  - 突触后电位为 $\epsilon'(t) = \gamma_l^{ij} O(t)$，膜电位按脉冲时刻累加，并与随离散时间步递减的动态阈值比较，在每个神经元最多发放一次的约束下产生首个脉冲。

- **与 QNN 的逐层功能等价**
  - 设 $n$ 比特无符号 QNN，量化范围 $T = 2^n - 1$，步长 $\alpha^l$。
  - 通过以下构造实现 **一层 SNN 与一层 QNN 的严格等价**：
    1. 时间窗口用 $T$ 个离散等级。
    2. 逻辑级 $k$ 映射到物理时间 $t_k$，使得 $O(t_k) = (T - k)/T$。
    3. 突触缩放因子 $\gamma_l^{ij} = w_l^{ij} \alpha^{l-1} T$。
    4. 动态阈值 $\theta^l(t) = \alpha^l (T - k),\; t_k \le t < t_{k+1}$。
  - 在该构造下，SNN 的膜电位等于 QNN 的预激活，输出脉冲时间编码的整数等于 QNN 经取整与截断后的量化输出（命题 1 及证明）。

- **混合 SNN‑前向 / QNN‑反向训练**
  - **前向**：完全遵循 Otters++ 的硬件执行路径——将激活转为脉冲时间，采样器件响应（可加入噪声），计算膜电位、阈值比较、脉冲输出。
  - **反向**：不通过离散脉冲事件微分，而是经由与 SNN 等价的 **QNN 路径**使用 **直通估计器（STE）** 计算梯度，更新权重 $W_l$、偏置 $\beta_l$ 和步长 $\alpha_l$。
  - 损失函数采用知识蒸馏：输出 logits 的 KL 散度 + 各层隐藏状态的 MSE：
    $$\mathcal{L} = \text{KL}(\sigma(\hat{y}^S/\tau) \parallel \sigma(\hat{y}^T/\tau)) + \sum_{l=0}^{L} \|h_l^S - h_l^T\|_2^2$$
  - 训练感知 **实测器件运行间变异**：在离散时间 $\hat{t}_{ij}$ 处，于上下界 $[O_{\text{low}}, O_{\text{high}}]$ 内加入高斯噪声采样响应：
    $$o_{ij} = \text{Clamp}_{[\ell_{ij}, h_{ij}]}\!\big(O_{\text{mean}}(\hat{t}_{ij}) + \epsilon_{ij}\big),\quad \epsilon_{ij} \sim \mathcal{N}(0,\sigma^2)$$

- **系统级能耗建模**
  - 能耗分解为投影层 $E_{\text{fc}}$ 与注意力分数 $E_{\text{score}}$，公式中显式包含 **阈值比较、脉冲处理（累加、模拟读取、稀疏数据移动）、泄漏、二进制 K/V 读写** 等项，并考虑 **器件共享** 与 **多跳通信**（Network‑on‑Chip）下的模拟读取成本。

### 3. 实验设计
- **数据集与基准**：GLUE 基准的 7 个任务：QQP、MNLI‑m、SST‑2、QNLI、RTE、MRPC、STS‑B（Pearson 相关系数）。
- **对比方法**：
  - **传统模型/量化模型**：BERT base、DistilBERT、TinyBERT、Q2BERT、BiT。
  - **脉冲 Transformer**：SpikingFormer、SpikingBERT、SpikeLM（含 1‑bit 版）、1‑bit Sorbet、Otters。
  - 所有 SNN 基线使用 4 比特时间窗口（$T=15$）。
- **评估维度**：任务准确率（均值±标准差，考虑硬件变异时 20 次独立试验）、每层总能耗（mJ）、能耗成分分解、通信跳数对总能耗的影响。

### 4. 资源与算力
- **硬件**：实验使用 **3 块 NVIDIA A100 GPU**。
- **训练时长**：论文未给出具体训练时钟时间，但描述了采用 BERT base 作为教师、固定 4‑bit 窗口（$T=15$）的设置；模型大小为 13.4M 参数，属于轻量学生模型。

### 5. 实验数量与充分性
- **任务级实验**：在 7 个数据集上评测了 **名义器件** 和 **实测变异** 两种条件下的 Otters++，并与其他十多种模型对比，表 I 给出完整数字。
- **能耗实验**：分别绘制了各模型每层总能耗对比、能耗成分百分比堆叠图、以及随通信跳数（0–10 跳）变化的总能耗曲线，覆盖不同模拟读取假设（最小/最大）和器件共享程度。
- **消融研究**：对比了“直接 QNN‑to‑SNN 转换（Otters）”与“集成 TTFS 前向训练（Otters++）”的准确率差距（表 II），清晰说明混合训练的价值。
- **与数字 TTFS 对比**：在统一设定下，计算了量化时间数字 TTFS 注意力块能耗为 19.09 mJ，高于 Otters++ 的 14.2 mJ，量化了数字解码开销的消除收益。
- 实验覆盖 **性能、能效、硬件变异鲁棒性、通信开销敏感性、训练策略影响** 等多个维度，对比方法多样且公平，充分支撑结论。

### 6. 主要结论与发现
- Otters++ 在 GLUE 上 **平均得分 84.17%**，比原先 Otters 提高 0.95 个百分点，比 SpikingBERT、SpikeLM、1‑bit Sorbet 等脉冲 Transformer 基线高出 3.34–4.37 个百分点。
- 在实测运行间变异下，平均得分保持 84.17%（标准差±0.28），**鲁棒性得到验证**。
- 系统级能耗分析表明，Otters++ 每层能耗 **仅 14.2 mJ**，为 SpikingLM 的 1/1.84，Sorbet 的 1/3.02，SpikingBERT 的 1/5.68，且能耗优势在通信跳数增加时仍然保持。
- 物理 TTFS 计算通过器件衰减直接替代数字时间解码，有效降低了算术、内存访问和数据移动开销。

### 7. 优点
- **硬件‑算法协同创新**：首次将光电突触的自然衰减直接用作 TTFS 的时间项，将硬件非理想性转化为计算优势。
- **可训练性突破**：建立了 SNN‑QNN 严格等价性，并设计了忠实硬件的混合训练框架，解决了不可微、过度稀疏和硬件失配问题。
- **硬件变异感知训练**：通过实测响应包络注入噪声，使模型在训练阶段即可适应真实器件波动，不牺牲精度。
- **详尽的能耗评估**：构建了覆盖计算、存储、通信、模拟读取及器件共享的精细系统级能耗模型，贴近实际部署。
- **实验全面扎实**：在多个 NLP 基准上对比数十种方法，并结合消融、通信跳数等分析，证据链完整。

### 8. 不足与局限
- **器件规模与延迟**：当前原型 TFT 面积较大（0.012 mm²，沟道 30 μm），单层光学衰减窗口约 100 μs，每块 Transformer 约需 0.8 ms，虽理论上可通过更快的读出和更高光强压缩，但实际集成仍有距离。
- **仅验证 4‑bit 时间窗**：实验固定为 $T=15$（4 比特），未探索更高精度下器件衰减的保真度与等价性保持情况。
- **未与线性注意力结合**：论文讨论中指出，线性复杂度注意力与 Otters++ 可互补，但当前仍采用 $O(N^2)$ 的自注意力，未来可进一步降能。
- **模拟电路能耗基于文献假设**：ADC、TFT 等能耗参考已有研究，但缺少整体流片实测验证。
- **训练依赖教师模型**：需要预训练 FP32 BERT 作为教师进行知识蒸馏，训练管线稍复杂。
- **应用范围局限**：仅在 GLUE 自然语言理解任务上评估，是否适用于更大规模的 LLM 或其他模态尚需检验。

（完）
