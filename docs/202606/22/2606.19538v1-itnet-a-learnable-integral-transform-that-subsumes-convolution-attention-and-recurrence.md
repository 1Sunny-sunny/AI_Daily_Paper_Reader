---
title: "ITNet: A Learnable Integral Transform That Subsumes Convolution, Attention, and Recurrence"
title_zh: ITNet：一种可学习的积分变换，涵盖卷积、注意力和循环
authors: "Ashim Dhor, Rasel Mondal, Pin Yu Chen"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19538v1"
tags: ["query:sr"]
score: 8.0
evidence: 统一的序列建模可学习积分变换
tldr: 现有卷积、注意力和循环网络各自为政，研究指出它们本质是同一可学习积分变换的特殊情况。本文提出积分变换网络(ITNet)，用一个小型MLP实现可学习核，统一建模位置与特征依赖的成对交互。通过参数化可涵盖卷积、自注意力、LSTM等，并证明是连续算子通用逼近器。开发分块核融合、重要性加权蒙特卡洛积分和低秩分解实现高效计算。单一ITNet在多模态基准上匹配或超越专用架构，表明从数据即可恢复三大架构族行为。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19538v1/fig-003.webp\", \"caption\": \"Table 1: ImageNet-1K top-1 accuracy. ITNet results are mean ± std over 3 random seeds.\", \"page\": 7, \"index\": 3, \"width\": 598, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-006.webp\", \"caption\": \"Table 2: GLUE development-set scores. ITNet results are mean± standard deviation over 3 random seeds. Models with comparable pre-training data (BookCorpus + Wikipedia, ∼16GB) are shown.\", \"page\": 8, \"index\": 6, \"width\": 829, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-010.webp\", \"caption\": \"Table 5: Kernel input ablation across all modalities (ITNet-B). ✓= included, ✗= excluded. Results: ImageNet-1K top-1 (%), ModelNet40 OA (%), GLUE avg, VQA v2 test-dev (%), NLVR2 (%).\", \"page\": 9, \"index\": 10, \"width\": 782, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-011.webp\", \"caption\": \"Table 6: Notation reference (Part 1): spaces, signals, ITNet operator, and kernel parameterization.\", \"page\": 20, \"index\": 11, \"width\": 764, \"height\": 1054}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-002.webp\", \"caption\": \"Table 7: Notation reference (Part 2): convolution, self-attention, and recurrence (Theorems 1–3).\", \"page\": 21, \"index\": 2, \"width\": 744, \"height\": 1190}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-004.webp\", \"caption\": \"Table 8: Notation reference (Part 3): universal approximation, kernel recovery, implementation, backpropagation, and general symbols.\", \"page\": 22, \"index\": 4, \"width\": 798, \"height\": 1175}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-009.webp\", \"caption\": \"Table 11: Parameter breakdown for ITNet-B (12 layers, d = 768, H = 12).\", \"page\": 56, \"index\": 9, \"width\": 677, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the ITNet architecture. The model consists of L stacked pre-norm residual layers. Each layer applies (i) layer normalization, (ii) the ITNet operator - a learnable integral operator with a kernel depending on positions x, y and features u(x), u(y) and (iii) a feed-forward network (FFN) for local refinement, with residual connections after each block. Positional information is provided through the domain Ω and used directly by the kernel. This design mirrors Transformer-style architectures while generalizing convolution (local kernels), attention (contentdependent kernels), and recurrence (causal kernels) within a unified framework.\", \"page\": 57, \"index\": 1, \"width\": 824, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-005.webp\", \"caption\": \"Table 15: ModelNet40 training hyperparameters.\", \"page\": 72, \"index\": 5, \"width\": 764, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-007.webp\", \"caption\": \"Table 16: Wall-clock throughput and peak memory. Results are reported on ImageNet-1K (vision), GLUE pre-training (language), ModelNet40 (3D point cloud), and VQA v2 (multimodal). Here n denotes the number of input tokens.\", \"page\": 73, \"index\": 7, \"width\": 830, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2606.19538v1/fig-008.webp\", \"caption\": \"Table 17: Rank sweep for ITNet-B on ImageNet-1K validation.\", \"page\": 73, \"index\": 8, \"width\": 744, \"height\": 215}]"
motivation: 现有网络架构的碎片化并非信号处理的根本差异，而是对同一可学习积分变换的不完整认知。
method: 提出ITNet，以MLP实现的可学习核进行积分变换，通过参数化和高效计算技术统一卷积、注意力和循环。
result: 单一ITNet架构在ImageNet-1K、GLUE等任务上达到或超越专用网络性能。
conclusion: 单一可学习积分变换机制能够从数据中同时习得卷积、注意力和循环的行为。
---

## 摘要
卷积网络、循环网络和变换器各自编码了不同的归纳偏置——局部性、顺序记忆和内容依赖的成对交互——且自提出以来在数学上一直保持分离。我们表明，这种割裂反映的不是信号处理方式的根本差异，而是对同一个底层数学对象的不完整认识：一种可学习的积分变换。我们提出了积分变换网络（ITNet），一种围绕可学习核构建的统一架构，该核同时依赖于位置和特征。该核实现为一个小型神经网络（具体为MLP），用于建模成对交互，使模型能够从数据中自适应其行为。我们证明，在适当的参数化下，卷积、自注意力（包括多头）以及自回归循环（包括LSTM、GRU、S4和Mamba）均作为特例出现，并且ITNet是连续算子的通用逼近器。为实现实用性，我们开发了分块核融合、重要性加权蒙特卡洛积分以及可学习的低秩分解，从而实现高效且可扩展的计算。在ImageNet-1K、GLUE、ModelNet40、VQA v2和NLVR2上，具有共享算子和轻量级模态特定编码器的单一ITNet架构，匹配或超越了专用基线。结果表明，单一学习到的交互机制可以从数据中恢复所有三个架构族的行为。

## Abstract
Convolutional networks, recurrent networks, and transformers each encode different inductive biases -- locality, sequential memory, and content-dependent pairwise interaction -- and have remained mathematically distinct since their inception. We show that this fragmentation reflects not a fundamental diversity in how signals should be processed, but rather incomplete views of a single underlying mathematical object: a learnable integral transform. We introduce the Integral Transform Network (ITNet), a unified architecture built around a learnable kernel that depends jointly on positions and features. This kernel is implemented as a small neural network, specifically an MLP, that models pairwise interactions, enabling the model to adapt its behavior from data. We show that convolution, self-attention (including multi-head), and autoregressive recurrence (including LSTM, GRU, S4, and Mamba) arise as special cases under appropriate parameterizations, and that ITNet is a universal approximator of continuous operators. To make this practical, we develop tiled kernel fusion, importance-weighted Monte Carlo integration, and learned low-rank factorization, enabling efficient and scalable computation. A single ITNet architecture with a shared operator and lightweight modality-specific encoders matches or exceeds specialized baselines on ImageNet-1K , GLUE, ModelNet40, VQA\,v2 and NLVR2. The results demonstrate that a single learned interaction mechanism can recover the behavior of all three architectural families from data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机与背景**：当前深度学习领域三大主流架构——卷积网络(CNN)、循环网络(RNN/LSTM)和Transformer——各自拥有独立的数学基础与归纳偏置(CNN关注局部平移不变性，RNN关注顺序记忆，Transformer关注内容依赖的全局交互)。这种架构的"碎片化"迫使研究者在看到数据前就需预设架构，限制了统一处理多模态数据的能力。
- **核心问题与整体含义**：论文指出这种分裂并非信号处理的根本差异，而是对同一底层数学对象的不完整认识。其核心思想是，**卷积、自注意力与循环均可视为一种“可学习的积分变换”的特殊情况**。通过构建一个统一的、基于可学习核的积分变换网络(ITNet)，可以从数据中同时习得这三大架构族的行为，无需分开设计，证明了单一机制统一建模的可行性与优越性。

### 2. 论文提出的方法论
- **核心思想**：定义一个可学习的积分变换算子，该算子的核函数 $\kappa_\theta$ 不仅依赖于空间位置 $x, y$，还依赖于位置上的特征 $u(x), u(y)$，通过积分实现全局信息聚合。其核心公式为：
  $$(\mathcal{K}_\theta[u])(x) = \int_{\Omega} \kappa_\theta(x, y, u(x), u(y)) u(y) d\mu(y) + W_\theta u(x)$$
- **关键技术细节**：
  - **核的参数化**：$\kappa_\theta$ 由一个小型MLP实现，其输入包括查询端与键端的位置、相对位置、距离、特征及其哈达玛积(Hadamard product)，并采用随机傅里叶特征提升对高频空间函数的表达能力。
  - **理论统一性**：通过严格的数学证明（定理1-3与定理4），论文指出：
    - 当核退化为仅依赖相对位置且为恒等矩阵时，算子等价于**卷积**。
    - 当核参数化为特定的softmax点积形式时，算子等价于**自注意力**（含多头注意力）。
    - 当核施加因果约束后，算子可精确恢复**RNN, LSTM, GRU, S4/Mamba**等循环或状态空间模型。
    - ITNet本身是连续算子的**通用逼近器**。
  - **高效计算实现**：为解决精确计算 $O(n^2d^2)$ 的复杂度，提出了三种加速策略：
    1. **分块核融合 (Tiled Kernel Fusion)**：利用Triton在SRAM上分块计算，避免实例化完整核矩阵，减少高带宽内存读写。
    2. **蒙特卡洛近似 (Monte Carlo)**：对每个查询,从可学习的提议分布中采样 $M \ll n$ 个键，实现无偏随机估计，复杂度降为 $O(nMd^2)$。
    3. **低秩分解 (Low-Rank Factorization)**：将核近似为 $\Phi_\theta^\top\Psi_\theta$，将双重积分解耦为一次聚合和一次查询，实现线性复杂度 $O(ndr)$。

### 3. 实验设计
- **数据集与评估基准**：
  - **视觉**：**ImageNet-1K** (图像分类)。
  - **语言**：**GLUE** (自然语言理解，包含CoLA, SST-2, MNLI等任务)。
  - **3D点云**：**ModelNet40** (3D形状分类)。
  - **多模态**：**VQA v2** (视觉问答) 与 **NLVR2** (视觉推理)。
- **对比方法**：在各自基准上与高度优化的专用模型对比，如视觉上的ConvNeXt、Swin Transformer、DeiT；语言上的BERT、RoBERTa、DeBERTa；点云上的PointNet++、PointNeXt；多模态上的ViLT、BLIP等。
- **模型配置**：设计了ITNet-S (22M)、ITNet-B (86M)、ITNet-L (307M)三种规模，拥有共享的核心算子与轻量级模态特定编码器。

### 4. 资源与算力
- **计算平台**：论文中提及使用 **H200-140GB** GPU进行性能分析（如Triton Kernel Profiling），并在大规模训练中使用bfloat16混合精度训练。
- **训练配置**：ImageNet-1K训练包含300 epochs，使用1024的batch size，8块GPU的分布式训练情形。GLUE任务预训练步数为500K。具体的总GPU卡数与训练时长未在全文详细汇总，但根据给出的模型规模和训练超参数，可推断消耗了可观的算力。

### 5. 实验数量与充分性
- **实验数量充足**：
  - **四大领域全覆盖**：在视觉、语言、3D、多模态共计超过10个具体任务/数据集上进行了评估，充分验证了架构的跨领域泛化能力。
  - **多尺度模型比较**：测试了小型(22M)、基础(86M)和大型(307M)模型，并与对应参数量的专用SOTA模型对比（如Table 1-4）。
  - **深入的消融研究**（Table 5, 附录M）：
    - **核输入消融**：系统地移除核的输入项（如相对位置、距离、哈达玛积、特征内容等），证明仅有联合所有组件的核才能取得最优效果。
    - **架构参数消融**：分析了核MLP宽度($w_\kappa$)、傅里叶特征参数(频率数$L$、带宽$\sigma$)、模型深度($L$)等影响。
    - **效率分析**：详细对比了精确、MC、低秩模式在不同序列长度下的吞吐量、显存占用及精度损失(Table 16-19)，并比较了与FlashAttention-2等其他高效注意力机制的效率(Table 18)。
- **客观性与公平性**：实验使用标准基准和官方训练配方，与各自数据集的专用SOTA进行公平比较，报告了至少3次随机种子的均值和标准差。

### 6. 论文的主要结论与发现
- **统一架构的有效性**：单一ITNet架构能在所有四个领域（视觉、语言、3D、多模态）匹配或超越为特定数据设计的专用架构（如ITNet-L在ImageNet-1K上85.8%超越ConvNeXt、Swin等；在GLUE上追平或超越BERT等）。
- **归纳偏置的可学习性与适应性**：消融实验表明，核能够根据任务自适应地调整其对内容与位置的偏重（视觉任务更依赖空间位置，语言任务更侧重内容交互），而不是硬编码。
- **理论统一性的实证支持**：移除核中任何关键成分（如内容、相对位置、哈达玛积）都会导致性能下降，证明了联合建模内容-位置交互的哈达玛积项和相对位置项是超越传统架构（如纯注意力或纯卷积）的关键。

### 7. 优点
- **理论深刻性与统一性**：首次在数学上严格证明并实现了用单一可学习算子精确涵盖CNN、Transformer、RNN（含S4/Mamba）三大主流架构族，并给出了通用逼近性证明，具有极高的理论价值。
- **极强的架构泛化能力**：成功地在四种截然不同的模态和领域取得SOTA或接近SOTA的结果，展示了“一次设计，处处适用”的潜力。
- **务实的系统优化**：不仅停留在理论，还开发了分块融合、MC采样、低秩分解等高效计算方法，使该复杂算子在实际长序列任务中可用，实现了性能与效率的良好平衡。
- **详尽的实验剖析**：消融实验设计全面，从输入、架构、效率等多角度深刻揭示了模型各组件的作用，增强了可解释性。

### 8. 不足与局限
- **计算开销基数高**：虽然提出了加速方法，但在不使用近似的情况下，其精确计算的 $O(n^2d^2)$ 复杂度仍然远高于Transformer的 $O(n^2d)$。在使用近似（如低秩分解）时，仍存在理论上的精度损失。
- **未拓展至超大规模模型**：论文承认尚未将ITNet扩展到十亿参数级的自回归生成任务（如LLM），其在超长上下文语言建模上的扩展性、训练稳定性和最终性能仍有待验证。
- **训练成本**：由于核是MLP且需要考虑内容依赖，其完整反向传播计算图仍比标准Transformer复杂，训练内存和时间成本更高（如文中提到反向传播耗时是前向的2.8倍）。
- **多模态耦合成本**：在联合图像-文本域的设定下，端到端耦合的训练方式增加了计算成本，如何解耦或模块化训练仍是一个开放问题。
- **理论假设与实际应用的差距**：一些理论证明（如恢复卷积）依赖于理想化的数学构造（如狄拉克δ函数），在实际实现中需通过近似来实现限制或边界处理。

（完）
