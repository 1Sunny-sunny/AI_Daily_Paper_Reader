---
title: "BIT-Nav: Brain-Inspired Trajectory Memory for Embodied Navigation"
title_zh: BIT-Nav：脑启发的轨迹记忆用于具身导航
authors: "Rithvik Jonna, Aakash Gurram, Man Namgung, Wyatt Mackey, Tinoosh Mohsenin"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21398v1"
tags: ["query:sr"]
score: 6.0
evidence: 受海马路径整合启发的轨迹记忆方法
tldr: 针对具身导航中视觉语言模型（VLM）因稀疏选帧无法捕捉长程结构化行为信号的问题，受大脑海马路径整合机制启发，提出BIT-Nav框架。通过Bi-GRU编码动作与位姿序列，并以对比学习优化，生成紧凑的轨迹记忆token，以恒定计算成本注入VLM，弥补了稀疏帧表示的不足，提升了长程导航推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21398v1/fig-003.webp\", \"caption\": \"Fig. 2. BITE block. The BITE block consists of a 2-layer bidirectional GRU that processes the agent’s action history as 7-D motion features m1:t, a masked mean-pool that compresses the full sequence into a single vector h̄ ∈ R512, and a multimodal projector MLP that maps h̄ into the token space of Qwen3-VL-8B - adding exactly one trajectory memory token to the LLM input at every decision step, regardless of trajectory length.\", \"page\": 4, \"index\": 3, \"width\": 1052, \"height\": 585}, {\"url\": \"assets/figures/arxiv/2606.21398v1/fig-002.webp\", \"caption\": \"Fig. 3. Architecture comparison for trajectory encoding. Bi-GRU achieves the lowest test loss compared to Transformer, GRU, and LSTM.\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.21398v1/fig-001.webp\", \"caption\": \"Fig. 4. SimCLR-based trajectory retrieval using a Bi-GRU encoder with different input feature dimensions. The 2-D input consists of [is forward,is turn]; the 3-D input uses [∆x, sin θ, cos θ]; the 5-D input uses [cx, cy ,∆θ,is forward,is turn]; and the 7-D input uses [cx, cy , sin θ, cos θ,∆θ,is forward,is turn].\", \"page\": 6, \"index\": 1, \"width\": 514, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2606.21398v1/fig-005.webp\", \"caption\": \"Fig. 6. Evaluation across trajectory lengths. (a) Average input token count per query across both tasks. Legend values are token counts at step 124. (b–c) Accuracy on Q1 and Q2, respectively. Curves are LOWESSsmoothed (bandwidth 0.20)\", \"page\": 7, \"index\": 5, \"width\": 514, \"height\": 586}, {\"url\": \"assets/figures/arxiv/2606.21398v1/fig-004.webp\", \"caption\": \"Fig. 7. Simulation examples of the R2R dataset in Isaac Sim. The first column shows the initial frame, and the second shows the current frame. The third column indicates the agent’s position on the full map; the blue line represents the agent’s trajectory, and the end of the blue arrow indicates the agent’s current position. Columns 5 and 6 present the model’s responses at the current position. Compared to the base Qwen model, BIT-Nav better captures past trajectory information.\", \"page\": 8, \"index\": 4, \"width\": 1052, \"height\": 527}]"
motivation: 现有方法增加历史帧数未能提升精度，瓶颈在于缺乏对转弯、位移等结构化行为历史的表征。
method: 采用Bi-GRU编码相对位姿和动作序列，通过多正例对比学习训练，将嵌入投影为VLM的记忆token。
result: 所提方法能以恒定token成本提供结构化轨迹记忆，增强VLM的长程时空推理能力。
conclusion: BIT-Nav通过仿脑轨迹记忆有效突破了VLM导航中的表示瓶颈，实现了高效的长程导航决策。
---

## 摘要
用于具身导航的视觉语言模型（VLMs）依赖于从不断增长的轨迹历史中选择固定数量的帧。随着回合的延长，这种选择变得越来越稀疏，然而先前的工作表明，当帧数从8扩展到64时，准确率并未提高，这表明瓶颈不在于帧的数量，而在于表示本身。稀疏的帧选择无法捕捉长期推理所需的结构化行为信号：转弯模式、累积位移和路径拓扑。我们提出了BIT-Nav（脑启发的轨迹记忆用于导航），这是一个框架，它用紧凑的学习轨迹记忆来增强冻结的VLM导航流水线。受海马体路径整合的启发，其中空间经验被压缩为结构化的情景痕迹，而不是存储为原始感官回放，BIT-Nav通过在共享相同行为意图的轨迹前缀上使用多正样本InfoNCE对比目标，训练一个双向GRU编码器对动作和相对位姿序列进行编码。得到的嵌入通过一个轻量级MLP投影到VLM标记空间中，并在每个决策步骤作为一个记忆标记注入，以恒定的标记代价为模型提供结构化的运动历史条件，无论回合长度如何。

## Abstract
Vision-Language Models (VLMs) for embodied navigation rely on selecting a fixed number of frames from a growing trajectory history. As episodes extend, this selection grows increasingly sparse, yet prior work shows no accuracy gain when scaling from 8 to 64 frames, suggesting the bottleneck is not frame quantity but the representation itself. Sparse frame selection cannot capture the structured behavioral signal that long-horizon reasoning requires: turning patterns, cumulative displacement, and path topology.   We introduce BIT-Nav (Brain-Inspired Trajectory Memory for Navigation), a framework that augments frozen VLM navigation pipelines with a compact learned trajectory memory. Motivated by hippocampal path integration, where spatial experience is compressed into structured episodic traces rather than stored as raw sensory replay, BIT-Nav trains a Bi-GRU encoder over action and relative pose sequences via a multi-positive InfoNCE contrastive objective on trajectory prefixes sharing the same behavioral intent. The resulting embedding is projected into the VLM token space via a lightweight MLP and injected as a single memory token at each decision step, conditioning the model on structured motion history at constant token cost regardless of episode length