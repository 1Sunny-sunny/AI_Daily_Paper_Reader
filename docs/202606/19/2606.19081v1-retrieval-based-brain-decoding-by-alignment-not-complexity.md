---
title: "Retrieval-Based Brain Decoding by Alignment, not Complexity"
title_zh: 基于检索的脑解码：对齐而非复杂性
authors: "Matteo Ciferri, Matteo Ferrante, Nicola Toschi"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19081v1"
tags: ["query:sr"]
score: 9.0
evidence: 使用基础模型对齐的fMRI检索式脑解码
tldr: 认知科学认为大脑概念以高维向量组织，fMRI信号因时空平均而线性化。本研究探索使用对比目标将fMRI活动映射到基础模型嵌入空间，发现线性对比解码器在图像、文本、音频上均优于岭回归和非线性方法，表明解码增益主要源于训练目标而非架构复杂度，为大脑解码提供了原则性策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19081v1/fig-005.webp\", \"caption\": \"Figure 1: The same linear contrastive model is employed across three experimental conditions, dif fering only in the stimulus modality (audio, textual, or visual). For each modality, neural responses from fMRI are aligned through subject-specific linear transformations and mapped into the corresponding stimulus embedding space (obtained from a pretrained foundation model such as CLIP for images, CL AP for audios, or LLaMA for text) via a contrastive learning objective. Training is carried out independently for each modality. At test time, retrieval is per formed by comparing brain-predicted embeddings with estimated stimulus embeddings.\", \"page\": 2, \"index\": 5, \"width\": 1025, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2606.19081v1/fig-004.webp\", \"caption\": \"Table 1: Retrieval accuracies (mean ± std) per dataset/metric.\", \"page\": 4, \"index\": 4, \"width\": 591, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2606.19081v1/fig-001.webp\", \"caption\": \"Figure 2: (a) Each heatmap represents the cosine similarity between predicted and ground-truth stimulus embeddings, computed sample-by-sample. Results are shown for three datasets (NSD, HUTH, GTZAN) and two models: a linear Ridge regression (lef t column) and the best contrastive learning model (the linear one, right column). The diagonal reflects correct predictions with high similarity between corresponding stimuli, while of f-diagonal values indicate confusion between dif ferent candidates. All similarity matrices are normalized using a row-wise sof tmax to emphasize alignment between prediction and target embeddings. The CL model produces more concentrated diagonal pat terns, indicating superior matching accuracy compared to Ridge approach. (b) Each plot displays the distribution of Pearson correlation coef ficients computed between model predictions (Linear in blue, Non-Linear in orange) and the ground truth stimulus embeddings, evaluated separately for each embedding feature. Dashed ver tical lines indicate the mean correlation for each model. A t-test was per formed for each comparison, testing the alternative hypothesis that the linear model yields higher correlations than the non-linear model. The resulting p-value and ef fect size (Cohen’s d) are repor ted in the legend. Results demonstrate that the linear model consistently achieves significantly higher correlations, with ef fect sizes ranging from moderate (Cohen’s d = 0.50) to large (Cohen’s d > 1), depending on the dataset .\", \"page\": 5, \"index\": 1, \"width\": 964, \"height\": 709}, {\"url\": \"assets/figures/arxiv/2606.19081v1/fig-002.webp\", \"caption\": \"Figure 3: Quantitative bar char ts to visualize decoding results. Stars above the bars reveal significance, according to the table in the Appendix. Double stars indicate pvalue lower than 1e-10.\", \"page\": 5, \"index\": 2, \"width\": 929, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2606.19081v1/fig-003.webp\", \"caption\": \"Figure 4: Random samples of brain decoding results. For each panel, the target column shows the ground-truth stimulus (music track, image, or sentence, depending on the modality), while the neighbor columns display the top retrieved candidates from the model’s latent space based on cosine similarity. (a) Retrieval of images viewed by par ticipants. (b) Retrieval of text/sentences corresponding to the neural response. (c) Retrieval of music tracks from brain activity. These qualitative examples illustrate that the predicted neural embeddings of ten retrieve semantically related stimuli, highlighting the model’s ability to capture meaningful structure in brain representations.\", \"page\": 6, \"index\": 3, \"width\": 1023, \"height\": 673}]"
motivation: 探究对比学习目标是否比模型复杂度更能提升大脑解码性能。
method: 用线性对比解码器将fMRI信号映射到基础模型嵌入空间，并与岭回归、非线性方法比较。
result: 线性对比解码器在多种模态数据集上一致优于其他方法。
conclusion: 大脑解码中，对比-线性模型是有效策略，训练目标选择比架构复杂度更重要。
---

## 摘要
认知科学中一个突出的理论表明，大脑中的概念被组织为高维向量，语义意义由该空间中的方向和相对角度捕获。脑解码旨在从神经活动中重建或检索刺激（或其表示），并涉及寻找一个函数来近似大脑如何表示概念。这推动了对对比目标的研究，将其作为生物学上可行的候选方法来反转大脑损失函数。在这项工作中，我们研究如何将功能性磁共振成像（fMRI）活动与视觉、语言和音频领域基础模型的嵌入空间进行通用映射。尽管神经计算在微观尺度上高度非线性，但 fMRI 测量在空间和时间上平均信号，并进一步被噪声平滑，从而有效地将可观测表示线性化。与这些观点一致，我们在多个数据集上的实验表明，线性对比解码器始终优于岭回归和标准的非线性替代方法，并且这些结果在图像、文本和声音中具有普适性。这些发现表明，解码性能的提升更多地来自训练目标的选择，而非架构的复杂性，这表明对比线性模型是脑解码的一种原则性策略。

## Abstract
A prominent theory in cognitive science suggests that concepts in the brain are organized as high-dimensional vectors, with semantic meaning captured by directions and relative angles in this space. Brain decoding is the effort of reconstructing or retrieving stimuli (or their representations) from neural activity and involves finding a function that approximates how the brain represents concepts. This motivates the investigation of contrastive objectives as biologically plausible candidates to reverse the brain loss function. In this work, we study how functional MRI (fMRI) activity can generally be mapped with the embedding spaces of foundation models in vision, language, and audio. Although neural computations are highly non-linear at the microscale, fMRI measurements average signals across space and time, further smoothed by noise, effectively linearizing the observable representation. Consistent with these views, our experiments across multiple datasets demonstrate that linear contrastive decoders consistently outperform ridge regression and standard non-linear alternatives, and that these results generalize across images, text, and sound. These findings indicate that decoding gains arise more from the choice of training objective than from architectural complexity, pointing to contrastive-linear models as a principled strategy for brain decoding.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景与动机**：
  - 认知科学理论认为人脑中的概念以高维向量形式组织，语义信息由向量间的方向和角度承载。脑解码的目标是从神经活动（如fMRI）中重建或检索刺激的表征，这需要找到一个映射函数来近似大脑的表示方式。
  - 对比学习被认为是一种生物学上合理的优化原则，因为脑内的比较和相似性判断可能天然适合对比目标。同时，fMRI信号由于在时空上平均了大量神经活动，且被噪声平滑，其宏观表现呈现出高度的线性化，使得线性模型在处理fMRI数据时具备理论优势。
- **核心问题**：在脑解码任务中，训练目标（对比损失 vs. 均方误差）和模型架构复杂度（线性 vs. 非线性）哪一个对解码性能的影响更大？
- **整体含义**：该研究旨在证明 **“对比线性”策略** 在多种模态（图像、文本、音乐）的fMRI检索解码中是一种简单、统一且高效的原则性方案，性能增益主要来自训练目标的选择（对齐而非逐点误差最小化），而非增加网络深度。

### 2. 论文提出的方法论
- **核心思想**：使用一个受试者特定的线性映射，通过对比学习目标将fMRI体素活动直接对齐到预训练基础模型（如CLIP、CLAP、LLaMA）的嵌入空间，整个过程完全基于检索式解码，不依赖复杂的生成模型。
- **模型架构**：
  - 模型本质是线性变换的堆叠：形式上表述为 $z = W_o \, W_h \, A_k \, x$，其中 $x \in \mathbb{R}^d$ 是fMRI输入，$A_k$ 是受试者特定的对齐矩阵（将个体映射到共享维度），$W_h$ 和 $W_o$ 是隐藏层和输出层权重。
  - 实际训练中，所有激活函数选择为“Identity”（恒等映射），使得堆叠的线性层等价于一个低秩分解的正则化线性变换，无任何非线性成分。
  - 对比基准：**岭回归**（$L_2$损失）和**浅层MLP**（使用ReLU/GELU等非线性激活，但同样用对比损失训练）。
- **训练目标与算法流程**：
  - 采用NT-Xent对比损失，最大化预测嵌入 $z_i$ 与对应真实刺激嵌入 $y_i$ 的余弦相似度，并排斥同批次其他刺激：
    $$L_{\text{contrastive}} = -\frac{1}{N}\sum_{i=1}^{N} \log \frac{\exp(S_{ii}/\tau)}{\sum_{j=1}^{N} \exp(S_{ij}/\tau)}$$
    其中 $S_{ij} = \frac{z_i^\top y_j}{\|z_i\| \|y_j\|}$，$\tau$ 为温度参数。
  - 训练使用AdamW优化器，基于验证损失早停。
- **评估方式**：计算预测嵌入与测试集内所有真实嵌入的余弦距离，执行最近邻检索，报告Top-1和Top-3准确率（均在受试者内部进行）。

### 3. 实验设计
- **数据集与场景**：
  - **视觉**：Natural Scenes Dataset (NSD)，fMRI数据来自4名受试者观看自然图像，使用General ROI掩模提取GLM beta值，刺激由CLIP图像编码器嵌入（512维）。
  - **语言**：HUTH Language数据集，3名受试者聆听自然故事，选取编码模型相关性最高的前10,000个皮层体素，刺激由LLaMA3-8B词嵌入获得。
  - **音乐**：GTZan fMRI数据集，5名受试者聆听音乐片段，选取音乐响应前3,000个体素，刺激由CLAP音频编码器嵌入（512维）。
- **基准对比**：
  - **方法对比**：线性对比解码器（**Linear-CL**）、带有非线性激活的对比MLP（**Non-Lin. CL**）、标准岭回归（**Ridge Reg.**）。
  - **消融实验**：移除受试者对齐层 $A_k$，考察纯粹解剖对齐模型（**Anat.-CL**）的性能。
  - **额外分析**：ROI级别解码对比（NSD数据集）、嵌入特征相关性分析、不同温度参数敏感性测试，以及采用MindEye2检索协议的额外比较。
- **实验公平性**：所有解码器输出同一目标空间的向量，使用相同的余弦相似度检索协议评估；超参数对每个数据集分别调优（如学习率、dropout、温度、批次大小等）。

### 4. 资源与算力
- 论文未明确说明训练所使用的GPU型号、数量以及具体的训练时长。仅提及使用AdamW优化器和不同的批次大小（128至2048），并与公开数据集的典型fMRI解码任务规模相符，但未提供算力消耗的细节。

### 5. 实验数量与充分性
- **实验组合**：
  - 3个不同模态（图像、语言、音乐） × 3种解码方法（Linear-CL, Non-Lin. CL, Ridge），每个数据集报告Top-1和Top-3准确率。
  - 统计检验：对所有关键结果进行了配对t检验并报告效应量（Cohen’s d），以验证改进的显著性。
  - 消融实验：移除对齐层后的性能退化评估。
  - 特征级分析：皮尔逊相关系数分布对比。
  - 定性可视化：相似度矩阵、检索结果示例。
  - 超参数探索：温度、批次大小、深度、隐层维度等全面扫描（见表A3）。
  - ROI解码：NSD数据上5个功能区的独立评估。
- **充分性与客观性**：实验覆盖了三种感官模态，对比了线性和非线性模型，并进行了严格的统计验证。消融实验和补充分析进一步支撑核心结论，实验设计通过统一框架和公开数据集保证了客观性与可复现性。但由于非线性架构搜索空间巨大，不能完全排除特定复杂非线性模型可能超越线性模型。

### 6. 论文的主要结论与发现
- **训练目标起决定性作用**：线性对比解码器（Linear-CL）在所有模态和所有指标上均一致地、显著地优于岭回归和非线性对比MLP。
- **架构复杂度不带来增益**：引入非线性激活反而导致检索准确率下降，线性映射在嵌入特征相关性上表现更好（效应量中等至大）。
- **对齐原则优于误差最小化**：对比损失通过优化余弦相似度和排斥干扰项，更有效地保留了嵌入空间的几何结构，而岭回归虽在MSE上更低，但检索性能更差。
- **受试者特定对齐有效**：联合训练受试者对齐层能进一步提升解码性能。
- **通用性**：这一策略在视觉、语言、音乐三个领域均成立，且目标嵌入空间不需要本身是“对比学习训练出的”（如LLaMA），说明几何感知型损失函数具有普适优势。

### 7. 优点
- **方法简洁且有效**：仅用单一线性对比模型统一三种模态，实现强性能，验证了“不要过度复杂化”的原则。
- **系统对比研究**：首次在严格控制下隔离了训练目标和架构深度的贡献，填补了已有研究的缺失环节。
- **广泛验证**：覆盖图像、语言、音乐数据集，并结合统计检验与多种消融实验，结论稳健。
- **清晰的理论解释**：将线性模型的成功与fMRI信号的宏观线性化理论、概念向量空间理论相结合，给出了合理机理解释。
- **完全可复现**：提供匿名GitHub仓库及公开数据集，所有实验设置和超参数详细列出。

### 8. 不足与局限
- **非线性架构探索有限**：仅测试了浅层MLP，可能特定设计（如更深的网络、不同归一化、对抗训练）或更大规模数据下非线性模型可能实现超越。
- **时间结构被忽略**：所用实验数据均为GLM beta/HRF平均响应，去除了时间动态。未尝试LSTM、Transformer等时间模型，结论不宜推广到时变信号。
- **未跨数据集泛化**：解码模型仅在单模态内部训练和评估，未测试跨数据集或跨被试的迁移能力。
- **存在伦理风险**：脑解码可提取丰富语义信息，论文虽提及需注意隐私和知情同意，但未提出具体缓解措施。
- **性能上限非最优**：论文目标不是追求检索任务的最优SOTA，而是比较损失函数和深度的相对优势，因此绝对性能并非行业最高。

**（完）**
