---
title: Feature leakage and the identifiability of direct-dependency entropy models of neural activity
title_zh: 特征泄露与神经活动直接依赖熵模型的可识别性
authors: "Houman Safaai, Bernardo L. Sabatini"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01661v1"
tags: ["query:sr"]
score: 8.0
evidence: 神经活动的条件最大熵模型与信息投影
tldr: 生物神经元接收复杂树突输入，但群体活动常用每个输入独立作用的直接模型来预测。研究指出，条件最大熵模型在拟合输出率和输入-输出共活动时，特征泄漏会使高阶交互效应被吸收到低阶参数中，导致模型看似成功预测却未揭示真实机制。通过状态重新加权、条件log-odds对比等诊断方法，可在仿真和CA1海马数据中分离分布内预测与响应规则的恢复，证明直接熵模型解释力应谨慎解读为观测分布下的预测，而非机制证据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01661v1/fig-003.webp\", \"caption\": \"Figure 1: State weighting can determine whether a fixed response table appears first-order. Bubble color is the true response P (y = 1 | x), bubble area is the state weight P (x), shaded backgrounds are best first-order readouts and dashed lines mark the P̂ = 0.5 boundary. a–c A pure XOR-like interaction is well described by a first-order readout under empirical rare-corner sampling (F1 = 0.86) but not under balanced sampling (F1 = 0.00). d–f An asymmetric AND-like interaction shows the opposite masking pattern: empirical sampling misses the high-response corner, while balanced sampling recovers a strong first-order readout. g–i A genuinely additive response remains first-order under both samplings. Thus the diagnostic separates stable first-order response form from first-order attribution that depends on the sampled state distribution.\", \"page\": 4, \"index\": 3, \"width\": 957, \"height\": 1008}, {\"url\": \"assets/figures/arxiv/2606.01661v1/fig-002.webp\", \"caption\": \"Figure 2: Ground-truth controls for distribution-sensitive first-order attribution. (a) A pure higher-order generator with zero linear drive gives very different first-order entropy attribution depending only on input representation and correlation. Seven representations are shown: binary correlated inputs can make the direct model capture much of the recoverable information (ϕdir ≈ 0.84 at ρ = 0.9); skewed positive representations (chi-squared, exponential, half-normal, lognormal) give intermediate attribution between the symmetric and binary extremes; and symmetric Gaussian and uniform representations with centered interaction features give ϕdir ≈ 0 (Supplementary Table S3). (b) The interaction model remains predictively useful on held-out data across the same conditions and representations (colors as in a); positive values mean lower held-out negative log likelihood for the interaction model than for the direct model. (c) The reweighting control is specific: it removes apparent first-order attribution for a higher-order response but preserves a genuine first-order response.\", \"page\": 8, \"index\": 2, \"width\": 967, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2606.01661v1/fig-001.webp\", \"caption\": \"Figure 3: State-weight tuning with the response table P (y | x) held fixed; the x-axis λ sweeps the input-state distribution from balanced (λ = 0) toward a leakage-prone/empirical distribution (λ = 1). We use cube (equivalently local cube) as a shorthand for the conditional response table of a single binary output y to a chosen triple of its binary inputs (x1, x2, x3): the three inputs take 2 3 = 8 joint states—the eight corners of a cube in {0, 1}3—and the table records P (y=1 | x1, x2, x3) at each corner. A CA1 cube is such a table estimated from the mouse hippocampal CA1 recording, with the output y and the three inputs x1, x2, x3 all recorded CA1 neurons. a,b For pure second- and third-order synthetic response tables, the first-order attribution (blue) rises as λ increases, while the correctly specified higher-order model (gray dotted “true rule”) stays fully explanatory; the orange dashed curve is the accompanying projection of higher-order input features onto first-order features. c,d For estimated CA1 cubes—each cube’s response table held fixed while only the weighting of its 8 input states is swept—moving the input-state distribution from balanced toward the empirical CA1 distribution increases both the first-order entropy attribution F1 (c) and the higher-order-to-first input-feature leakage (d); lines are medians and bands are the interquartile range across cubes. The apparent amount of first-order explanation therefore changes because P (x) changes, not because P (y | x) changes.\", \"page\": 10, \"index\": 1, \"width\": 968, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2606.01661v1/fig-004.webp\", \"caption\": \"Figure 4: Local CA1 reweighting diagnostics. a Ordered coskewness-projection coefficients are larger for input pairs selected into the local-cube analysis than across all CA1 pairs, placing the diagnostic subsets in a leakage-prone regime. b Centered higher-order input features have substantial first-order projection under empirical weights; product and balanced weights remove most of that feature leakage (n = 776 cubes). c The empirical-to-balanced F1 drop is not explained by input-feature leakage alone (Spearman ρ = −0.05), showing that the response table matters; shading is point density. d Estimated local CA1 response tables show high first-order attribution under empirical weights that drops under product and balanced weights (boxplots over n = 776 cubes; medians with bootstrap 95% CIs). e Per-cube first-order attribution under empirical versus balanced weights; each point is one local response table, colored as retained (n = 350) or distribution-sensitive (n = 324) of the 674 empirical successes, or gray if below the empirical-success threshold (n = 102, of 776 cubes total). f Counts of retained versus distribution-sensitive tables among the 674 successes.\", \"page\": 12, \"index\": 4, \"width\": 967, \"height\": 924}, {\"url\": \"assets/figures/arxiv/2606.01661v1/fig-005.webp\", \"caption\": \"Figure 5: Raw coactivity prediction is a descriptive test, not an interaction-identification test. a,b In a known higher-order simulation, rare-corner sampling makes a first-order model explain entropy and predict raw higher-order coactivities; product and balanced state weights expose the mismatch. Negative R2 in (b) indicates performance worse than the within-order mean-coactivity baseline. c–e In local CA1 response tables, raw coactivity R2 changes across empirical, product-refit, balanced-refit and empirical-to-balanced transfer controls; the scatter plots show the empirical and transfer cases in raw coactivity space. f Conditional pairwise logit contrasts remain large in retained, distribution-sensitive, and below-threshold local tables, showing that raw coactivity prediction and conditional additivity are different questions. g,h In a known lagged generator, instantaneous features predict delayed coactivities when present and past inputs are highly autocorrelated; the mechanism test is the held-out comparison in (h), where the correctly lagged model improves negative log likelihood across the autocorrelation sweep.\", \"page\": 16, \"index\": 5, \"width\": 969, \"height\": 476}]"
motivation: 探究直接依赖性最大熵模型对神经活动的成功预测是否反映了真实计算机制，还是由于特征泄漏导致的表象。
method: "引入状态重新加权固定 P(y|x) 改变 P(x)、条件 log-odds 对比检验局部可加性，以及时间泄漏控制等诊断手段，分析信息投影中的参数吸收效应。"
result: 仿真中纯高阶响在泄漏采样下能通过一阶检验，但重加权后被正确分类；真实海马数据经重加权后近半数看似一阶的表变为分布敏感，远超加性替代零模型。
conclusion: 直接模型的熵解释度和共活动预测仅反映特定输入分布下的预测能力，不能作为排除高阶或隐藏机制的可靠证据，需泄漏诊断进行甄别。
---

## 摘要
生物神经元在具有分支且电兴奋的树突上接收数千个突触输入，然而群体活动通常用直接的输入-输出规则建模，其中每个输入独立地贡献于一个标量驱动。我们研究了此类模型的成功预测对于神经计算揭示和不揭示的内容。对于匹配输出发放率和成对输出-输入共活动量的条件最大熵模型，直接模型所解释的熵是在采样输入分布下的一种预测度量，而非机制识别检验。一个受限的最大熵拟合是一次信息投影：被省略的交互项、时间项或隐状态项，每当它们与纳入的充分统计量相关时，都可能被吸收进拟合的一阶参数中。对于稀疏相关的二元输入，这种吸收具有显式的协偏度形式。我们引入了将分布内预测与响应规则的恢复区分开来的诊断方法：在保持 P(y|x) 固定时改变 P(x) 的状态重加权、用于局部可加性的条件对数几率对比，以及时间泄露控制。在地面真值模拟中，纯高阶响应在易泄露采样下可能通过一阶熵和原始共活动检验，但在重加权后被正确归类。应用于从 CA1 海马记录中选取的、富集泄露信息的局部表格时，在经验权重下看似一阶的表格中，大约一半在平衡重加权下变为分布敏感的，远高于匹配的可加性替代零假设。因此，直接的熵解释比例和原始共活动预测，应被解释为在观测状态分布下的预测，而非证据表明直接模型之外的机制缺失或微小。

## Abstract
Biological neurons receive thousands of synaptic inputs on branching, electrically excitable dendrites, yet population activity is often modeled with direct input-output rules in which each input contributes independently to a scalar drive. We study what successful prediction by such models does, and does not, reveal about neural computation. For conditional maximum-entropy models that match output rates and pairwise output-input coactivities, the entropy explained by a direct model is a prediction measure under the sampled input distribution, not a mechanism-identification test. A restricted MaxEnt fit is an information projection: omitted interaction, temporal, or hidden-state terms can be absorbed into fitted first-order parameters whenever they are correlated with the included sufficient statistics. For sparse correlated binary inputs, this absorption has an explicit coskewness form. We introduce diagnostics that separate in-distribution prediction from recovery of the response rule: state reweighting that holds P(y|x) fixed while changing P(x), conditional log-odds contrasts for local additivity, and temporal leakage controls. In ground-truth simulations, purely higher-order responses can pass first-order entropy and raw coactivity tests under leakage-prone sampling, but are correctly classified after reweighting. Applied to selected, leakage-enriched local tables from CA1 hippocampal recordings, approximately half of tables that appear first-order under empirical weights become distribution-sensitive under balanced reweighting, far above a matched additive-surrogate null. Thus direct entropy-explained fractions and raw coactivity predictions should be interpreted as predictions under the observed state distribution, not as evidence that mechanisms outside the direct model are absent or small.