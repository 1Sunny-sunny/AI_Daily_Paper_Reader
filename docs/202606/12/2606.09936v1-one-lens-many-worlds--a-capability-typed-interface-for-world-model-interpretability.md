---
title: "One Lens, Many Worlds : A Capability-Typed Interface for World-Model Interpretability"
title_zh: 一镜多世界：面向世界模型可解释性的能力类型化接口
authors: "Bhavith Chandra Challagundla, Sanskar Pandey, Param Thakkar, Rishikesh Mallagundla, Yugandhar Reddy Gogireddy, Wenhao Lu, Hindol Roy Choudhury, Shravani Challagundla, Mohamed Deraz Nasr, Spursh Deshpande"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09936v1"
tags: ["query:sr"]
score: 7.0
evidence: 世界模型可解释性方法统一框架
tldr: 世界模型架构多样（如PlaNet、Dreamer、IRIS、I-JEPA），导致可解释性方法重复实现。WorldModelLens通过能力类型接口统一了世界模型的可解释性工具，定义编码、转换等核心方法和可选头，提供统一hook与缓存层，使分析代码一次编写即可复用，消除了碎片化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09936v1/fig-002.webp\", \"caption\": \"Figure 1: The three-layer design. Adapters expose any world model through one typed interface; the hooked-model wrapper mounts named hook points and a single cache; analyses read and edit activations without referring to any architecture.\", \"page\": 4, \"index\": 2, \"width\": 548, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-003.webp\", \"caption\": \"Figure 2: The same interface, populated differently. Solid teal boxes are heads a family exposes; dashed gray boxes are capabilities it declares absent. The required core (blue) is identical across all families, so analyses target it directly and consult the descriptor for everything optional.\", \"page\": 5, \"index\": 3, \"width\": 636, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-001.webp\", \"caption\": \"Table 1: Implemented backends and the capabilities each declares. The required core (encode, transition, initial state, sample) is identical across every family; the families differ only in which optional heads they expose. The full analysis suite applies to all of them without modification.\", \"page\": 8, \"index\": 1, \"width\": 832, \"height\": 277}]"
motivation: 现有可解释性工具假设Transformer语言模型，缺乏对动作、环境步骤和想象轨迹的支持，导致每种世界模型架构都需从零实现分析。
method: 提出WorldModelLens基板，基于能力类型适配器，要求模型实现四个必需方法和一组可选头，并通过统一hook与缓存层暴露时间索引激活和干预重放。
result: 强化学习和自监督世界模型均成为一等公民，无需相互模仿即可共享可解释性分析。
conclusion: 通过类型化接口，WorldModelLens打破了世界模型可解释性的工具碎片化，实现了“一次编写，处处运行”。
---

## 摘要
世界模型如今构建在截然不同的计算基底上。像 PlaNet 和 Dreamer 系列的潜在循环状态空间模型将观测压缩为循环状态；基于令牌的模型如 IRIS 将观测量化到学习到的码本中，并使用 Transformer 自回归地预测；而联合嵌入预测架构如 I-JEPA 在没有像素解码器的学习潜在空间中进行预测。应用于这些模型的可解释性方法，包括探测、激活修补、稀疏自编码器和意外分析，共享一组通用的原语，但它们要为每个架构从头重新实现，因为现有的钩子和缓存工具假设了一个没有动作、环境步骤或想象推演的 Transformer 语言模型。我们认为这种碎片化反映的是工具而非模型，并且世界模型的共享结构可以用一个小的类型化接口来捕获。我们提出了 WorldModelLens，一个围绕能力类型化适配器组织的开源可解释性基础：每个模型实现四个必要方法（编码、转移、初始状态、采样），并通过一个明确的能力描述符声明一组可选头（解码、奖励、继续、行动者、评论家），这样强化学习和自监督的世界模型都是第一等的，无需互相模仿。一个单一的钩子和缓存层通过这个接口暴露带时间索引的激活、想象推演和干预重放，使得每个分析只需编写一次。

## Abstract
World models are now built on substantially different computational substrates. Latent recurrent state-space models such as PlaNet and the Dreamer family compress observations into recurrent states; token-based models such as IRIS quantize observations into a learned codebook and predict autoregressively with a transformer; and joint-embedding predictive architectures such as I-JEPA predict in a learned latent space with no pixel decoder. The interpretability methods applied to these models, including probing, activation patching, sparse autoencoders, and surprise analysis, share a common set of primitives, yet they are re-implemented from scratch for each architecture because existing hook-and-cache tooling assumes a transformer language model with no notion of actions, environment steps, or imagined rollouts. We argue that this fragmentation reflects the tooling rather than the models, and that the shared structure of world models is captured by a small typed interface. We present WorldModelLens, an open-source interpretability substrate organized around a capability-typed adapter: every model implements four required methods (encode, transition, initial state, sample) and declares a set of optional heads (decode, reward, continue, actor, critic) through an explicit capability descriptor, so that reinforcement-learning and self-supervised world models are first-class without either imitating the other. A single hook and cache layer exposes time-indexed activations, imagination rollouts, and intervention replay over this interface, allowing each analysis to be written once.